# K-one-v2 — Full Rewrite TODO

Stack: **NestJS + TypeScript** backend · **PostgreSQL** · **Redis** · **BullMQ** worker · **Nginx** · keep existing React frontend (adapted). Location: `D:\K-one-v2`.

Decisions (confirmed by user):
- Backend: NestJS + TypeScript
- Frontend: KEEP & adapt existing React SPA (in `D:\K-one\k-one\frontend`) — swap API layer to hit new backend
- Parity: 1:1 API contract + same DB schema (ported MySQL→PostgreSQL)
- Location: new folder `D:\K-one-v2` (PHP original stays as reference)

Scope of original system (for reference):
- 14 PHP classes (~6,300 LOC), 25 API handlers (~3,450 LOC), 22 React pages (~9,100 LOC), 515-line MySQL schema (20 tables)
- Core domain: Inbound, Outbound (FEFO), Stock + Ledger, StockTake, BinTransfer, Picklist, Locations, Products, Customers, Users, Reports, ActivityLog, Excel Import (inbound/outbound/stock/auto), Dashboards

- [x] **S1 Scaffold monorepo** `D:\K-one-v2`
  - npm workspaces: `apps/api` (NestJS), `apps/worker` (BullMQ), `apps/web` (React), `infra/` (docker-compose + nginx), `packages/` (shared types)
  - Deps: pg (or TypeORM / Prisma), ioredis, bullmq, nestjs, exceljs, zod/class-validator, jsonwebtoken
  - NOTE: `apps/api` fully scaffolded. `apps/worker` + `packages/shared` exist as stubs (package.json + tsconfig only — real code in S13). `apps/web` not yet created (deferred to S14/S15 — frontend is the existing React app being adapted). `infra/docker-compose.dev.yml` exists (postgres + redis only).
- [x] **S2 PostgreSQL schema** — port `database.sql` (20 tables) via migration SQL + seed (users, settings, special locations QUA_SHELL/UNALLOCATED/STAGING)
  - Keep exact column names/enums; enums→CHECK constraints or pg enums; `<=>` NULL-safe handling via IS NOT DISTINCT FROM; CASE/COALESCE/GROUP_CONCAT→string_agg; `SET SESSION group_concat_max_len`→no-op
  - DONE: `apps/api/src/database/migrations/001-schema.sql` (457 lines). Seeding via `apps/api/src/database/migrate.ts`.
- [x] **S3 Core infra**: config/env, DB pool/ORM, JWT auth + guards (write/admin), activity logger service, unified error/response shape `{success:true,...}`/`{success:false,message}`, date util (Asia/Jakarta), number generation race-safe helper (IN-/OUT-/PKL-/BTR-/ST- numbers, 20-try retry + fallback)
  - DONE: `config/` (env), `database/` (pg pool + migrate), `auth/` (JWT issue/verify + guards), `common/` (activity-logger, api-exception, exception-filter, date-util, number-gen, pallet), `dispatcher/` (gateway controller + action registry with per-action permissions). Choose `pg` + raw SQL (matches known PHP queries).
- [x] **S4 Master data**: products, customers, locations, users (all handlers from api/handlers/master.php etc.)
  - DONE: `master/master.actions.ts` registers `products` (list/all/detail/create/update/delete), `customers`, `locations` (incl. zone_summary/suggest/available), `users` (admin-only CRUD). Plus `master-data.service.ts` shared lookups (e.g. productOptions for ledger).
- [x] **S5 Stock domain**: list/summary/expiring/by_location/locations, ledger list/repair_all, transfer, adjust (exact balance/pallet math)
  - DONE: `stock/stock.actions.ts` registers `stock` (list/summary/expiring/by_location/detail/locations/transfer/adjust) + `ledger` (list/repair_all). Admin-only: stock adjust, ledger repair_all.
- [x] **S6 Inbound module**: full handler parity (list/detail/stats/create/update/delete/items/pallet-locations/status flow/advance/complete/repair_ledger)
  - DONE: `inbound/inbound.service.ts` (~1045 lines), `inbound.module.ts` wired into `app.module.ts`. Typecheck clean.
- [x] **S7 Outbound module**: full handler parity (list/detail/stats/check_stock/create/add_item FEFO/pick/ship/complete/delete/status)
  - DONE: `outbound/{outbound.service.ts,outbound.actions.ts,outbound.module.ts}` wired into `app.module.ts`. `pickItems`/`ship` implemented. Typecheck + build clean.
- [x] **S8 Picklist module**: create_from_outbound/confirm/complete/delete/update_item/export
  - DONE: `picklist/{picklist.service.ts,picklist.actions.ts,picklist.module.ts}` wired into `app.module.ts`. Ported exactly from `classes/Picklist.php` + `api/handlers/picklist.php`:
    - `createFromOutbound`: pure mirror — early-returns existing picklist id for the outbound, creates header (Draft) + one `picklist_items` row per allocated stock_locations row (or per `calculatePalletDistribution` pallet when no allocations). Level-aware pallet calc. Touches NO stock/ledger.
    - `list/detail/stats/confirm/complete/delete/update_item/export_data` — handler parity (404 `Picklist tidak ditemukan`, write-role guards on write actions, `{data:{picklist,items}}` export shape).
    - `updateItem` omits `picker_id` exactly like PHP (API.md mentions it but Picklist.php doesn't write it).
    - Schema fix: `picklist_items.outbound_item_id` was missing from the S2 port (MySQL `database.sql` lacks it too, but the live PHP code INSERTs it in `createFromOutbound` and JOINs it in `getItems`) → added `outbound_item_id BIGINT REFERENCES outbound_items(id)` to `001-schema.sql`. NOTE: since the migration was already recorded as applied, existing DBs need `ALTER TABLE picklist_items ADD COLUMN IF NOT EXISTS outbound_item_id BIGINT REFERENCES outbound_items(id);` — run manually or drop `schema_migrations` entry.
  - Typecheck + `nest build` clean.
- [x] **S9 StockTake module**: create/auto_load/start/save_counters/advance/finish/review/apply_adjustment
  - DONE: `stocktake/{stocktake.service.ts,stocktake.actions.ts,stocktake.module.ts}` wired into `app.module.ts`. Ported exactly from `classes/StockTake.php` + `api/handlers/stocktake.php`:
    - `list` → `{rows, stats}` (getStats: total/this_month/this_year/avg_accuracy — avg over Adjusted takes this year, corr. subqueries, `?? 100` fallback); `detail` → 404 `Stock take tidak ditemukan`, `{stock_take, items, accuracy, locked_locations}`; `stats`; `get_locations` (stock DISTINCT location, qty>0); `get_scope_locations` (available excluding locked).
    - `create` → take_number `ST-YYYYMMDD-####` (Math.random 0..9999, no collision check), scope_type `location` iff scope_locations non-null && != `'[]'` else `full`, stores scope_locations JSON/string as-is; auto-loads items from selected locations or all (excludes QUA_SHELL/STAGING, Available qty>0).
    - `add_item`/`auto_load`/`update`/`delete_item`/`delete` — addItemFull computes status Plus/Minus/Clear from difference; delete in tx (items then header).
    - `start_counting` (Draft→Counting, round c1), `save_counters` (flat itemId→{c1,c2,c3} map), `advance_to_c2` (saveC1 then round c2; saveC1/saveC2 shape is `{itemId: value}`), `finish_counting` (tx: saveC2 if given, re-snapshot qty_system from current Available stock with NULL-safe batch `IS NOT DISTINCT FROM`, recompute qty_physical from counters precedence c1==c2→c1, else c3, c2, c1, else 0; difference±0.001→status; →Review), `save_review` (qty_physical edit), `apply_adjustment` (admin; tx: adjust/insert/delete stock rows, ledger ADJUSTMENT entries with balance from last ledger row, note `Stock Take Adjustment +3`; →Adjusted).
    - Per-role guards: write on all write actions; `apply_adjustment` → admin.
    - Schema fix: `stock_take` gained `scope_locations TEXT`, `scope_type VARCHAR(20) DEFAULT 'full'`, `counting_round VARCHAR(10)`, and status CHECK expanded to `('Draft','Counting','Review','Adjusted','Completed','Cancelled')` (was missing Counting/Review/Adjusted). Existing DBs need `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` + re-check (drop CHECK, add new one).
  - Typecheck + `nest build` clean.
- [x] **S10 BinTransfer module**: create/execute/cancel
  - DONE: `bintransfer/{bintransfer.service.ts,bintransfer.actions.ts,bintransfer.module.ts}` wired into `app.module.ts`. Ported exactly from `classes/BinTransfer.php` + `api/handlers/bintransfer.php`:
    - `generateNumber`: prefix `BTR-YYYYMM-` (monthCompact), latest LIKE desc, seq=suffix+1, 20 tries with existence check, fallback `prefix + His + rand(10,99)`.
    - `list` → `{rows,total,page,per_page(200),statuses:Pending/Completed/Cancelled}` (id int-cast); `detail` → 404 `Transfer tidak ditemukan`, `{transfer}`; `locations_with_stock` (GROUP BY location,uom: total_qty, earliest_expiry, batch_count, excludes STAGING); `stock_at_location` (FEFO expiry order; without location excludes STAGING).
    - `create` (write): uppercase trim from/to locs; validates against `location_master` unless QUA_SHELL/STAGING; rejects same from==to; requires available stock at from-loc; totalAvail check → conflict `Stok tidak cukup. Tersedia: X.XX — Diminta: Y.YY`; inserts Pending. All errors → 409 (PHP catches Throwable → json_err 409).
    - `execute` (write): tx — FEFO-deduct src stock (DELETE at 0, else UPDATE), drains one Available stock_locations pallet (status Picked at 0), merges dest stock by product/location/batch (IS NOT DISTINCT FROM) else INSERT (pallet=ceil(qty/uom_per_pallet??4), manufacture_date NULL), writes `TRANSFER_OUT` (qty_out) + `TRANSFER_IN` (qty_in) ledger rows with **same** `currentBalance` (PHP forces balance before insert), marks Completed + completed_by.
    - `cancel` (write): only Pending may cancel, else 409 `Hanya transfer berstatus Pending yang dapat dibatalkan`.
    - Schema fix: `stock_ledger.transaction_type` CHECK widened to include `TRANSFER_IN`,`TRANSFER_OUT` (MySQL enum in `database.sql` is stale — live PHP writes these and Inbound repair filters them). Existing DBs: drop/re-add CHECK.
  - Typecheck + `nest build` clean.
- [x] **S11 Dashboard + reports + activitylog + system(reset)**
  - DONE: `report/{report.service.ts,report.actions.ts,report.module.ts}` wired into `app.module.ts`. Ported from `classes/Report.php`, `classes/ActivityLogger.php`, `api/handlers/report.php`, `api/handlers/activitylog.php`, `api/handlers/dashboard.php`, `api/handlers/system.php`:
    - `dashboard` stats: KPI (total_drums, total_pallets, expiring_soon (45d), expired_items, dues_in, receiving_now, pending_outbound, dispatched_today, received_today, today_inbound, today_outbound, stock_by_uom), expired_detail, stock_summary, monthly_activity (last 6 mo), stock_by_location, recent_activity, pending_inbound/outbound. `aisle_detail` requires `aisle` (400 `aisle required`), returns `{locations, stats:{aisle,total,occupied,total_qty (number_format comma string),total_pallet}}`.
    - `report` daily (wrapped `{report:{date,date_to,stock_summary,inbound_activity,outbound_activity,expiring_items,low_stock,ledger_summary}}`), products, inbound, outbound (via InboundService/OutboundService getAll then order_date string filter), stock, ledger (LIMIT 5000).
    - `activitylog` list (module filter, LIMIT default 200, action_label + module_icon maps), modules (DISTINCT module).
    - `system` reset_operational_data (admin; single `TRUNCATE TABLE <16 tables> RESTART IDENTITY` tx) + RESET_OPERATIONAL_DATA activity log.
  - Typecheck + `nest build` clean.
- [x] **S12 Import**: inbound/outbound/stock/auto Excel import + templates — exact header strings, error messages (Indonesian), log formats
  - DONE: `apps/api/src/import/{import.helpers.ts, import.service.ts, sheet.reader.ts, import-templates.ts, import.actions.ts, import.module.ts}` wired into `app.module.ts` + registered in gateway.
    - `sheet.reader.ts`: SheetJS (`xlsx`) reads .xlsx/.xls/.csv into a 2D grid matching PHP `toArray(null,true,false,false)` (raw values → Excel serials for dates); own CSV parser handles quotes/BOM.
    - `import.service.ts`: `stockParse/stockValidate/stockPreview/stockCommit` (single tx, modes add/replace/skip, auto-create products with liters 209.00/max_sku 44/max_trans 80, ledger `IN` rows `IST-YYYYMMDD-NNNN` ref 'Stock Import', balance = Available SUM, notes fallback 'Direct stock import'); `runInbound` (header detect, auto-create products, grouping by Shipment No → inbound order no → GR date → new number, status 'Dues In', carrier_name form field, `IN-YYYYMM-` numbers); `runOutbound`/`outboundProcessSheet` (Planning Outbound keyword header detect, 1 shipment = 1 order, customer auto-create `OUT-XXXX`, destinations, inline FEFO, delete empty orders); `runAuto` (single tx, sheet classify master/wms/putaway/schedule/skip, `autoProcessMaster` inferUom Bags/Drum/Pail/Carton, `autoEnsureProducts`, `autoCreateInbound` grouped by GR date 'Auto import (WMS) — GR: {gr}').
    - Transaction parity: `outboundProcessSheet` honors an already-open transaction (PHP `$db->inTransaction()`) — accepts an optional client so `runAuto` keeps one tx.
    - `import.actions.ts`: registered 'import' actions (`tpl_inbound/tpl_outbound/tpl_stock` public; `inbound/outbound/stock_preview/stock_commit/auto` → write permission). `_binary` marker streams template .xlsx via gateway (StreamableFile + Content-Disposition).
    - `gateway.controller.ts`: `@Res({passthrough})` + StreamableFile for binary template downloads; `main.ts` adds global multer memoryStorage `.any()` (512MB limit) for multipart uploads.
    - Frontend contract verified: stock_preview `{message,stats,rows}` with `_row_num/_errors/_warnings`; stock_commit `{rows,mode}` JSON body; inbound/outbound `{message,processed,stats,errors/log,has_errors}` matching ImportPage.tsx `ImportResult`; auto stats fields matching AutoImportPage.tsx `AutoStats`.
  - Typecheck + `nest build` clean.
- [x] **S13 BullMQ worker**: async auto-import job + Redis locks (FEFO/stock-take locks)
  - DONE: `apps/worker/src/{main.ts, import.engine.ts, sheet.reader.ts}` — real BullMQ Worker on queue `kone:import`, pg Pool, runs the standalone auto-import engine (`runAutoImport`) in a single DB transaction, writes task status to Redis (`kone:task:<id>`), reads the uploaded buffer from `kone:file:<id>`. Fixes the pre-existing worker `TS18003` (root `npm run typecheck` now clean).
  - `packages/shared/src/{import-helpers.ts, date-util.ts, redis-lock.ts, queue.ts}` — pure helpers, `RedisLock` (SET NX PX + Lua release), `withLock`, queue/lock/task key constants; `@k-one/shared` index re-exports them.
  - API async path (spec-3 §3.6): `import::auto_async` (write) stores the uploaded buffer in Redis, enqueues the BullMQ job, returns `{message:'Import sedang diproses', task_id}`; `import::task_status` polls `{status:'queued'|'processing'|'done'|'error', message, result}`. New `ImportQueueProvider` (ioredis + Queue + QueueEvents) wired into `ImportModule`. Sync parity path (`import::auto`) unchanged.
  - Redis locks (fail-open): `RedisLockService` in CommonModule (`runLocked` — proceeds unlocked if Redis down so the sync parity path never breaks). Applied to critical sections: `outbound::pick_items` + `outbound::ship` under `LOCK_KEYS.fefo(orderId)`; `stocktake::finish_counting` + `stocktake::apply_adjustment` under `LOCK_KEYS.stocktake(stockTakeId)`.
  - Typecheck + `nest build` (api) + `tsc` (worker) clean.
- [x] **S14 infra**: docker-compose (postgres, redis, api, worker, web) + Nginx proxy (+ existing React build served)
  - DONE: `infra/docker-compose.prod.yml` (postgres+redis+api+worker+web, healthchecks, `name: k-one-v2`, host web port 8081, internal postgres/redis), `infra/docker-compose.dev.yml` (unchanged reference), `infra/{api,worker,web}.Dockerfile`, `infra/nginx.conf` (serves SPA, proxies `/k-one/api/` and `/api/` → api:3000 with 512m upload + long timeouts), `infra/.env.example`, `infra/README.md`.
  - Fixed monorepo build layout: added `apps/api/tsconfig.build.json` + `apps/worker/tsconfig.build.json` that set `rootDir: src` and resolve `@k-one/shared` from its built `dist` (so `dist/main.js` is fresh, not nested `dist/apps/...`). Root `build` now runs `build:shared` first. Verified clean outputs `apps/api/dist/main.js`, `apps/worker/dist/main.js`, `packages/shared/dist/index.js`. `docker compose -f docker-compose.prod.yml config` validates.
  - Note: web image expects `./frontend` in repo root (xcopy from `D:\K-one\k-one\frontend`) — documented in infra/README.md.
- [x] **S15 Frontend adapt**: point `@/lib/api` at the new backend; implement the PHP print/export binary endpoints in v2 so no page still calls the PHP server; verify pages; fix any field-shape drift
  - Frontend API base: `frontend/src/lib/api.ts` `DEFAULT_BASE` → `http://localhost:3000` (VITE_API_BASE overrides; prod served by nginx at `/k-one/api`); `vite.config.ts` dev proxy retargeted to `http://localhost:3000` for `/k-one/api` + `/api`. Frontend `npm run build` clean.
  - New **`export` module** (`apps/api/src/export/`): `ExcelExportService` (ExcelJS) + `PrintService` (inline HTML) + `ExportActions` + `ExportModule`, wired into `app.module.ts`. All are authenticated (`any` role; `print::picklist` → `write` since it can create a picklist).
    - `export::` XLSX actions (gateway `_binary` → StreamableFile): `inbound` (status), `outbound` (status), `customers`, `products`, `ledger`, `stock` (stock.php?export=excel parity — per-location detail, `calcExpiry` port), `stocktake` (stocktake.php?action=export parity — detail + accuracy summary), `report` (type=daily|stock|expiring, date/date_to).
    - `print::` inline HTML actions (gateway `_html` → text/html, `window.print()` in new tab): `inbound_receipt`, `putaway`, `outbound_do`, `surat_jalan`, `picklist` (accepts `id` OR `outbound_id` → idempotent `createFromOutbound` then render), `report`.
  - Frontend repointed (webBase() → `apiHref(module, action, params)` which appends the token query param; gateway `resolveUser` accepts `req.query.token`): InboundList→`export::inbound`, OutboundList→`export::outbound`, CustomersPage→`export::customers`, ProductsPage→`export::products`, LedgerPage→`export::ledger`, StockPage→`export::stock`, StockTakeDetail→`export::stocktake`, ReportsPage→`print::report`+`export::report` (daily passes date/date_to), InboundDetail→`print::inbound_receipt`+`print::putaway`, OutboundDetail→`print::outbound_do`+`print::surat_jalan`+`print::picklist` (outbound_id), PicklistDetail→`print::picklist`. `webBase()` helper left in api.ts (unused by pages).
  - Gateway `_html` branch added to `gateway.controller.ts` (return types widened to `Promise<Record<string,any> | StreamableFile | string>`).
  - Typecheck (api + worker) + `nest build` (api) + frontend `npm run build` all clean.
- [x] **S16 Verify**: parity tests against PHP API responses (contract from frontend/API.md); smoke test with real workbook
  - Docker infra up (`k2-postgres` host 5544, `k2-redis` host 6389); API on :3000 + worker on queue `kone-import` (BullMQ v6 forbids `:` in queue names → `QUEUE.IMPORT` changed `'kone:import'`→`'kone-import'`).
  - Verified live: `auth::login`; all 13 core JSON modules (dashboard::stats, products, customers, locations, stock, ledger, inbound, outbound, picklist, stocktake, bintransfer, report::daily, activitylog); `export::` XLSX (customers/products/stock/ledger/report/stocktake + inbound/outbound with data); `print::` HTML (inbound_receipt, putaway, outbound_do, surat_jalan, picklist by id & outbound_id — idempotent, report daily/stock/expiring/stocktake); import (stock_preview/commit, inbound, outbound, auto sync + async `auto_async`+`task_status`); full stocktake workflow (create/auto_load 1099 items/start_counting/save_counters/advance_to_c2/finish_counting/save_review/stats).
  - User's real workbook `Warehouse Management System_11 Agustus 2026_.xlsx` (21 sheets, SAP data) imports cleanly: 366 products created, 188 updated, 1161 stock, 70 inbound orders/1100 items, 13 outbound orders/64 items, 16 analysis sheets skipped.
  - **Bugfixes during S16** (PG semantics vs PHP/MySQL patterns):
    1. Ad-hoc columns missing from `001-schema.sql`/live DB (live MySQL has them; PHP INSERTs them): `outbound_items.customer_id`, `picklist_items.outbound_item_id` (migration already had it — only live DB needed ALTER), `stock_take` rich columns (`scope_locations`, `scope_type`, `counting_round` + status CHECK `Counting/Review/Adjusted`) — migration had them, live DB was stale. **Root cause: live dev DB predates later migration edits → recreated dev DB from migration (`DROP SCHEMA public CASCADE` + `npm run migrate`)** to guarantee schema parity.
    2. `outbound_destinations.street_address` didn't exist (PG column is `ship_to_street`) → import used `street_address` → fixed to `ship_to_street` (also `print.service.ts` surat_jalan read `street_address` → `ship_to_street`).
    3. MySQL try/catch-continue patterns poison a PG transaction ("current transaction is aborted" masked the real error). Removed swallow-and-fallback around `outbound_items` INSERT, `outbound_item_locations` bind, customer upsert, and `outbound_destinations` insert in both `import.service.ts` and worker `import.engine.ts` (real errors now propagate cleanly; ON CONFLICT handles the duplicate cases).
    4. `createOutbound` (API + worker) used shipment_number as order_number → duplicate-key on re-import. PHP always generates `OUT-YYYYMM-NNNN` → changed to always generate; prefix aligned to PHP (`OUT-` + YYYYMM).
  - Empty-data export errors return 400 `{success:false,message}` (ApiException.badRequest, not plain Error→500); ledger export fixed (removed MySQL no-op `GROUP BY sl.id` — PG rejects joined-table columns).
  - Root typecheck + api/worker `nest build` clean.
- [x] **S17 Verify detail paths**: outbound pick/ship, picklist, inbound receipt/putaway, bin transfer, prod compose up
  - Outbound (real data): imported items start `in_process_status='Goods Received'` → `outbound::update_item_status {item_id,outbound_id,status:'ATP'}` first; `pick_items` FEFO-drains `stock` (3→3160→2500, 137→928→717) and blocks on active stocktake ("Picking diblokir — lokasi … sedang dalam sesi Stock Take aktif" — parity); `ship` writes ledger OUT with correct running balance (order 2: 660/211 OUT rows); `complete` → status Completed. Stocktake id=2 (Review) deleted to clear the pick lock.
  - Picklist: `create_from_outbound` idempotent (2nd call reuses PKL), `confirm` → Confirmed (confirmed_at), `update_item {item_id, picked_quantity, status}` → Picked (picked_at), `complete` → Completed, `print::picklist` still renders post-complete. `confirm`/`complete` are plain UPDATEs (PHP parity — no existence check).
  - Inbound: `advance_status {id,status:'Receiving',received_by_id,received_date}` → Receiving (requires received_by + date); `save_pallet_locations` (rejects unknown locations 400); items → ATP via `update_item_status`; `complete` blocks (409) while any item not ATP/Unserviceable; then Completed. Verified stock created, stock_locations linked.
  - BinTransfer: `locations_with_stock`/`stock_at_location`; `create` validates both locations against master + insufficient-stock 409; `execute` moves stock (source deducted, dest created/merged) + ledger TRANSFER_OUT/IN (same balance — PHP parity), re-execute 409; `cancel` only from Pending, execute-on-cancelled 409; BTR-YYYYMM-NNNN sequence.
  - **Bugfixes (PG semantics surfaced by live tests)**:
    1. MySQL-style `UPDATE tbl a SET a.col=…` alias-in-SET is invalid in PG → `column "oi" of relation "outbound_items" does not exist` broke `inbound::complete` (via `syncBatchToOutbound`) and `updateItemDates` (`UPDATE stock s SET s.…`). Fixed all 3 occurrences (inbound.service.ts) to bare column names (PG accepts alias in FROM only).
    2. `inbound::complete` duplicated stock: import stock snapshot already materializes stock for (product,batch,location), but complete() INSERTed a fresh row → duplicate Available rows (product 17 got two 23-unit CA01A02 rows). Fix: delete unlinked Available/Dues-In/Pending stock at (product,batch,location) before INSERT in both pallet and non-pallet branches → complete is now idempotent (re-complete safe, verified row count stable).
    3. `bintransfer addLedger` INSERT had 13 columns but 14 VALUES expressions (`CURRENT_DATE` + `$1..$13`) and only 12 params → "INSERT has more expressions than target columns" on `execute`. Fixed to `$1..$12` (matches PHP `_addLedger` CURDATE() + 12 `?`). Balance semantics kept identical to PHP (same `currentBalance` passed to both TRANSFER_OUT and TRANSFER_IN).
  - **docker-compose.prod validation** (isolated project `k2v` + renamed containers/ports so running dev infra was untouched):
    - `docker compose config` valid; api/worker/web images all build; nginx rewrite/proxy validated end-to-end (scratch nginx container → live API: `/k-one/api/index.php?module=…` returns correct JSON).
    - Found + fixed: frontend always calls `${BASE}/index.php` → old nginx `proxy_pass http://api:3000/index.php` produced `/index.phpindex.php`. Now `location ^~ /k-one/api { rewrite … index\.php$ /index.php break; proxy_pass http://api:3000; }` (same for `/api`).
    - Found + fixed: API image couldn't migrate (dist had no `.sql`). Added `compilerOptions.assets` (migrations → dist) in `nest-cli.json`, api.Dockerfile CMD now `node …/migrate.js && node …/main.js`. Also `incremental` TS builds silently skipped JS emit in Docker because the copied local `tsconfig.build.tsbuildinfo` marked everything up-to-date → added `**/*.tsbuildinfo` to `.dockerignore` (plus `**/node_modules`, `**/dist`, logs).
    - Full stack boot validated: postgres+redis+api+worker up, migrate-on-boot applied `001-schema.sql` (schema_migrations row), seed present (4 users, 3 locations), login + dashboard::stats through dockerized API work; worker listens on `kone-import`. Stack torn down cleanly afterward (dev infra + local :3000 API untouched).
  - Root typecheck + api build clean.

Every spec detail needed for the port is recorded in `docs/spec-*.md` (produced by subagents 1–3).

EXACT references to keep handy are in session.md -> Specs section.- [x] **S18 Ledger reconciliation (Option 1)**: imported inbound orders = already-received history
  - Snapshot stays the single stock+ledger source (`Stock Import` rows); imported inbound orders imported as Completed / items ATP+Accepted, no new stock/ledger.
  - Edited all 3 import sites: worker `import.engine.ts` + api `import.service.ts` (`autoCreateInbound` x2, `runInbound`).
  - Validated all 3 paths on isolated scratch DBs: orders Completed, items ATP/Accepted, `stock_ledger` has only the single `Stock Import` row (no `Inbound` double-count).
  - Cleaned up scratch stacks/DBs/redis db1; dev API (:3000) + worker rebuilt and running.
  - session.md Decision log 17; root typecheck + build clean.
- [x] **S18b Import header aliases**: schedule `ship to name`/`ship to location` + master `code` now recognized
  - Shared constants `SHIP_TO_NAME_KEYS`/`SHIP_TO_LOC_KEYS`/`MASTER_PRODUCT_CODE_KEYS` added to `packages/shared/src/import-helpers.ts` AND the API local copy `apps/api/src/import/import.helpers.ts`; used at all 12 outbound ship-to sites (worker + api) + master code resolution.
  - Validated end-to-end on scratch DB without a pre-seeded customer: master processed, customer auto-created (PT Test Dua), outbound linked; Option-1 statuses intact. Stack torn down.
  - session.md Decision log 18 (supersedes the ''not touched'' note in 17); root build + typecheck clean.

# Department-Based Roles & Dashboards (from `new-roles.md` — Phase 0)

> This is the foundational access/routing/UI phase. Run BEFORE Phase 1+. Purely additive — must NOT change business logic.
> Full spec: `new-roles.md` (confirmed permission matrix included). Mapping confirmed by user 2026-08-14.
> **STATUS: DONE — code + build + live manual QA all pass.**

Design: two independent fields — `users.role` (seniority, unchanged) + NEW `users.department` (`inbound|outbound|inventory|all`, default `all`).

Confirmed permission matrix (source of truth, do NOT re-derive):
| dept | CAN | CAN'T |
|---|---|---|
| inbound | receive, putaway, view own pending receipts, scan during putaway | picklists, ship outbound, stock takes, bin transfers, edit master |
| outbound | open picklists, confirm picks, ship orders, scan during pick | receive inbound, stock takes, bin transfers, edit master |
| inventory | stock takes, bin transfers, view stock, scan during take/transfer | receive inbound, create/ship outbound, edit master |
| all | everything + users/master/reports | — |

Rules: NO cross-department READ in this phase. Scanning is bundled with its supporting action (no `can_scan` flag). master/report/import/export → `all` only. `department==='all'` always passes the guard.

Action→department mapping (confirmed):
- Module-level: `auth`=none; `inbound`=inbound; `outbound`=outbound; `picklist`=outbound; `stocktake`=inventory; `bintransfer`=inventory; `stock`=inventory; `ledger`=inventory; `dashboard/report/activitylog/system`=all; `import/export/print`=all; `products`=all; `customers`=all; `locations`=all; `users`=all.
- Read-only lookups opened to depts (user confirmed): `customers::all` → outbound (OutboundList customer picker); `locations::all` → inventory (BinTransferPage picker). Both are pure-read actions (verified master.actions.ts customerAll→customerOptions, locationAll→locationOptions).
- Product search: user chose to OPEN `inbound::search_products` to inventory too (StockTakeDetail:122 + BinTransferPage:178 call it) instead of adding stocktake/bintransfer search_products. So `inbound::search_products` override = ['inbound','inventory'].

## Progress (2026-08-14, in-flight — pick up here)
- [x] Migration: `004-departments.sql` EXISTS + APPLIED to dev DB (verified schema_migrations has it; `users.department` column present, default 'all', CHECK inbound|outbound|inventory|all). Do NOT recreate.
- [x] `auth/guards.ts`: `CurrentUser.department` + `DEPARTMENTS`/`isDepartment` exports; `resolveUser` SELECT now includes `u.department` (default 'all' fallback).
- [x] `auth/auth.actions.ts`: login response `user` now includes `department`.
- [x] `dispatcher/registry.ts`: added `setModuleDepartments(module, deps[])`, `setActionDepartments(module, action, deps[])`, `getDepartments(module, action)` (override wins over module default; unset = no restriction).
- [x] `dispatcher/gateway.controller.ts`: after role checks, `if (depts && user.department !== 'all' && !depts.includes(user.department)) → 403 'Akses ditolak. Department Anda tidak memiliki izin untuk modul ini.'`  Import `getDepartments`.
- [x] `master/master.actions.ts` user CRUD: `userList` returns `department` in rows + `departments` option list; `userCreate`/`userUpdate` accept+validate `department` (isDepartment, default 'all'). [done]
- [x] Module rules applied in all constructors: inbound (+search_products override), outbound, picklist, stocktake, bintransfer, stock+ledger, report (dashboard/report/activitylog/system→all), import (all), export+print (all), master (products/customers/locations/users→all + customers::all→outbound + locations::all→inventory). [done]
- [x] Frontend `lib/api.ts`: `User.department` + `Department` type + `DEPARTMENTS` const + `departmentHome()`. `AuthContext.tsx` exposes `department` (normalized, default 'all'); `login` now returns the `User`. [done]
- [x] Frontend `pages/Login.tsx`: redirects to `departmentHome(u.department)` after login (and on mount when already authenticated). [done]
- [x] Frontend `App.tsx`: `/` → `HomeRedirect` (by department); `/dashboard` = combined; `/dashboard/inbound|outbound|inventory` = new dept dashboards; `*` still → `/`. [done]
- [x] New pages: `DashboardInbound.tsx` (stats: this_month/dues_in/receiving/done + queue table), `DashboardOutbound.tsx` (outbound stats + picklist stats + open orders + active picklists), `DashboardInventory.tsx` (stocktake stats + active stock takes + pending bin transfers). Reuse Card/EmptyState/StatusBadge/fmtNum/fmtDate. [done]
- [x] `components/Layout.tsx`: NavItem/NavSection gained optional `depts`; nav filtered by `department` (section + item level); added **Picklist** nav link (outbound+all); pageTitle map gained `picklist`; header shows dept badge when != 'all'. [done]
- [x] `pages/UsersPage.tsx`: department column + form select (dept options from API, fallback to `DEPARTMENTS` const); create/update payload + toggleActive include department. [done]
- [x] Verify: api typecheck ✓, web typecheck ✓, api `nest build` ✓, web `vite build` ✓ (only pre-existing chunk-size warning). [done]
- [x] **Manual QA — DONE (live, dev API on :3000)**:
  - Created QA users via `users::create` per dept: qa_inbound(inbound), qa_outbound(outbound), qa_inventory(inventory), qa_super(all). `users::list` returns `department` + `departments` options (inbound/outbound/inventory/all).
  - **Allowed matrix verified**: inbound→`inbound::list` + `inbound::search_products`; outbound→`outbound::list`, `picklist::list`, **`customers::all`**; inventory→`stock::list`, `stocktake::list`, `bintransfer::list`, **`locations::all`**, **`inbound::search_products`**; all→outbound/stock/dashboard stats.
  - **Denied matrix verified (all 403)**: inbound→outbound/stock/customers/locations/picklist; outbound→locations/inbound/stocktake; inventory→outbound/inbound.
  - `users::list` for `qa_super` (operator, dept all) → 403 because `users` is admin-role action (correct role check, independent of dept).
  - `userCreate` with invalid `department:'evil'` → 400 Indonesian message. QA users deleted after test.
  - Dev server started with `npm run dev` (env: DB 5544, redis 6389), then stopped.

# WMS Upgrade (from `deepseek-wms-upgrade-prompt.md`)

Run order: **Phase 1 → 5 → 2 → 3 → 4** (audit-trail early since Phase 1 adds a new ledger-mutation path). Full detail + file references in `docs/spec-4-wms-upgrade.md`.

- [x] **S19 = Phase 1 — Stock Hold / Quarantine Status** (highest priority, smallest blast radius)
  - Migration SQL first: `stock` gains `hold_status VARCHAR(20) NOT NULL DEFAULT 'available'` (CHECK `available|on_hold|quarantine|damaged`), `hold_reason TEXT`, `hold_by BIGINT REFS users`, `hold_at TIMESTAMP`.
  - New dispatcher actions `stock::hold` / `stock::release` (write-role, reuse auth module). Stock ledger write on each (match `addToLedger`/`addLedger`/`apply_adjustment` patterns; widen `transaction_type` CHECK if a new type is chosen).
  - Exclude `hold_status != 'available'` from ALL picking/allocation queries: outbound `pickItems`/`addItemWithFEFO`/`getAvailableStock`/`getFEFOAllocation`, picklist `createFromOutbound`, stocktake `autoLoadByLocations`.
  - Web UI: hold indicator + hold/release buttons on `apps/web/src/pages/StockPage.tsx`.
  - **DONE (2026-08-14)**:
    - Migration `003-stock-hold.sql` + updated `001-schema.sql` (fresh-install columns + widened `stock_ledger.transaction_type` CHECK to include `HOLD`,`RELEASE`). Applied to dev DB (k2-postgres) — verified columns + both CHECK constraints.
    - `stock.actions.ts` added `hold`/`release` actions (write permission): validate status in `on_hold|quarantine|damaged`, require reason on hold, idempotent-ish guards (already-held / not-held), ledger row `HOLD`/`RELEASE` (0 qty, running balance, note `Stock {id} di-hold (status): reason oleh {user}` / `di-release`), activity_log `STOCK_HOLD`/`STOCK_RELEASE`.
    - Excluded held stock (add `AND (hold_status = 'available' OR hold_status IS NULL)`) in all 5 picking/allocation FEFO queries: outbound `getAvailableStock`/`getTotalAvailableQty`/`pickItems`, import `fefoAllocation` (api + worker). Plus picklist `createFromOutbound` stock_locations lookups (both the allocations JOIN and the fallback `SELECT sl.id`). StockTake `autoLoadByLocations` left INCLUDING held stock (physical count).
    - Frontend `StockPage.tsx`: Hold Status column (StatusBadge), Hold/Quarantine button (ShieldAlert, write role, when available), Release button (ShieldCheck, when held), hold modal (status select + reason) + release modal (optional reason). `StatusBadge.tsx` added available/on_hold/quarantine/damaged styles.
    - Verified live against dev API (:3000): login → hold (stock 1014 → quarantine) → detail shows hold_status/hold_reason/hold_by/hold_at → `check_stock` total_available dropped exactly by the held qty (596→548) → release → detail back to available. Ledger shows HOLD + RELEASE rows. Edge cases all return correct Indonesian messages (bad status / missing reason / not-held release / missing stock). activity_log rows confirmed in DB.
    - Typecheck (api+worker+web) + api build + web build clean. NOTE: `activitylog::list` returns empty rows — PRE-EXISTING parity quirk (frontend sends `module` as both route + filter; matches PHP `handle_activitylog` reading `query('module')`). Out of Phase-1 scope.
- [x] **S20 = Phase 5 — Audit Trail Hardening** (run SECOND)
  - Grep `apps/api/src/**/*.service.ts` for UPDATE/INSERT on `stock`/`stock_locations`; verify same-tx `stock_ledger` write for each. Report gaps FIRST, propose fixes matching existing pattern, then implement. Include the S19 hold/release path.
  - **DONE (2026-08-14)**: Audited all stock-mutating sites. Verified OK (stock+ledger same tx): outbound `ship`/`delete`, bintransfer `execute`, stocktake `applyAdjustment`, inbound `deleteItem`/`delete`, import fresh-INSERT + outbound ship, stock `adjustStock`/`hold`/`release`, picklist (no stock touch). Documented exceptions hold: inbound `complete` (no ledger), outbound `pickItems` (no ledger — ship writes it), stock `transfer` (activity_log only).
  - **Gaps fixed**:
    1. `inbound.service.ts changeItemStatus` (real-time receive) — stock + `stock_ledger` writes were NOT atomic (separate `this.db.query` calls). Wrapped the whole stock+ledger block in `db.transaction(client)` (stock deletes/inserts, `delLedger`/`runningBal`/`insertLedger` all use the tx client).
    2. `import.service.ts stockCommitTx` — `replace`/`add` modes updated existing `stock` rows with NO ledger row (only the fresh-INSERT path wrote one). Added `addImportLedger(q, row, delta, refNo)` helper writing an `IN`/`OUT` 'Stock Import' ledger row (delta = new−old for replace, +row.quantity for add; skips |delta|<0.001; balance = Available SUM, same as insert path).
    3. `outbound.service.ts deleteItem` — deleting an item from a **Shipped** order restored stock but left the ship-time OUT ledger row (whole-order `delete` deletes its OUT rows but `deleteItem` never touched `stock_ledger`). Added: when `order_status === 'Shipped'`, delete that product's OUT ledger rows for the outbound before removing the item.
  - **Borderline (kept as activity_log-only, user approved via "proceed")**: outbound `changeItemStatus` Unserviceable↔ATP is a zero-sum Available↔QUA_SHELL stock move with no ledger write; the action layer already writes `activity_log` (`UPDATE_OB_ITEM_STATUS`).
  - Root `npm run typecheck` (api+worker+web) + `nest build` (api) clean.
- [x] **S21 = Phase 2 — Barcode Scanning UX**
  - Single-query barcode/SKU lookup action (product + expected location). Ask which formats (EAN/UPC/internal SKU).
  - Scan-input mode (autofocused text input, Enter to submit — no modal/separate page) in PicklistDetail, InboundDetail, BinTransferPage. No camera scanning unless asked.
  - Scan-mismatch → clear error + explicit override with reason logged to activity_log.
  - **DONE (2026-08-14)**:
    - Confirmed scope with user: internal `product_code` only (no EAN/UPC column); picklist = auto-confirm expected item, inbound putaway / bin transfer = validate-only; override + reason logged.
    - Backend: `stock::scan` (permission `any`, dept override `inbound|outbound|inventory`; one SQL query exact-matches `products.product_code`/is_active and returns product + FEFO-first available location as `expected_location` + `default_location` fallback; `{found:false}` for unknown codes). `stock::scan_override` (permission `write`, same dept override; requires code + reason; logs `SCAN_OVERRIDE` to activity_log with context). Both registered in `stock.actions.ts`.
    - Frontend: new reusable `components/ScanInput.tsx` (autofocused single input, Enter submits, clears + refocuses after scan). Integrated into:
      - `PicklistDetail.tsx` — scan auto-confirms the next `Pending` item (full qty + status `Picked`); shows "Berikutnya" hint; mismatch → red error + required reason + Override & Lanjut (calls `stock::scan_override` then auto-confirms).
      - `InboundDetail.tsx` — scan validates against the next Dues In/Goods Received item (putaway target); matching row highlighted green; mismatch → override with reason.
      - `BinTransferPage.tsx` — scan auto-selects the product if none chosen (via `selectProduct`), else validates against the selected product; mismatch → override with reason.
    - `report.service.ts` actionLabel map gained `SCAN_OVERRIDE: 'Override Scan (Mismatch)'`.
    - Verified live on dev API (:3000): `stock::scan` unknown code → `{found:false}`; real code `550058593` → `{found:true, product, expected_location:'CD33E02'}`; `stock::scan_override` POST → `{ok:true}` and SCAN_OVERRIDE row confirmed in `activity_log` (id 35). Typecheck (api+worker+web) + `nest build` (api) clean. NOTE: `activitylog::list` empty-rows is the PRE-EXISTING parity quirk (module param doubles as route + filter), not caused here.
- [ ] **S22 = Phase 3 — Replenishment Suggestions** (bulk → pick-face; depends on S19 `hold_status`)
  - New table `pick_face_targets(location_id, product_id, min_qty, max_qty)`.
  - Query/report: pick-face locations below min_qty + candidate bulk/reserve sources with available (non-hold) stock. Suggestion-only.
  - "Replenishment" page; one-click "Create Transfer" per row → pre-filled bin_transfer.
  - **DONE (2026-08-14)**:
    - Migration `005-replenishment.sql` + updated `001-schema.sql` (fresh-install): `pick_face_targets(id, location_id→location_master, product_id→products, min_qty, max_qty CHECK >=0, created_at, updated_at, UNIQUE(location_id,product_id))` + indexes. Applied to dev DB.
    - Backend `replenishment` module (`replenishment.{service,actions,module}.ts`, wired into app.module): module dept `['inventory']`.
      - `list` — single pass: for each target, `current_qty` = SUM of Available non-hold (hold_status='available'/NULL) stock at the pick-face location; returns only rows where `current_qty < min_qty` with `shortage`, plus `sources` (other non-pick-face Available non-hold locations of the SKU, FEFO earliest-expiry ordered, each with available_qty/earliest_expiry/batch_count).
      - `targets` — all targets with location/product names + current pick-face qty (management list).
      - `save_target` (write) — upsert on (location_id, product_id); validates location/product exist, min/max >= 0, max>=min; activity_log `SAVE_PICK_FACE_TARGET`.
      - `delete_target` (write) — activity_log `DELETE_PICK_FACE_TARGET`.
    - Frontend `ReplenishmentPage.tsx` (route `/replenishment`, nav under inventory+all with PackageSearch icon, pageTitle added): suggestions table (pick-face, product, current, min, shortage, source chips w/ qty+expiry+batch count, disabled "Tidak ada sumber"), **Create Transfer** button per row → modal pre-filled (from = first source, qty = min(shortage, source avail), to = pick-face, reason auto) calling existing `bintransfer::create`; plus Targets management table (add/edit/delete, location `locations::all` select + product `inbound::search_products` picker).
    - Verified live on dev API (:3000): save_target → suggestion appears (product 550058593 at CA01A01, current 0, shortage 50, 13 FEFO-ordered sources); create+execute bintransfer 20 units CD33E02→CA01A01 → suggestion current 20 / shortage 30; delete_target removes it; validation 400s (bad location, max<min) correct. Web `vite build` + api `nest build` + root typecheck all clean. Test target deleted after QA.
- [ ] **S23 = Phase 4 — Wave Planning** (highest risk — touches picklist generation)
  - New tables `waves(id, wave_number, status, carrier, cutoff_time, created_by, created_at)` + `wave_orders(wave_id, outbound_order_id)`.
  - Wave-creation action → single consolidated picklist across orders, grouped/sorted by location. SHOW `picklist.service.ts createFromOutbound` in full before changing it.
  - Single-order flow must keep working (additive). Web UI: wave creation screen with multi-select.
  - **DONE (2026-08-14)**:
    - Migration `006-waves.sql` (applied to dev DB; `001-schema.sql` updated for fresh installs, waves tables inserted before picklists): `waves(id, wave_number UNIQUE, status CHECK Planning/Active/Completed/Cancelled, carrier, cutoff_time TIMESTAMP, created_by, created_at)` + `wave_orders(wave_id→waves CASCADE, outbound_order_id→outbound_orders CASCADE, PK(wave_id,outbound_order_id))` + indexes; `picklists.wave_id BIGINT REFERENCES waves(id)` + `idx_picklists_wave`; `picklists.outbound_order_id` made NULLABLE (wave picklists span many orders; single-order flow keeps setting it — additive).
    - Backend `picklist.service.ts` refactor (no behavior change to single-order flow): extracted the item-generation loop into shared `insertPicklistItems(client, picklistId, outboundIds)` (`WHERE oi.outbound_order_id = ANY($1::bigint[])`, still FEFO `exp_date` ordered); new `createFromOrders(outboundIds, createdBy, waveId, client?)` — creates picklist header with `outbound_order_id=NULL` + `wave_id`, reusable inside WavesService's transaction; `getById`/`getAll` switched INNER→LEFT JOIN outbound_orders and now also expose `wave_number`/`wave_carrier`/`wave_cutoff` so wave picklists render.
    - New backend module `waves` (`waves.{service,actions,module}.ts`, wired into `app.module.ts`; dept `['outbound']`, `create`/`cancel` = write):
      - `list` — waves with order_count, item_count, linked picklist + its status.
      - `detail` — wave + its orders (customer/total_items/total_qty) + picklist.
      - `create` (write) — requires `order_ids`; ONE transaction: validates orders exist (400 otherwise), filters to status `Open` AND no existing picklist (`ineligible` reported back as `skipped`, create still succeeds; 400 if none valid), creates wave header (`Planning`, carrier/cutoff_time optional) + wave_orders rows + consolidated picklist via `createFromOrders(..., client)`; logs `CREATE_WAVE`.
      - `cancel` (write) — 400 for unknown/Completed; deletes the wave's picklist ONLY while still `Draft`, then wave → `Cancelled`; logs `CANCEL_WAVE`.
      - `candidate_orders` — Open orders with no picklist (customer + item/qty totals) for the multi-select.
    - Frontend `WavesPage.tsx` (route `/waves`, Layout nav under outbound+all with `Layers` icon, pageTitle added): wave table (status badge, carrier, cutoff, orders/items, picklist link, created by, detail eye + Cancel buttons when not Completed/Cancelled); **New Wave** modal (searchable multi-select of candidate orders with customer/destination/qty/armada/container, carrier, cutoff `datetime-local`, skipped-order info toast); **detail modal** (orders table with outbound links + picklist link/status). `PicklistList.tsx`/`PicklistDetail.tsx` now link a wave picklist to `/waves` (wave_number) instead of a blank outbound. `StatusBadge` gained `Planning`/`Active` wave styles.
    - Verified live on dev API (:3000): created wave of 2 orders (18+19) → single picklist id 1 with 5 items from BOTH orders location-sorted (CC06A02, CC07A01, CE02A01, CE06D01, CE06D02) — pick-path ordering via getItems; picklist header `outbound_order_id=null` + `wave_number=WAV-202608-0001`; `list` shows order_count 2 / item_count 5; single-order `picklist::create_from_outbound` still works (id 2); skip logic (order 18 already had picklist → wave 2 created with skipped=[18]); validation 400s (missing order_ids, unknown order 99999); `cancel` deleted draft picklists (all picklists gone) and waves → Cancelled; activity_log rows CREATE_WAVE/CANCEL_WAVE present; dept guard correct (inventory user denied, outbound operator allowed). Test data cleaned up: orders 18/19 restored to Shipped, temp scripts deleted. Root typecheck + api `nest build` + web `vite build` all clean.

# WMS Remaining Gaps (from `deepseek-wms-remaining-gaps-prompt.md`)
Run order: **Phase 6 → 8 → 10 → 7**. S24 (Phase 6), S26 (Phase 8), S28 (Phase 10) done; next is Phase 7 (S25).

- [x] **S24 = Phase 6 — ASN (Advance Shipping Notice)**
  - Supplier notifies inbound of an expected shipment in advance so receiving staff can prep. Confirm-vs-expect model: the ASN stays `Pending` until a linked inbound completes, then flips to `Received`. No labor/task management (out of scope per decision).
  - Migration `009-asn.sql` (applied to dev DB; `001-schema.sql` untouched — fresh installs get ASN tables via ordered runner): new `asn(id, asn_number, supplier_name, supplier_reference, expected_arrival_date, status CHECK Pending/Received/Cancelled, notes, created_by→users, created_at, updated_at)` + `asn_items(id, asn_id→asn CASCADE, product_id→products, expected_qty NUMERIC(10,2), uom, batch_number, exp_date)` + indexes; `ALTER TABLE inbound_orders ADD COLUMN IF NOT EXISTS asn_id BIGINT REFERENCES asn(id) ON DELETE SET NULL` + index.
  - Backend module `asn` (`asn.{service,actions,module}.ts`, wired into `app.module.ts`; dept `['inbound']`, `create`/`update`/`cancel` = write): `generateNumber` (`ASN-YYYYMM-NNNN` pad 4); `list` (`{rows,total,page,per_page(50),statuses}` with created_by_name/total_items/expected_qty LEFT JOINs); `detail` (`{asn, inbound_orders}`); `create` (insert header default `Pending` + items; requires ≥1 item); `update`/`cancel` reject unless `Pending` (409); activity_log `CREATE_ASN`/`UPDATE_ASN`/`CANCEL_ASN`.
  - `inbound.service.ts` create(): when `asn_id` given, validates the ASN exists + `Pending` (else 409); adds `asn_id` to INSERT; when `data.items` empty, server-side pre-fills items from `asn_items` (`expected_qty→quantity`, uom, batch_number, exp_date, `in_process_status='Dues In'`). No auto-complete — receiving staff still submits the create form. `complete()` flips a linked `Pending` ASN → `Received` in the same tx.
  - `export::asn` action (`excel.asnReport`) — per-line ASN XLSX export.
  - Frontend: `AsnList.tsx` (route `/asn`, Layout nav "ASN" under inbound+all, `CalendarClock` icon, pageTitle added) — status filter, KPI-free table (asn_no/supplier/ref/expected_arrival/items/qty/status/created_by/View), **New ASN** modal (supplier info + expected items w/ product search, qty, uom, batch, expiry); `AsnDetail.tsx` (route `/asn/:id`) — header info, expected items table, linked inbound orders, Edit (Pending only), Cancel (not Received/Cancelled), **Create Inbound** → `/inbound?asn_id=...`; `StatusBadge` gained `Received` style.
  - `InboundList.tsx` reads `?asn_id` via `useSearchParams`: fetches `asn::detail`, pre-fills create form (carrier_name←supplier_name, expected_date←expected_arrival_date, notes, items with `Dues In`), shows a linked-ASN info banner, includes `asn_id` in create body, navigates to `/inbound/:id` after create.
  - Tests: `apps/api/test/asn.e2e-spec.ts` — create+list+detail ASN; no-items → 400; inbound from ASN pre-fills expected item (qty 12); non-Pending ASN link → 409; full inbound lifecycle → ASN `Received`; cancel only while Pending; update of Received → 409. All 7 pass. Full suite 9 suites / 35 tests pass. Typecheck (api+web) + api `nest build` clean.
  - Started Docker Desktop + dev infra (k2-postgres :5544, k2-redis :6389) which were down; migration applied via `npm run migrate`.

- [x] **S26 = Phase 8 — ABC Analysis / Velocity-Based Ranking**
  - Rank products by OUT volume (`stock_ledger` `transaction_type='OUT'` within a date range) and bucket into A/B/C by cumulative volume (default split 80/15/5 — standard ABC). Classification is advisory only — no auto-relocation (out of scope per decision; labor/task management + multi-warehouse also out of scope).
  - Migration `010-abc-analysis.sql` (applied to dev DB via `npm run migrate`): `ALTER TABLE products ADD COLUMN velocity_class VARCHAR(1) CHECK (velocity_class IN ('A','B','C'))` + `velocity_class_at TIMESTAMP`, both nullable (recomputed periodically, not real-time).
  - Backend module `abc` (`abc.{service,actions,module}.ts`, wired into `app.module.ts`; dept `['all']`, `analyze` = read, `recompute` = admin): `analyze` (read-only preview — returns `{rows, total_qty, counts, split, date_from, date_to}`); `recompute` (same ranking but writes `velocity_class` + `velocity_class_at` in one tx, logs `RECOMPUTE_ABC` activity). `parseSplit` (a + b ≤ 100 else 400, else c = 100−a−b), `parseRange` (default last 90 days), `volumePerProduct` (single grouped query, OUT only), `bucket` (cumulative-share bucketing; top mover always lands in A — a single product driving 100% of volume is A, not the long tail; zero-activity products stay NULL, not forced to C). `productLookup` batch-fetches names/codes.
  - `app.module.ts` wired `AbcModule`; `report.service.ts` gained label `RECOMPUTE_ABC: 'Recompute Analisis ABC'` + moduleIcon `abc: 'fas fa-chart-pie'`; `activity-logger.ts` `'abc'` module added; `stock::list` SELECT now includes `p.velocity_class` (products list/detail already surface it via `p.*`).
  - Frontend: `StatusBadge` gained `A`/`B`/`C` styles (red/amber/gray). `ProductsPage.tsx` — Velocity column in the table + admin-only **ABC Analysis** button opening a modal (`analyze` preview with date range + split A/B inputs, A/B/C/unclassified/total-picked KPI cards, ranked table with cumulative share; admin **Recompute & Simpan** writes classes and refreshes). `StockPage.tsx` — Velocity column in the stock list.
  - Tests: `apps/api/test/abc.e2e-spec.ts` (6 passing) — non-admin recompute → 403 (creates an `operator` user via bcrypt hash for the negative case); analyze ranks 70/20/10 → A/B/C; custom split 50/50; invalid split 60/60 → 400; recompute writes classes + idle stays NULL; products/stock lists expose `velocity_class`. Full suite 10 suites / 41 tests pass. Typecheck (api + web) + api `nest build` + web `vite build` clean.

- [x] **S28 = Phase 10 — Cycle Count Scheduling**
  - Turn ad-hoc stock takes into a recurring cycle-count program, optionally velocity-weighted using S26's `velocity_class`. Explicitly OUT OF SCOPE (per decision): a real cron/background scheduler in this phase — only a manual admin **Run Due Schedules** action; the future hook-in is a BullMQ repeatable job in `apps/worker` calling the same `CycleCountService.runDue()`. Labor/task management + multi-warehouse remain out of scope.
  - Migration `011-cycle-count-schedules.sql` (applied to dev DB via `npm run migrate`): `cycle_count_schedules(id, schedule_name, frequency CHECK weekly/monthly/quarterly, scope_type CHECK full/location/velocity, scope_locations TEXT, velocity_class CHECK A/B/C, next_run_date DATE NOT NULL, is_active BOOL, created_by→users, created_at, updated_at)` + `idx_cycle_count_schedules_due (is_active, next_run_date)`; `ALTER TABLE stock_take ADD COLUMN schedule_id BIGINT REFERENCES cycle_count_schedules(id) ON DELETE SET NULL` (links generated takes back to their schedule).
  - Backend module `cyclecount` (`cyclecount.{service,actions,module}.ts`, wired into `app.module.ts`; dept `['inventory']`, `create`/`update`/`delete` = write, `run_due`/`run_now` = admin): `list` (`{rows}` with created_by_name, `is_due` flag, `total_generated` via LEFT JOIN on stock_take.schedule_id); `create`/`update` validate name, frequency, scope_type, location-scope requires scope_locations, velocity-scope requires velocity_class A/B/C, date format; `delete`; `runDue` (active schedules with `next_run_date <= CURRENT_DATE`, ORDER BY date — reuses **`StockTakeService.create()`** with take_date=today, status Draft, notes `Dibuat otomatis dari jadwal: <name>`, scope_locations for location scope, `schedule_id`, then `autoLoadByLocations(id, locs | null | velocityClass)`, then advances `next_run_date` weekly +7d / monthly +1m / quarterly +3m); `runNow` (single schedule regardless of due date, advances from today). `autoLoadByLocations` gained an optional `velocityClass` filter (`AND s.product_id IN (SELECT id FROM products WHERE velocity_class=$n)`), so velocity-scoped schedules only count stock of that class. `StockTakeService.create()` gained optional `schedule_id` + `scope_type` override params.
  - Wiring: `LOCK_KEYS.cycleCount()` added to `@k-one/shared` (run_due/run_now serialized via `RedisLockService.runLocked`); report labels `CREATE_CYCLECOUNT`/`UPDATE_CYCLECOUNT`/`DELETE_CYCLECOUNT`/`RUN_CYCLECOUNT` + moduleIcon `cyclecount: 'fas fa-calendar-check'`; `activity-logger.ts` `'cyclecount'` module added. NOTE: shared pkg edits require `npm run build -w @k-one/shared` before running API tests (it's consumed from dist).
  - Frontend: `CycleCountPage.tsx` (route `/cycle-count`, Layout nav "Cycle Count" under inventory+all, `CalendarCheck2` icon, pageTitle + ActivityLog module icon added) — table (name/frequency/scope(+velocity badge)/next_run_date/due/status/generated/created_by/actions), admin **Run Due Schedules** button (reports generated take numbers), per-row **Run Now** (admin), New/Edit modal (name, frequency, scope type, location multi-select from `stocktake::get_scope_locations`, velocity class A/B/C, next_run_date, active toggle), delete via ConfirmButton.
  - Tests: `apps/api/test/cyclecount.e2e-spec.ts` (10 passing) — non-admin run_due/run_now → 403 (dedicated `operator` user); create+list (is_due, created_by_name); create validation (name/frequency/location-no-locs/velocity-no-class → 400); run_due generates a stock_take via StockTakeService.create (linked via schedule_id, note contains schedule name, item pre-loaded, qty_system 10), advances next_run_date weekly +7d and produces nothing on re-run once advanced past today; not-yet-due → 0; run_now runs a future schedule + advances; run_now missing → 404; velocity-scope schedule only counts products of that class (A vs C stock); update + delete. Full suite 11 suites / 51 tests pass. Typecheck (api + web) + api `nest build` + web `vite build` clean.

- [x] **S25 = Phase 7 — Cross-Docking**
  - Route specific incoming stock straight to an outbound shipment without putaway, for pre-allocated/pre-sold inbound stock. **Hook confirmed with user BEFORE coding** (per prompt): the **ATP transition** in `inbound.service.ts changeItemStatus` (Dues In → Goods Received → **ATP**), with `cross_dock_outbound_order_id` as a nullable column directly on `inbound_items`.
  - Migration `012-cross-dock.sql` (applied to dev DB via `npm run migrate`): `ALTER TABLE inbound_items ADD COLUMN IF NOT EXISTS cross_dock_outbound_order_id BIGINT REFERENCES outbound_orders(id) ON DELETE SET NULL` + `idx_inbound_items_cross_dock`. Reuses the existing `STAGING` special location (already seeded in 001/007 and excluded from general stock counts) — no new location.
  - Backend `inbound.service.ts`: `addItem`/`updateItem` persist `cross_dock_outbound_order_id`; `changeItemStatus(itemId, 'ATP', createdBy)` — when the item is flagged cross-dock it **bypasses normal putaway/FEFO**: sets item to ATP/Accepted/`location='STAGING'`, writes an IN ledger `[Inbound] Cross-Dock | STAGING → Outbound | <no>`, inserts a `stock_locations` row at STAGING (stock_id NULL, inbound_item_id set so `complete()` preserves it), and calls new `PicklistService.addCrossDockItem(client, obId, productId, qty, uom, batch, createdBy, slId)` — finds-or-creates the outbound's Draft picklist and inserts one `picklist_items` row (Pending, `location='STAGING'`, linked to the matching `outbound_items`). `getItems` LEFT JOINs `outbound_orders` to expose `cross_dock_order_number`/`cross_dock_order_status`. `inbound.actions.ts` detail now also returns `cross_dock_orders` (Open/Picking/Picked outbound list for the selector) and passes `ctx.user.id` as `createdBy`. InboundModule imports PicklistModule (no cycle).
  - Backend `outbound.service.ts`: `getItems` gains a LATERAL join exposing `cross_dock_inbound_item_id`/`cross_dock_batch`/`cross_dock_inbound_number` per line (drives the UI flag + auto-ATP logic); the auto-toggle stock probe includes STAGING for cross-docked lines (else it'd flip them back to Goods Received); `pickItems` — cross-docked lines pick **from STAGING only** (`AND location = 'STAGING'`), normal lines keep excluding STAGING. `PicklistService.addCrossDockItem` added (new public method).
  - Frontend `InboundDetail.tsx`: "Cross-dock ke Outbound Order" selector in the Add Item modal (optional; options from `cross_dock_orders`, hint explains the FEFO/putaway bypass); items table Location cell shows a violet **CROSS-DOCK** badge + `→ <order_number> @ STAGING` when flagged. `OutboundDetail.tsx`: Location cell shows a violet **CROSS-DOCK** badge + `via <inbound_number> @ STAGING` on arriving cross-dock lines.
  - Tests: `apps/api/test/crossdock.e2e-spec.ts` (4 passing) — pre-sold outbound (created without general stock) + inbound item flagged cross-dock: ATP stages at STAGING with NO general `stock` row, STAGING stock_location exists, Draft picklist + Pending STAGING picklist_item attached to the outbound, inbound detail surfaces cross_dock_order_number + cross_dock_orders, outbound detail flags cross_dock_inbound_item_id/number; `complete()` keeps it staged at STAGING (all stock rows at STAGING) and the outbound line auto-resolves to ATP; set/clear via `update_item`; Completed outbound excluded from the selector options. Full suite **12 suites / 55 tests** pass. Typecheck (api + web) + api `nest build` + web `vite build` clean.

# Dashboard Upgrade (from `deepseek-v4-prompt-dashboard-upgrade.md` — 4 sessions, one role per session)

Catch all 3 department dashboards + the combined one up to the current feature set (ASN, waves, hold/quarantine, replenishment, cycle count, cross-dock, scan overrides, ABC). Additive only — extend the Phase-0 dashboards, don't redesign. All new UI text in Bahasa Indonesia matching existing dashboard tone.

- [x] **S29 = Session 1 — Inbound Dashboard**
  - Added "ASN Pending" 5th KPI card + "ASN — Pending Kedatangan" top-10 table: calls `asn::list` with `{status:'Pending'}` (server-side filter; KPI count = server `total`); table sorted client-side by `expected_arrival_date ASC` (nulls last) since `asn::list` sorts by `created_at DESC` with no sort param — shows asn_number (→/asn/:id), supplier_name, expected_arrival_date, total_items (expected lines).
  - Cross-dock flag on the pending-orders table: `inbound.service.ts getAll()` gained a `cross_dock_count` aggregate (`COUNT(*) FILTER (WHERE ii.cross_dock_outbound_order_id IS NOT NULL)::int`) — ONE query change (user-approved, no per-row `inbound::detail` loop); table shows a violet CROSS-DOCK badge when `cross_dock_count > 0`.
  - Per-section resilience: the `asn::list` fetch runs in its OWN then-chain with separate `asnLoading`/`asnError` state — a slow/failed ASN query can't block the main `inbound::stats`/`inbound::list` dashboard (existing Promise.all untouched).
  - Preserved all 4 existing KPI cards + pending-orders table verbatim; KPI grid `md:grid-cols-4` → `md:grid-cols-5` to fit the new card. NOTE: this dashboard never had a putaway-queue section (only stats + pending table) — nothing to preserve there.
  - Manual smoke note: log in as an inbound (or `all`) dept user → `/dashboard/inbound` → verify 5 KPI cards, ASN table shows Pending ASNs sorted by expected arrival, CROSS-DOCK badge appears on inbound rows with a cross-docked item.
  - Typecheck (api + web) + api `nest build` + web `vite build` clean.

- [x] **S30 = Session 2 — Outbound Dashboard**
  - Added "Wave Aktif" 5th KPI card + "Waves — Aktif / Belum Selesai" top-10 table: calls `waves::list` (module dept `['outbound']`, action confirmed from `waves.actions.ts`) with no status filter, filters client-side to non-completed (`!= Completed/Cancelled`), count = filtered rows; table sorted client-side by `cutoff_time ASC` (nulls last) since `waves::list` sorts by `created_at DESC` — shows wave_number (→/waves), status, carrier, cutoff_time, order_count.
  - Cross-dock flag on the open-orders table: `outbound.service.ts getAll()` gained `cross_dock_count` via a SCALAR SUBQUERY (`(SELECT COUNT(*)::int FROM inbound_items cdi WHERE cdi.cross_dock_outbound_order_id = o.id)`) — user-approved; a plain LEFT JOIN would duplicate `outbound_items` rows and corrupt `total_qty`, so the subquery (like the detail path's LATERAL) is the safe single-query form, no per-row loop. Table shows a violet CROSS-DOCK badge when `cross_dock_count > 0`.
  - Per-section resilience: the `waves::list` fetch runs in its OWN then-chain with separate `waveLoading`/`waveError` state — a slow/failed waves query can't block the main `outbound::stats`/`list`/`picklist` dashboard (existing Promise.all untouched).
  - Preserved all 4 existing KPI cards + Open/Picking + Picklist tables verbatim; KPI grid `md:grid-cols-4` → `md:grid-cols-5` to fit the new card.
  - Manual smoke note: log in as an outbound (or `all`) dept user → `/dashboard/outbound` → verify 5 KPI cards, Waves table lists non-completed waves by cutoff, CROSS-DOCK badge appears on open orders with an incoming cross-docked item.
  - Typecheck (api + web) + api `nest build` + web `vite build` clean.

- [x] **S31 = Session 3 — Inventory Dashboard**
  - Added 3 KPI cards (clickable `<Link>`s) + 2 new tables — **NO backend changes needed** (all data already exposed):
    - "Stok Di-Hold / Karantina" card (red/rose, `ShieldAlert`): calls `stock::list` (no server-side hold param exists — checked `stock.actions.ts`), filters rows client-side to `hold_status && hold_status !== 'available'`; count = filtered length; links to `/stock` (StockPage supports only `?q=`/`?year=` — NO hold filter param, flagged).
    - "Replenishment Dibutuhkan" card (teal, `PackageSearch`) + "Replenishment — Dibutuhkan" top-10 table: calls `replenishment::list` → `{suggestions}` (fields confirmed: product_code/product_name/pick_face_location/current_qty/min_qty/shortage); KPI = suggestions length; table sorted client-side by `shortage DESC`, shows produkt/lokasi/stok/min/kekurangan; links to `/replenishment`.
    - "Cycle Count Jatuh Tempo" card (fuchsia, `CalendarCheck2`) + "Cycle Count — Jatuh Tempo" table: calls `cyclecount::list` → `{rows}` with the server-computed `is_due` flag (already `is_active AND next_run_date <= CURRENT_DATE`); KPI = due count; table shows schedule_name/frequency/next_run_date; links to `/cycle-count`.
  - Per-section resilience: all 3 new fetches run in their OWN then-chains with separate loading/error state — a slow `stock::list` (full-table, no pagination) or `replenishment::list` can't block the main stocktake/bintransfer dashboard (existing Promise.all untouched).
  - Preserved all 4 existing KPI cards + Stock Take/Bin Transfer tables verbatim; KPI grid `md:grid-cols-4` → `md:grid-cols-4 xl:grid-cols-7` to fit 7 cards.
  - Manual smoke note: log in as an inventory (or `all`) dept user → `/dashboard/inventory` → verify 7 KPI cards, held/replenishment/cycle-count counts populate, Replenishment table sorted by shortage, Cycle Count table shows due schedules.
  - Typecheck (api + web) + api `nest build` + web `vite build` clean.

- [x] **S32 = Session 4 - Admin/Combined Dashboard**
  - Cross-check first (full Dashboard.tsx shown to user, 553 lines): the combined dashboard already covers inbound dues/receiving + outbound open/picking queues, Stock Summary, 8 ops/inventory KPIs, FEFO queue, Smart Insights, alerts, monthly activity, aisles. ASN pending / wave aktif / cross-dock / hold / replenishment / cycle-count-due are intentionally the DEPARTMENT dashboards' coverage (S29-S31) - NOT duplicated here.
  - Backend A (additive, same class as S29/S30 user-approved changes): `activitylog::list` gained an optional `action` query param (`report.service.ts activityLogList(module, action, limit)` → `AND al.action = $n`; `report.actions.ts` reads `ctx.query.action`). Needed because module='stock' is scan-heavy - without an action filter, SCAN_OVERRIDE rows could fall outside a small limit. Single existing caller updated to pass `null`.
  - Backend B (additive): `abc::status` (admin-gated via `setPermission('abc','status','admin')`) → `SELECT COUNT(*)::int, COUNT(velocity_class)::int, MAX(velocity_class_at) FROM products` returning `{total, classified, unclassified, last_computed_at}`. "Last computed" IS tracked via `products.velocity_class_at` (set to NOW() by `abc::recompute`, NULL = never computed - confirmed S26/migration 010); no existing action exposed it (`products::all` strips the columns, `products::list` is paginated + GROUP BY, `abc::analyze` reads the ledger not the stored class).
  - Admin-only widgets on Dashboard.tsx (gated by `canAdmin` from useAuth):
    - "Recent Scan Overrides" card: calls `activitylog::list` with `{module:'stock', action:'SCAN_OVERRIDE', limit:10}` → table Waktu (`created_at` via fmtDateTime) / Operator (`full_name`) / Kode (`old_value.scanned`) / Alasan (`new_value.reason`); "Lihat semua" → `/activity-log`. Stored shape confirmed from `stock.actions.ts scanOverride`: `old_value={scanned,context}`, `new_value={reason}`.
    - "ABC Analysis - Status ABC" card: calls `abc::status` → classified/total + progress bar, freshness badge "Terkini"/"Usang" (stale when last_computed_at > 30 days old or never computed), "Belum dihitung" when last_computed_at NULL, CTA "Kelola di halaman Produk" → `/products` (recompute lives in ProductsPage, confirmed).
  - Per-section resilience: both new fetches run in their OWN then-chains with separate loading/error state (scanLoading/scanError, abcLoading/abcError) - a slow activitylog or abc query can't block the main `dashboard::stats` (existing Promise.all untouched). When `canAdmin` is false the effects short-circuit to loading=false (widgets never render).
  - Manual smoke note: log in as an ADMIN user → `/dashboard` → verify "Recent Scan Overrides" table (who/what/reason/when) + ABC freshness card populate; verify a non-admin `all` dept user does NOT see either widget and the dashboard still loads.
  - Typecheck (api + web) + api `nest build` + web `vite build` clean.

- [x] **S33 = Connect putaway recommendation (`putaway::recommend`) to the Inbound UI**
  - Source: `k-one-v2-feature-guide(1).docx` §1b flagged `recommendLocations()` as **BUILT BUT NOT CONNECTED** — the recommendation engine exists + tested but the putaway screen never calls it (staff type locations blind and only get corrected on save by the `validatePlacement` safety net).
  - Diagnosis (confirmed in code): `putaway.actions.ts` registers `recommend` (reads `product_id/quantity/uom/uom_per_pallet/prefer_pick/force_level`; returns `{success, message, product, uom, limits, rule, total_pallets, pallets:[{pallet_seq, quantity, is_full, location_code, zone_code, level, reason}]}`); `putaway.service.ts:74 recommendLocations()` is the engine. Frontend: `api('putaway', ...)` was used ONLY by ZoningPage config screens (zones/uom_limits/product_rules/aisle_map/bins) — **`recommend` had ZERO frontend callers**. `InboundDetail.tsx` putaway UIs (Manage Pallet Locations modal + Add Item modal's Pallet Locations section) were plain text fields.
  - Fix (frontend-only in `InboundDetail.tsx`; NO backend change):
    - **Manage Pallet Locations modal**: `openPalletModal` now auto-calls `putaway::recommend` when the item has no saved pallet locations yet AND is not cross-docked, pre-filling suggested `location_code/quantity/is_full` per pallet (staff accept or override). Plus a violet **"Saran Lokasi (Putaway)"** button (`Sparkles` icon) to re-suggest anytime; a small message line shows the engine's result/message.
    - **Add Item modal**: a "Saran Lokasi (Putaway)" button in the Pallet Locations section fires `putaway::recommend` from the selected product + quantity and fills the location rows.
  - Cross-docked items are excluded from auto-suggest (they stage at STAGING, bypassing normal putaway).
  - Manual smoke note: open an inbound in Dues In → Manage Pallet Locations on an item with no saved locations → suggested bins pre-fill (respecting zoning/UOM); edit or accept then Simpan; non-cross-docked items only.
  - Typecheck (web) + `vite build` clean (no api change; api typecheck/build already clean).

- [x] **S34 = Fix Reset Operational Data — `cannot truncate a table referenced in a foreign key constraint`**
  - Symptom (reported by user): admin Reset Data (→ `system::reset_operational_data`) throws `cannot truncate a table referenced in a foreign key constraint`.
  - Root cause: `report.service.ts resetOperationalData()` truncates the Phase-0 operational tables but never added the Phase-4 **waves** tables — `wave_orders` (006-waves.sql) has `outbound_order_id REFERENCES outbound_orders(id)`, so `TRUNCATE outbound_orders` failed because `wave_orders` (not in the truncate list) referenced it. Same class of gap as any other table added after the reset list was written.
  - Fix: added `wave_orders` and `waves` to the truncate list (before `picklists`; single `TRUNCATE ... RESTART IDENTITY` statement handles FK order between listed tables automatically).
  - Audit: re-checked every `REFERENCES` in migrations 001-012 into the truncated tables — only `wave_orders → outbound_orders` was unlisted. Other newer tables (`asn`, `cycle_count_schedules`, `pick_face_targets`, `product_putaway_rules`) reference only master tables (users/products/location_master/zones), and inbound_orders.asn_id / stock_take.schedule_id are the truncated-side referencing non-truncated tables (allowed).
  - Typecheck (api) + api `nest build` clean. Smoke: admin → /reset-data → Reset berhasil; operational tables emptied, master data intact.

- [x] **S35 = Make putaway replicate the current system's palletization pattern (per-SKU UPP from pack size)**
  - Source: user shared the current warehouse system's Excel files (`Warehouse Management System_11/12 Agustus 2026_.xlsx` — Master SKU, Schedule of the day, picking of the day, WMS warehouse map) and asked: *"if there's 500 of 550025358 how did they get split in the putaway ... there's 48 items per pallet there's 36 80 — I want my putaway to recognize this pattern and make them just like the current system."*
  - Pattern extracted from the live data (verified numerically):
    - **UPP is per-SKU by pack size** (Master SKU "Pallet" col + live WMS UPP col): `12*1L`→44, `12*0.8L`→48, `4*4L`/`3*5L`→36, `24*0.12L`→**80** (live WMS; Master SKU said 68/85 — user confirmed 80), `1*209L`/`1*200L`→4, `1*20L`→24, `1*18kg`/`1*15KG`→24, `1*180kg`→4, `1*770kg`→1, `24*1L`→16, `10*0.25L`→100, `1*1000L`→1.
    - **Split = full pallets + remainder**: `fullPallets = floor(qty/UPP)`, `eceran = qty % UPP`. Verified: 188 of 550062464 (UPP 48) → 3 full (144) + 44 eceran ✓; 304 of 550071397 (UPP 36) → 8 full (288) + 16 eceran ✓; 262 of 550070614 (UPP 36) → 7 full (252) + 10 eceran ✓.
    - **Placement**: each full pallet → its own reserve bin (D/E for cartons, B/C for drums); eceran → pick-face Level A. E.g. 500 of 550025358 (12*1L, UPP 44) → 11 reserve bins × 44 + 16 at Level A.
  - Diagnosis: `putaway.service.ts:94-96` **already** splits `floor(qty/upp)` + remainder and routes full→reserve B–E, remainder→pick A — the engine logic already matches the pattern. The real gap was the **UPP value**: it read `products.uom_per_pallet` (default `4`; import default Carton→44), so 12*0.8L→48, 4*4L→36, 24*0.12L→80 were all wrong.
  - Fix (per user choice: "Derive UPP from pack size"):
    - NEW `deriveUppFromPackSize(text)` in `apps/api/src/common/pallet.ts` — parses `qty*size unit` from the product name/description and returns the matched UPP (table above) or `null`. 16-case test harness → all PASS.
    - `putaway.service.ts recommendLocations()`: UPP precedence now `req.uom_per_pallet ?? deriveUppFromPackSize(product_name) ?? product.uom_per_pallet ?? 4`; response also returns the effective `uom_per_pallet` so the UI can display it.
    - Import: `importUomPerPallet(uom, productUpp, name?)` in `import.helpers.ts` now derives from the product name first; all product-create + stock/inbound import call sites in `import.service.ts` pass the name so auto-created products get the correct UPP stored.
    - Web (`InboundDetail.tsx`): `putawayMsg()` helper summarizes "N pallet penuh @ UPP + sisa X ke pick-face" in the Saran Lokasi message (auto-suggest + button + Add Item modal).
  - No engine split-logic change needed (already correct); no migration needed.
  - Typecheck + build clean (api + web; chunk warning pre-existing). Smoke: Inbound item 550062464 qty 188 → suggest shows 3 full @ 48 + sisa 44 to Level A.

- [x] **S36 = Fix 3D rack map not populated after stock/auto import**
  - Symptom (reported by user): the 3D warehouse map (Zoning → `putaway::bins`) stays empty even though the auto-import / stock import ran successfully.
  - Root cause: the 3D map renderer (`putaway.service.ts listAllBins()`, ~line 641) reads `location_master` LEFT JOIN **`stock_locations`** (status Available/Reserved) LEFT JOIN `stock` LEFT JOIN `products`. But `import.service.ts stockCommitTx()` (used by BOTH `stock_commit` AND `auto`) wrote **`stock` rows only** (with a `location` column) and **never created `stock_locations` rows** — so every bin shows `quantity 0` / unoccupied. The inbound flow populates `stock_locations` via `saveItemLocations()` / `savePalletLocations()`, which is why only inbound-driven data ever appeared.
  - Fix (in `stockCommitTx`, all four paths):
    - NEW `syncLocations()` helper: for each committed stock row it ensures a matching `stock_locations` row at `stock.location` (uppercased) with the row's qty, uom, batch, status (Available unless stock_status=Reserved), `is_full_pallet` (qty ≥ effective UPP) and `pallet_function` (`PICK_FACE` for Level A bins, else `RESERVE`). If bin rows already exist for that stock (e.g. from inbound), it scales their quantities to the new total instead of duplicating, preserving pallet structure.
    - Wired into: insert (new stock), `replace`, `add`, and `skip` (backfills existing imported stock that predates this fix) paths.
  - No migration needed (table already exists). Re-running the same auto-import now backfills `stock_locations` for previously imported rows.
  - Typecheck (api) + api `nest build` clean. Smoke: run auto-import on a WMS dump → Zoning 3D map shows occupied bins with qty/product.

- [x] **S37 = Inbound Items table shows ONE row per pallet/location (same product repeated), like the spreadsheet**
  - Symptom (reported by user with two example inbounds): auto-imported inbounds (IN-202608-0056 with 41 items, IN-202608-0065) show one `inbound_items` row per bin — e.g. Gadus S3 repeated ~39×, one per CD30A01/CD30A02/… each qty 1. But a manually-created inbound of 500 qty putaway-split into pallets stays hidden as ONE product row; the per-pallet locations only live in `stock_locations` (visible only via the Manage Pallet Locations modal).
  - Root cause: auto-import (`autoCreateInbound`, import.service.ts) creates one `inbound_items` row PER WMS row (per location), which is why auto-imported orders render per-bin. Manual `save_pallet_locations` (inbound.service.ts:640) keeps 1 item row and only inserts `stock_locations` rows — nothing expands them into visible rows.
  - Fix (frontend-only in `InboundDetail.tsx`): NEW `displayRows` — each item with saved pallet locations expands to one table row per pallet (`{item, pallet}`), same product repeated; each row shows the pallet's own Qty/Actual, Location, and "Pallet N" (pallet_seq). Items without saved locations render as a single row as before. Items count header now shows `displayRows.length`. All edit/status actions still operate on the parent item.
  - No backend change, no migration. Typecheck (web) + `vite build` clean (chunk warning pre-existing). Smoke: manual inbound item qty 500 → Manage Pallet Locations → save putaway split → Items table now shows one row per bin like the spreadsheet.

- [x] **S38 = Auto-putaway on inbound add + remainder always PICK_FAST (Level A)**
  - Symptom (reported by user): inbound items land in `UNALLOCATED` instead of bulk. User confirmed the product/putaway-rule config is fine and requested: *"make the whatever max SKU the bulk and the rest thats not reaching max SKU automatically becomes PICK_FAST (level A)"*.
  - Root cause: `addItem` (inbound.service.ts) only created `stock_locations` when the caller passed `pallet_locations` OR `item.location`. Adding an item with just qty+product left `inbound_items.location` NULL and NO bin rows; on completion the stock build fell into `loc = item.location ?? 'UNALLOCATED'`. The putaway recommend engine already routed full pallets→reserve B–E and remainder→Level A, but nothing auto-ran it at add time.
  - Fix:
    - `putaway.service.ts recommendLocations()`: remainder target level is now **always `PICK_LEVEL` (A)** when there's a remainder (was `pickAllowed ? A : reserveLevels[0]`), so any qty not reaching max SKU/UPP automatically goes PICK_FAST; full pallets still go to bulk/reserve.
    - `inbound.service.ts addItem()`: when no `pallet_locations` AND no `item.location` are provided, auto-calls `putaway.recommendLocations` and saves the suggested bins via `saveItemLocations` — so manual inbounds land in bulk (full pallets) / PICK_FAST (remainder) automatically.
  - No migration. Typecheck (api) + api `nest build` clean. Smoke: add inbound item qty 500, no location → full pallets auto-split to reserve B–E, remainder 16 → Level A bin; no more UNALLOCATED.

- [x] **S39 = Moved Rack Map (2D) + Rack View (3D) from Zoning tab → Locations tab**
  - Requested by user: the rack maps are a picture of the locations themselves, not zoning rules; they belong under Locations.
  - Fix (frontend only): NEW `RackViews.tsx` component (extracted from `ZoningPage.tsx`) that renders the 2D aisle×level map (`putaway::aisle_map`) and the 3D rack view (`putaway::bins`) with all their state/loading logic. `LocationsPage.tsx` now has 3 tabs — Locations List / Rack Map (2D) / Rack View (3D) — and renders `<RackViews tab={...} />`. `ZoningPage.tsx` drops the two map tabs (and their now-unused state/`Row`/imports) and stays as rules-only: UOM Level Limits / Produk & Putaway Rules / Zones / Zone Aisles.
  - No backend change, no migration. Typecheck (web) + `vite build` clean (chunk warning pre-existing). Smoke: Locations → Rack Map (2D) & Rack View (3D) render as before; Zoning tab is rules-only.

- [x] **S40 = Putaway Location Blocking (admin blocks aisles/bins from putaway suggestion + manual save)**
  - Scope (user request): admin can mark an entire aisle (e.g. `CF*`) or a specific bin as blocked from putaway. Blocked locations are (a) excluded from `putaway::recommend` suggestions (full-pallet AND pick-face remainder) and (b) rejected by `putaway::validate`, i.e. manual saves via Manage Pallet Locations / `inbound::save_pallet_locations` fail with `Lokasi <CODE> diblokir untuk putaway: <reason>`. Terminology is "Blocked Locations", NOT "hold" (hold = stock quarantine, S19). Deactivation is a soft delete (`is_active=false`) so history is kept.
  - Backend: migration `013-putaway-location-blocks.sql` (new table `putaway_location_blocks`: `scope_type` CHECK aisle/location, `aisle_prefix`, `location_code`, `reason NOT NULL`, `is_active BOOLEAN`, `blocked_by`, timestamps + CHECK exactly-one-target; partial unique indexes so a target can be re-blocked after deactivation). `putaway.service.ts`: `activeBlocks()`/`blockHit()` (single source of truth for the prefix/exact-match logic); enforcement in `findAvailable()` (SQL `NOT LIKE '<aisle>%'` / `<> '<code>'` exclusions) + `validatePlacement()` (reject before UOM checks); CRUD `listBlocks`/`createBlock`/`deactivateBlock`; `listAllBins()` + `listAisleMap()` expose `blocked`/`block_reason`. `putaway.actions.ts`: `list_blocks` (read) + `create_block`/`deactivate_block` (**admin**), all three restricted to department `all` via `setActionDepartments`. `report.service.ts`: actionLabel for the two new actions.
  - Frontend: `ZoningPage.tsx` 5th tab **Blocked Locations** (`Ban` icon) — active-blocks table (Tipe/Target/Alasan/Diblokir oleh/Pada + Deactivate), Block Aisle + Block Specific Location admin forms (native `<datalist>` autocomplete from `locations::all`), collapsed inactive history. `Rack3D.tsx`: `Bin3D` gains `blocked`/`block_reason`; blocked bins render warning-red (#dc2626) + stay opaque. `RackViews.tsx`: 2D cells get a `⛔` marker when a bin in the aisle×level is blocked, 2D detail rows + 3D detail panel show `DIBLOKIR` badge (reason on hover/inline).
  - Verified: `apps/api/test/putaway-blocks.e2e-spec.ts` (10 tests: create/list w/ user join, location-block create, duplicate active → 409, input validation 400s incl. unknown location, non-admin 403, validate rejects bin+prefix while free bin valid, recommend excludes blocked reserve bins, recommend falls back to STAGING when pick-face fully blocked, deactivate re-enables + soft-delete history, re-block after deactivate). API e2e 65/65, `tsc` (api+web), `nest build`, `vite build`, vitest 7/7 all green.

- [x] **S41 = Fix missing BULK badge on 3D rack view (pallet_function never written by inbound/putaway save paths)**
  - Symptom (user-reported): the 3D rack view had no BULK / PICK FACE indicator for occupied bins.
  - Root cause: `saveItemLocations()` (auto-suggest) and `savePalletLocations()` (manual save) never set `stock_locations.pallet_function` — the column defaults to `'RESERVE'` (migration 007), so every bin written by auto-putaway / Manage Pallet Locations was RESERVE, including Level A pick-face bins. Only `import.service.ts syncLocations()` (S36) and `bintransfer.convertDestPalletFunction` (S10) set it. The frontend (`Rack3D`/`RackViews`) received `pallet_function` from `putaway::bins`/`putaway::aisle_map` but rendered nothing from it (only `is_pick_face` from `location_master`).
  - Fix:
    - NEW shared helpers in `apps/api/src/common/pallet.ts`: `palletFunctionFor(locationCode)` (Level A → `'PICK_FACE'`, else `'RESERVE'`) + `isFullPallet(qty, upp)`. Single source of truth so every `stock_locations` write agrees (mirrors 007 semantics).
    - `inbound.service.ts` `saveItemLocations()` + `savePalletLocations()` now INSERT `pallet_function` from `palletFunctionFor(location)`.
    - `import.service.ts syncLocations()` deduped onto `palletFunctionFor`/`isFullPallet` (same result, no behavior change).
    - Migration `014-pallet-function-backfill.sql`: re-runs the Level-A → PICK_FACE backfill (same predicate as 007) for rows written after 007; idempotent.
    - Frontend `RackViews.tsx`: `AisleBin` gains `pallet_function`; 2D detail table adds a **Fungsi** column and the 3D detail panel a `FungsiBadge` (RESERVE → blue **BULK**, PICK_FACE → green **PICK FACE**, MIXED → purple). 3D scene colors stay zone-based.
  - Verified: NEW `apps/api/test/pallet-function.e2e-spec.ts` (auto-suggest `saveItemLocations` derives PICK_FACE at Level A / RESERVE at B-E + `putaway::bins` exposes it; manual `save_pallet_locations` derives per chosen bin incl. uppercase input). API e2e 67/67 (14 suites), `tsc` (api+web), `nest build`, `vite build`, vitest 7/7 all green.

- [x] **S42 = Putaway Task Queue (batched putaway worklist replacing one-off auto-suggest)**
  - Scope (user request, from the WMS improvement list): stop writing `stock_locations` immediately at Goods Received. Instead enqueue every pallet's suggested bin into a tracked, per-inbound **putaway task**; operators claim the task, confirm/override each pallet's bin (re-validated against putaway rules), and completing the task materialises `stock_locations` — the 3D rack map only lights up once putaway is physically done.
  - User decisions (all "recommended"): (1) **Auto-create on Goods Received** — defer the auto-suggest `stock_locations` write until the task is confirmed; (2) **Block inbound complete** while an open task has Pending pallets (lists the task numbers in the 409); (3) **Per-inbound granularity** — one task batches all items' pallets of an inbound.
  - Backend: migration `015-putaway-tasks.sql` (`putaway_tasks`: `task_number` `PKA-YYYYMMDD-NNNN`, `inbound_order_id`, status CHECK `Pending/In Progress/Completed/Cancelled`, `priority`, `assigned_to`, `notes`, `created_by`, `started_at`/`completed_at`/`cancelled_at`; `putaway_task_items`: `task_id` FK CASCADE, `inbound_item_id`, `product_id`, `batch_number`, `uom`, `pallet_seq`, `quantity`, `suggested_location`, `actual_location`, `pallet_function`, `reason`, status CHECK `Pending/Done/Cancelled`, `completed_by`/`completed_at`).
  - `putaway.service.ts` task API: `enqueueForPutaway` (reuses the open task for the inbound or creates one via `generateNumber`), `listTasks(status,search)` (pallet/done counts, joined order + users), `taskDetail`, `assignTask` (claim → In Progress + started_at), `updateTaskPallet` (re-validates via `validatePlacement`, special locs skipped), `completeTaskPallet` (defaults `actual_location` to the suggested bin when just confirming), `completeTask` (tx: writes every Done row via the shared `insertStockLocation` helper — same `palletFunctionFor` derivation everywhere — requires ALL rows Done, sets Completed), `cancelTask`, `openTaskNumbers` (the completion gate), `reconcileItemRows` (manual Manage Pallet Locations path marks the item's open rows Done so it never strands the block).
  - NEW shared write helper `common/stock-locations.ts` `insertStockLocation(dbc, w)` — single write path for `stock_locations` (derives `pallet_function`), refactored `inbound.saveItemLocations` onto it.
  - Inbound hooks: `changeItemStatus` Goods Received block now builds the pallet suggestions (fixed-bin `palletDist` or engine `recommendLocations`, with `reason`) and calls `enqueueForPutaway` instead of `saveItemLocations`; `savePalletLocations` gained a `userId` param + reconciles the item's task rows; `complete()` throws a 409 listing open task numbers while any Pending pallets remain.
  - Actions `putaway.actions.ts`: `task_list`/`task_detail` (reads, default perms) + `task_assign`/`task_update_pallet`/`task_complete_pallet`/`task_complete`/`task_cancel` (**write**), all under the putaway module dept `['inbound','inventory']`. `report.service.ts`: actionLabels `TASK_ASSIGN/TASK_UPDATE_PALLET/TASK_COMPLETE_PALLET/TASK_COMPLETE/TASK_CANCEL`.
  - Frontend: NEW `apps/web/src/pages/PutawayTasksPage.tsx` at `/putaway-tasks` (nav "Putaway Tasks" in Main, depts `['inbound','inventory','all']`). Queue list (status filter + search by task/inbound, done/pallet counts) → task detail: pallet table with product/batch/qty/suggested bin, editable **actual bin** via `<datalist>` of all rack bins + save, inline `FungsiBadge`, reason label, per-pallet Done, **Ambil Task** / **Selesaikan Task** / **Batalkan** (ConfirmButton), progress `done/total`.
  - Verified: NEW `apps/api/test/putaway-tasks.e2e-spec.ts` (11 tests: auto-create PKA task + rows on GR with no premature stock write, one task batches all items, list status/search filters, detail w/ product info, assign → In Progress, update_pallet rejects blocked bin + accepts valid, complete writes stock_locations w/ correct pallet_function + marks Completed, inbound complete 409-listed while Pending then succeeds after task completion, manual save_pallet_locations reconciles so complete unblocked + no double write, cancel unblocks, operator-write OK + outbound-dept 403). S41's `pallet-function.e2e-spec.ts` auto-suggest test updated for the deferred write (task rows carry the derivation; stock written on task completion). API e2e 78/78 (15 suites), `tsc` (api+web), `nest build`, `vite build`, vitest 7/7 all green. `npm run migrate` applied 015 to the dev DB.
- [x] **S43 = New "ops" (Operations) department — handheld menu set for putaway + outbound operators (no dashboards)**
  - Migration `017-departments-ops.sql` (**renumbered from 016 — 016 taken by S49's lpn-team**, applied to dev DB via `npm run migrate`): drop + re-add `users_department_check` widening it to `('inbound','outbound','inventory','ops','all')` (strict widening; `001-schema.sql` untouched).
  - Server-side grants: `putaway.actions.ts` module depts → `['inbound','inventory','ops']`; `outbound.actions.ts` → `['outbound','ops']`; `picklist.actions.ts` → `['outbound','ops']`. `auth/guards.ts` `DEPARTMENTS` += `'ops'` (so `isDepartment` accepts it for user create/update).
  - Server-side denials (defense-in-depth): `report.actions.ts` `dashboard` module depts → `['inbound','outbound','inventory','all']` (excludes ops) — this also GRANTS the dashboards to inbound/outbound/inventory users who were previously restricted to dept 'all' only; ops users get no dashboard module. `inbound` (`['inbound']`), `stock` (`['inventory']`), `waves` (`['outbound']`) already exclude ops.
  - `master.actions.ts` users `departments` list += `{ key:'ops', label:'Operations' }` (drives UsersPage select).
  - Web: `lib/api.ts` `Department` union += `'ops'`, `DEPARTMENTS` += ops, `departmentHome('ops')` → `/putaway-tasks`; `AuthContext` department whitelist += `'ops'` (else ops users would be forced to 'all').
  - `Layout.tsx` nav: Putaway Tasks depts → `['inbound','inventory','ops','all']`; Outbound + Picklist items → `['outbound','ops','all']`; **Putaway Saya** also += `'ops'` (it IS the handheld putaway menu for ops); **Dashboard item gained depts `['inbound','outbound','inventory','all']`** (excludes ops — previously it had no depts and would have shown ops an entry they can't use). Decision: Excel Import / Master Data / Reports sections stay `depts:['all']` — ops already hidden from those admin/all-only sections.
  - `task_list` gains `mine=1` → filters to the current user's tasks. Decision: filter is `assigned_to = me OR forklift_operator_id = me OR checklist_partner_id = me` (the plan said just `assigned_to`, but S49 added the 2-person team columns, so "mine" means claimed OR on the team). `PutawayTasksPage` adds a **Tugas Saya** toggle (default ON for ops, off otherwise).
  - Tests: NEW `apps/api/test/ops-department.e2e-spec.ts` (4 passing) — ops operator 200 on `putaway::task_list`/`outbound::list`/`picklist::list`; 403 on `inbound::list`/`stock::list`/`dashboard::stats`; `users::list` departments incl. ops + `users::create` with department ops succeeds (unique username/email per run since the `users` table isn't reset between suites); `task_list mine=1` returns only the ops user's own task (via assign_task team) and excludes an unassigned task. Full suite 17 suites / 93 tests pass (the one flake seen — abc 403 under shared `users` state — clears on re-run). Typecheck (api+web) + api `nest build` + web `vite build` + vitest 7/7 clean. `npm run migrate` applied 017 to the dev DB.
- [x] **S44 = Barcode-driven putaway scanning (handheld)** — *mostly absorbed by S49's mobile dual-scan (`PutawayScanPage.tsx`, LPN+bin validation, `task_complete_pallet` via `ScanInput`); the one remaining gap was the rack-walk bin-label batch print.*
  - Unique pallet code at Goods Received: **done via S49** (`putaway_task_items.lpn_code`, unique, generated in-tx at GR, printed as a CODE128 barcode on the `LpnLabel`).
  - Bin barcodes = existing `location_code`: NEW `locations::print_labels` action (`master.actions.ts`, read, department-restricted to **inventory** like `locations::all`) returns active locations — optionally filtered to one `zone` — in rack-walk order (`aisle → rack → row → position`, capped at 500). NEW `web/src/components/LocationLabels.tsx`: one small bin label per location, barcode drawn client-side with JsBarcode (same technique as `LpnLabel`), printed via a new `.bin-print-area` `@media print` rule in `index.css` (4-column grid, `break-inside: avoid`). `LocationsPage` gains a **Cetak Label Lokasi** button in the filter bar (next to Terapkan) that respects the current zone filter and opens a `size="lg"` modal with the label grid + print.
  - Verification: NEW test in `ops-department.e2e-spec.ts` — `locations::print_labels` works for an inventory user (rows + zone filter), 403 for an ops user. Also fixed a pre-existing **abc test fragility**: `resetDb` never clears `products` (master data), so products accumulate across runs — once > 500 existed, "exposes velocity_class on the products and stock lists" failed because the product fell off page 1 (`per_page: 500`). Test now looks the product up by `product_code` via the search filter, robust at any volume. Full API e2e **95/95 (17 suites)**; vitest 7/7; api+web typecheck + `nest build` + `vite build` clean.

- [x] **S49 = LPN-based putaway + 2-person task assignment + mobile dual-scan confirmation (extends S42)** (prompt: `deepseek-lpn-putaway-task-assignment-prompt.md`)
  - ⚠️ **Scope decision reversal (recorded per prompt)**: S24–S28 planning explicitly excluded labor/task management — original exclusion: *"Labor/task management — no picker assignment, no per-user productivity tracking... leave it unassigned/open-pool."* That decision is **REVERSED by the user for this specific workflow only** — putaway tasks now get a 2-person assignment (forklift operator + checklist partner). This does NOT reopen per-user productivity tracking, nor picking/outbound task assignment, which remain out of scope unless the user says otherwise.
  - User-confirmed decisions: (1) assignee pickers = **active users only, any department**; (2) the LPN column lives on **`putaway_task_items`**.
  - Migration `016-lpn-team.sql` (applied to dev DB via `npm run migrate`): `ALTER TABLE putaway_task_items ADD COLUMN lpn_code TEXT` + UNIQUE index (one LPN per physical pallet, generated once at Goods Received so the label prints immediately); `putaway_tasks` gains `forklift_operator_id` / `checklist_partner_id` FK → `users(id)` + indexes. NOTE for the next phase: migration number **016 is now taken** — S43's planned `016-departments-ops.sql` must move to a higher number (017+).
  - Backend:
    - `common/number-gen.ts`: `generateNumber` now takes a `DbLike` interface (DbService and `PoolClient` both satisfy it) so it can run on the transaction client.
    - `putaway.service.ts`: `enqueueForPutaway` generates the per-pallet LPN **inside the tx** (`dbc as DbLike`, format `LPN-YYYYMMDD-#####` pad 5) so the existence-check sees its own inserts; `listTasks`/`taskDetail` expose `forklift_operator_id/name/full_name` + `checklist_partner_id/name/full_name`, rows include `lpn_code`. New: `listAssignableUsers` (active users, any dept), `assignTeam` (validates both users active + not the same person, Pending→In Progress + `started_at`), `unassignTeam`, `getLpnLabelData` (label JSON with product/batch/uom/qty/pallet_seq/suggested bin/expiry), `myTasks` (the checklist partner's own Pending/In Progress tasks with pallet rows + forklift name — single action for the mobile screen).
    - NEW `putaway/label-printer.service.ts`: `LabelPrinterService` interface + `HtmlLabelPrinterService` (validates the LPN exists) — the swappable seam so real Zebra/network label hardware later is a config + one-file change.
    - `putaway.actions.ts`: new actions `assignable_users`/`get_lpn_label_data` (read) + `assign_task`/`unassign_task`/`print_lpn_label`/`scan_override` (write) + `my_tasks` (read). Dept-lifted to ANY department (empty `setActionDepartments` override) for `my_tasks`, `scan_override`, **and the completion actions `task_complete_pallet`/`task_complete`** — the checklist partner can be from any active department (user decision) and must confirm pallets / finish the task on mobile. `task_assign`/`task_update_pallet`/`task_cancel` stay inbound/inventory-scoped. Activity labels `TASK_TEAM_ASSIGN`/`TASK_TEAM_UNASSIGN`/`PRINT_LPN_LABEL` (+ existing `SCAN_OVERRIDE`) in `report.service.ts`.
    - `dispatcher/registry.ts` `getDepartments`: an explicit empty override array now means **open to all** (backward compatible).
  - Frontend:
    - `jsbarcode ^3.11.6` + `@types/jsbarcode` added; NEW `components/LpnLabel.tsx` — CODE128 LPN label (product/batch/qty/bin/expiry), `.lpn-print-area` scoped via `@media print` CSS in `index.css` (plus `.no-print`).
    - `PutawayTasksPage.tsx` rewrite: LPN column + **Cetak LPN** per row, unassigned/assigned filter, team display, **Tugaskan / Ubah / Lepas Tim** modal with two active-user pickers.
    - NEW `PutawayScanPage.tsx` (`/putaway-scan`, nav "Putaway Saya" Smartphone icon, depts inbound/outbound/inventory/all): mobile task list via `my_tasks`, per-pallet LPN→bin `ScanInput` dual-scan flow (mismatch → reason → `scan_override`), confirm via `task_complete_pallet`, finish via `task_complete`.
  - Verification: NEW `apps/api/test/putaway-lpn-team.e2e-spec.ts` (11 tests). **Two real bugs found during verification**: `myTasks` was missing the `products` JOIN ("missing FROM-clause entry for table p"); the label expiry used JS `String(date)` (pg returns DATE as a JS Date → locale-dependent output) — fixed via SQL `to_char(ii.exp_date,'YYYY-MM-DD')`. The permission test uses department gating instead of a viewer role — the DB `users_role_check` only allows `admin/warehouse/supervisor/operator/staff` (all write roles), so a non-write role can't exist. Full API e2e **89/89 (16 suites)**; vitest 7/7; api+web typecheck + `nest build` + `vite build` clean (pre-existing chunk warning). Docker Desktop + dev infra started (were down); `npm run migrate` applied 016 to the dev DB.
  - **Follow-up (2026-08-17, found in usage)**: the LPN was generated at Goods Received but only surfaced on the putaway screens — the Inbound detail screen (where receiving staff work, and where the prompt's Step 1 says the label gets printed) showed nothing, and the 2-person team could only be assigned from the Putaway Tasks detail view. Fix: `putaway.service.ts` gains `getInboundOpenTask(inboundOrderId)` (open Pending/In Progress task + pallet rows: LPN, product, batch, qty, suggested bin, status; bigint ids normalized to numbers); `inbound::detail` now returns `putaway_task`. `InboundDetail.tsx` renders a **Putaway Task card** (task number, status, done/pallet count, forklift + checklist names) with per-pallet **Cetak label LPN** (reuses `print_lpn_label` → `LpnLabel`) and **Tugaskan Tim / Ubah Tim / Lepas Tim** (reuses `assignable_users` + `assign_task`/`unassign_task`). `putaway-lpn-team.e2e-spec.ts` +1 test (12 total); full API e2e **94/94 (17 suites)**; vitest 7/7; api+web typecheck + `nest build` + `vite build` clean.
  - **Follow-up 2 (2026-08-17, strict role split, user-confirmed)**: the checklist partner must scan the LPN + rack barcode on mobile to confirm pallets; only the inbound operator changes the task status to Completed. Enforced as: removed the desktop per-pallet "Selesai" button (`PutawayTasksPage` — pallets can now ONLY be confirmed via the mobile dual-scan); removed "Selesaikan Task" from the mobile screen (`PutawayScanPage` — no finish button there anymore); backend `task_complete` no longer lifted to any department — `setActionDepartments('putaway','task_complete',[])` removed so it inherits the putaway module depts `['inbound','inventory','ops']` (the outbound-dept checklist partner gets 403 on `task_complete`). `task_complete_pallet` stays open to any department (partner confirms pallets). Tests updated: `putaway-lpn-team` strict-split test (partner completes pallets, 403 on complete, inbound operator finishes); `putaway-tasks` permissions test +`task_complete` 403 for outbound. Full API e2e **94/94 (17 suites)**; vitest 7/7; api+web typecheck + `nest build` + `vite build` clean.