# K-one-v2 — Session Context & Port Spec

> Purpose: resume the full rewrite (PHP MySQL → NestJS + PostgreSQL + Redis + BullMQ + Nginx) if context is lost.
> Source-of-truth files: `D:\K-one\k-one\{api\handlers\*.php, classes\*.php, database.sql, frontend\API.md, frontend\src\pages\*.tsx}`

## 0. Decisions (user-confirmed)
1. Backend **NestJS + TypeScript**
2. Frontend: **keep & adapt existing React SPA** (it's already React+Vite+TS), swap API layer
3. **1:1 parity** — same endpoints, same JSON shapes, same schema (Postgres port)
4. New monorepo at **`D:\K-one-v2`**; PHP original stays as reference

## 1. Environment
- Windows; PowerShell 5.1 shell (use `;` chaining, NOT `&&`; escape `$` as backtick in PS double-quoted strings)
- Node v26.5.1, npm 11.17.0, Docker 29.7.2, git 2.55
- Original runs in Docker: `kone-app` (php:8.2-apache), `kone-db` (mysql:8.0). DB: `sanchaya`? No — actual db = `sanchaya@mysql`, user `root`/`root` (from container env: DB_NAME=sanchaya DB_USER=root DB_PASS=root DB_HOST=db). Port 80.
- API base: `http://localhost/k-one/api/index.php?module=X&action=Y`, Bearer token auth, JSON `{success:true,...}|{success:false,message}`.
- Frontend currently at `http://localhost:5173` (Vite dev). Login `admin`/`admin123` (PHP bcrypt; API.md says seeded password `password`, but dev login used admin123).

## 2. Original system scope (line counts)
- classes: ActivityLogger 200, Auth 101, BinTransfer 408, Customer 83, ExcelExport 940, Inbound 1057, LocationManager 419, Outbound 1270, PalletHelper 169, Picklist 345, Product 230, Report 156, Stock 297, StockTake 618 (total ~6,293)
- handlers (25): auth, inbound(436), outbound(316), dashboard(177), master(337), stock(119), stocktake(185), bintransfer(79), picklist(88), ledger(99), import(89), import_auto(242), import_helpers(181), import_inbound(183), import_outbound(283), import_stock(228), import_templates(180), reports(92), report(3→routes to reports), products/customers/locations/users (3 each → master), activitylog(3), system(58)
- React pages: 22 (Dashboard, InboundList, InboundDetail, OutboundList, OutboundDetail, Stock, StockTakeList, StockTakeDetail, PicklistList, PicklistDetail, BinTransfer, Products, Customers, Locations, Users, Ledger, Reports, Import, AutoImport, ActivityLog, Login, ResetData)
- Components: Layout, Card, Modal, StatusBadge, Pagination, Spinner, Toast, Field, PageHeader, ConfirmButton, WebBtn

## 3. API contract (from frontend/API.md) — must match exactly
Auth: POST `auth/login` `{username,password}`→`{token,user}`; logout; GET auth/me. Roles: admin/warehouse/supervisor/operator/staff; `canWrite`=admin,operator,warehouse,supervisor,staff; `canAdmin`=admin.

Modules & key endpoints (all under `?module=&action=`):
- **dashboard/stats**, dashboard/aisle_detail&aisle=
- **inbound**: list(status,od_no,page,per_page), detail(id), stats, search_products(q), create, update, delete, add_item, update_item, update_item_qty, update_item_dates, update_item_pallet_no, update_item_status, delete_item, save_pallet_locations, save_item_location, advance_status, complete, repair_ledger
- **outbound**: list, detail, stats, search_products, check_stock(product_id,quantity,location)→{available,fefo}, create, update, add_item, pick_items, ship, complete, delete, delete_item, update_item_status
- **stock**: list(status,expiring,q,location), summary, expiring(days), by_location, locations, transfer, adjust
- **ledger**: list(product_id,start_date,end_date,limit), repair_all
- **picklist**: list, detail, stats, create_from_outbound(outbound_id), confirm, complete, delete, update_item, export_data(id)
- **stocktake**: list, detail, stats, get_locations, get_scope_locations, get_stock, create, add_item, auto_load, update, delete_item, delete, start_counting, save_counters, advance_to_c2, finish_counting, save_review, apply_adjustment
- **bintransfer**: list, detail, locations_with_stock(product_id), stock_at_location(product_id,location), create, execute, cancel
- **products**: list(search,page,per_page), all, detail, create, update, delete
- **customers**: list(search,page), all, detail, create, update, delete
- **locations**: list(zone,available_only), all, check(code), available(count,zone), zone_summary, suggest(quantity,uom,uom_per_pallet,zone), create, update, delete
- **users** (admin): list, create, update, delete
- **report**: daily(date,date_to), products, inbound, outbound, stock, ledger
- **activitylog**: list(module,limit), modules
- **import**: tpl_inbound, tpl_outbound, tpl_stock (download), inbound, outbound, stock_preview, stock_commit, auto
- **system**: reset_operational_data (probably in system.php handler — verify)

Status values (exact strings):
- Inbound order: Draft, Dues In, Receiving, Good Received, Goods Received, Unserviceable, Picked, ATP, Completed, Cancelled
- Inbound item in_process_status: Dues In, Goods Received, ATP, Unserviceable (note real code uses only `Goods Received` — the enum also lists `Good Received`)
- Outbound order: Open, Picking, Picked, Shipped, Delivered, Completed, Cancelled
- Picklist: Draft, Confirmed, Picking, Picked, Completed, Cancelled
- StockTake: Draft, Counting, Review, Adjusted (detail), list uses In Progress
- Stock stock_status: Available, Reserved, Expired, Dues In, Rejected, Pending

## 4. CRITICAL parity gotchas
1. **Two pallet-math conventions**: `Inbound/Outbound/Stock` use `ceil(qty/uomPerPallet)` (whole pallets) or level-A `round(...,2/4)`; `PalletHelper::calculatePallet` uses `floor`+integer `%` returning decomposed object. Preserve both; use per-context.
2. **NULL-safe batch match** `batch_number <=> ?` (= IS NOT DISTINCT FROM in Postgres) everywhere EXCEPT `Outbound::deleteItem` AND `deleteItem` stock lookups which use `= ?`.
3. **Two balance sources**: `Stock::adjust` balance = `SUM(stock.quantity)` where status='Available'; ledger write balances = `SUM(quantity_in)-SUM(quantity_out)` excluding `QUA_SHELL` location and `TRANSFER_IN/TRANSFER_OUT` types; `Outbound::addToLedger` balance = plain SUM(in)-SUM(out) over whole ledger for that product (no exclusions); `BinTransfer::execute` balance anchor = last ledger row balance for product.
4. **`Inbound::complete()` writes stock, NEVER ledger**; ledger written only by the real-time receive/status-change flow (`inbound_change_item_status` in handler) and `regenerateLedger`. `Outbound::pickItems` writes NO ledger (ledger at ship time). `Stock::transfer` writes NO ledger. `Picklist::complete/updateItem` touch NO stock.
5. **Level = 5th char of location code** (`loc[4]`, uppercase): level 'A' → fractional pallets `round(qty/upp,2)`; other levels → `int ceil`. Level-aware pallet calc used in inbound stock-build and picklist.
6. **`Outbound::update` nulls order-level ship_to_name/location/street** (never updates them); second UPDATE sets `shipped_by=currentUser, status=?` whenever shipped_date or status provided.
7. **`Outbound::addItemWithFEFO`**: FEFO allocation is in-memory only at add time — does NOT write outbound_item_locations/stock_locations/ledger, does NOT decrement stock. Decrement happens ONLY in `pickItems`.
8. **`expected_date` is stored but NEVER validated/required** in Outbound (handler pick_items needs it — verify handler; picklist create requires expected_date per API contract? spec says Outbound.php doesn't require — the pick_items handler may).
9. **Number generation** race-safe: prefix `IN/OUT-YYYYMM-` / `PKL-YYYYMM-` / `BTR-YYYYMM-` / `ST-YYYYMMDD-` (+ optional item counter). Find latest by LIKE prefix, seq=suffix+1, up to 20 tries, fallback `prefix+His+rand(10,99)`.
10. **Message parity**: exact Indonesian messages everywhere (quoted in specs). HTTP codes: validation 400, 401 unauthorized, 403 forbidden, 404 unknown.
11. **GROUP_CONCAT(DISTINCT x ORDER BY x SEPARATOR ', ')** → Postgres `string_agg(DISTINCT x, ', ' ORDER BY x)`; `SET SESSION group_concat_max_len=65536` → ignore.
12. **uthash**: `location_code` comparisons use COLLATE utf8mb4_general_ci → Postgres just use normal = on text.
13. **timezone**: Asia/Jakarta; `date('Y-m-d')` = server date; `CURDATE()`/`NOW()` → `CURRENT_DATE`/`NOW()`; DATEDIFF → `(expiry_date - CURRENT_DATE)` in days.
14. **Booleans/tinyint** → `smallint`/`boolean`; `ON DUPLICATE KEY UPDATE` → `INSERT ... ON CONFLICT ... DO UPDATE`.

## 5. MySQL→Postgres schema port (database.sql, 515 lines)
Tables (20): users, customers, products, location_master, warehouse_locations, inbound_orders, inbound_items, outbound_orders, outbound_destinations, outbound_items, stock, stock_locations, outbound_item_locations, location_allocations, stock_ledger, picklists, picklist_items, stock_take, stock_take_items, bin_transfers, activity_log, settings.
Key columns & enums:
- users(pk, username uniq, password, full_name, email uniq, role ENUM admin/warehouse/supervisor/operator/staff default staff, is_active tinyint→bool)
- customers(pk, customer_code uniq, customer_name, contact_person, phone, email, address, city, is_active)
- products(pk, product_code uniq, product_name, category, description, drums_per_pallet int default 4, uom_type ENUM Drum/Carton/Pail/EA/Bags default Drum, uom_per_pallet int default 4, liters_per_unit decimal, max_sku_qty default 44, max_trans_qty default 80, default_location, max_per_transaction default 80, reorder_level default 0, is_active)
- location_master(pk, location_code uniq, aisle, rack, row_name, position, zone, is_active) + idx aisle/zone
- warehouse_locations(location uniq, aisle, bay, row_code, slot, is_active)
- inbound_orders(pk, order_number uniq IN-YYYYMM-NNNN, order_date date, carrier_name, container_no, po_number, shipment_no, do_number, armada_no, production_date, expected_date, status ENUM(...), notes, remarks, received_by→users, received_by_name, received_date, created_by→users NOT NULL) idx created_by, received_by
- inbound_items(pk, inbound_order_id FK cascade, od_number, so_number, product_id FK, batch_no, location, quantity dec(10,2), uom, actual_qty, pallet, pallet_no, manufacture_date, exp_date, stock_status ENUM Accepted/Rejected/Pending, in_process_status ENUM Dues In/Goods Received/ATP/Unserviceable/Picked default Dues In, notes, batch_number) idx od/so
- outbound_orders(pk, order_number uniq OUT-YYYYMM-NNNN, order_date, customer_id FK, so_number, do_number, shipment_number, ship_to_name, ship_to_location, ship_to_street, destination, kota, armada_no, container_no, jenis_armada, expected_date, status ENUM Open/Picking/Picked/Shipped/Delivered/Completed/Cancelled default Open, shipped_date, notes, shipped_by FK, created_by FK)
- outbound_destinations(pk, outbound_id FK cascade, seq tinyint unsigned, ship_to_name, ship_to_location, ship_to_street, kota, destination, notes)
- outbound_items(pk, outbound_order_id FK cascade, product_id FK, quantity, uom, actual_qty, pallet, batch_no, exp_date, location, in_process_status ENUM Goods Received/ATP/Unserviceable default Goods Received, gr_plan_no, transaction_no, notes, od_number, so_number, destination_id FK, batch_number, stock_location_id FK) idx od/so/dest
- stock(pk, product_id FK, batch_number, location, quantity, uom, uom_type ENUM Drum/Carton/Pail default Drum, uom_per_pallet int default 4, pallet, manufacture_date, production_date, expiry_date, stock_status ENUM Available/Reserved/Expired/Dues In default Available) idx product, expiry, status, location, fefo(product,status,expiry)
- stock_locations(pk, stock_id FK, location_code, pallet_seq smallint default 1, quantity, original_quantity, uom, is_full_pallet tinyint, batch_number, inbound_item_id, status ENUM Available/Reserved/Picked/Empty default Available) idx stock/location/status/batch
- outbound_item_locations(pk, outbound_item_id FK, stock_location_id FK, quantity) uniq(item,loc)
- location_allocations(pk, reference_type ENUM Inbound/Outbound/Stock, reference_id, item_id, pallet_number, location, quantity, uom, is_full)
- stock_ledger(pk, transaction_date date, product_id FK, transaction_type ENUM IN/OUT/ADJUSTMENT/TRANSFER, reference_type, reference_id, reference_number, batch_number, quantity_in, quantity_out, uom, pallet, balance, location, notes) idx product/date/ref
- picklists(pk, outbound_order_id FK cascade, picklist_number uniq PKL-YYYYMM-NNNN, created_date, status ENUM Draft/Confirmed/Picking/Picked/Completed/Cancelled, notes, created_by→users, confirmed_at, picked_at, completed_at)
- picklist_items(pk, picklist_id FK cascade, product_id FK, batch_no NOT NULL, location, quantity, uom, pallet, picked_quantity, status ENUM Pending/Picked/Verified default Pending, picker_id, notes, picked_at, batch_number, stock_location_id, pallet_seq)
- stock_take(pk, take_number uniq ST-YYYYMMDD-NNNN, take_date, status ENUM Draft/In Progress/Completed/Cancelled + uses Counting/Review/Adjusted in code (verify), notes, created_by→users, counting_round)
- stock_take_items(pk, stock_take_id FK cascade, product_id FK, batch_number, uom, location, qty_system, counter_1, counter_2, counter_3, qty_physical, difference, status ENUM Plus/Minus/Clear, notes, counter_by)
- bin_transfers(pk, transfer_number uniq BTR-YYYYMM-NNNN, transfer_date, product_id FK, stock_id, batch_number, from_location, to_location, quantity dec(12,4), uom, reason, status ENUM Pending/Completed/Cancelled, created_by FK, completed_by FK, completed_at)
- activity_log(pk, user_id, username, full_name, action NOT NULL, module NOT NULL, reference_type, reference_id, reference_no, description, old_value, new_value, ip_address VARCHAR(45)) idx user/module/ref/created
- settings(pk, setting_key uniq, setting_value, updated_at)
- auth_tokens (created by api/index.php on demand): user_id, token uniq varchar(64), expires_at, created_at. Token: bin2hex(random_bytes(32)) = 64 hex chars; TTL 12h.
Seed: users admin (password hash of `password`), settings (warehouse_name='Shell CKB Warehouse', drums_per_pallet='4'), location_master QUA_SHELL/UNALLOCATED/STAGING.

NOTE: source of truth for live schema differences = the running mysql container (kone-db) — queries can be run via `docker exec kone-app php -r` with PDO, or check `database.sql`. The migration SQL in dev had extra columns (scope_locations, scope_type, counting_round on stock_take; stock_take uses statuses Counting/Review/Adjusted). Confirm against live DB before finalizing migration.

## 6. AUTH
- login: verify bcrypt (probably password_hash) → issue token (auth_tokens). In dev container: username `admin`, password `admin123` (from earlier login test). API.md seed says `password`. Provide both in seed or verify via PHP.
- Guards: any auth required for all except auth module & tpl downloads. write roles = admin,operator,warehouse,supervisor,staff. admin only: users, stock adjust (per contract), delete product, delete location, stocktake apply_adjustment, ledger repair_all, system reset.

## 7. IMPORT specs (critical — this is what originally caused the 500)
- All uses `import_raise_memory_limit()` to 512M (already implemented in PHP source: import_helpers.php line 159, called in import_read_sheet/import_auto_run/import_run_outbound). In Node, no memory limit needed — but use **streaming** for big files and run in BullMQ worker.
- import_helpers: import_parse_date (Excel serial >40000 → Y-m-d; YYYY-MM-DD; D/M/Y vs M/D/Y heuristic; null for empty/'0'), import_normalize_uom (car/ctn→Carton, drm→Drum, pal→Pail, bag→Bags, ea/pcs→EA), import_uom_per_pallet (drum→4, carton→product(44), pail→24, bags→1, ea→4), import_header_index, import_resolve_col (priority patterns, exact>substring), import_detect_header (max keyword score; score<1→index 0), import_getter (first non-empty non-'0'), import_is_meta_row.
- **import_stock_commit** returns `{imported, skipped, auto_created, auto_locations}`; mode add/replace/skip; duplicate key = stock(product,batch,location,status); ledger ref `'Stock Import'` / `'IST-YYYYMMDD-NNNN'`, notes `row.notes ?: 'Direct stock import'`.
- **import_auto pipeline** (single tx, rollback on any throw):
  1. Pass1 master: infer uom from upp (upp<=1→Bags, <=8→Drum, <=28→Pail, else Carton); create/update products; log "Master data: {c} produk baru, {u} diperbarui"
  2. Pass2 WMS+putaway→stock (wms forced Available), commit mode add; WMS→inbound grouped by manufacture_date ?: 'NO_GR_DATE'; order status Dues In, notes 'Auto import (WMS) — GR: {gr}'
  3. Pass3 schedule→outbound via import_outbound_process_sheet skipUnknown=true
  4. skip sheets = not master/wms/putaway/schedule
  - Return `{success:true, message:"Auto import selesai. Semua sheet diproses dalam satu aksi.", stats:{products_created, products_updated, stock_imported, stock_skipped, stock_auto_created, inbound_orders, inbound_items, outbound_orders, outbound_items, outbound_skipped, skipped_sheets:[...]}, log:[...]}`
  - Log templates quoted in spec doc (see "Order #N — Shipment: X", "SKIP (stok 0)...", "OD:... | FEFO OK (b@l)", etc.)
- **import_outbound_process_sheet** (shared, also standalone import/outbound): header keyword scan; group by shipment (empty→'NO_SHIPMENT_{n}'); per shipment create outbound via Outbound::create with shipment_number, ship_to, expected_date=planDate, notes 'Imported from Excel | Shipment: ...'; customer resolve/create code 'OUT-'+8-char uc alnum of name, ON DUPLICATE KEY keep name; items: product lookup (code→numeric-only→(int)material→prodname contains desc[:15]); FEFO allocate deliveryQty; skip when stok 0 or skipUnknown; bind stock_locations, INSERT IGNORE outbound_item_locations; log per-item FEFO line; delete order if 0 items; return `{success:true, message:"Import selesai: {o} orders, {i} items dari {n} shipments", stats:{orders_created, items_imported, rows_skipped, errors:0}, log:[...]}`
- **import_templates**: headers exactly (with `* ` on required): inbound (Shipment No, OD No, SO No, Item Code*, Uom*, ACTUAL QTY*, QTY ORDER, Pallet, Batch No, Manufacture date*, Exp Date*, Location, Remarks); outbound (Plan Date, Shipment Number*, Order No (OD), Purchasing Document, Ship-to Party Code, Material*, Description, Delivery quantity*, Sales Unit*, Name of Ship-To Party, Location of Ship-To Party, Street / Address, Goods Issue Date, SO Number, TRANSPORT); stock (`product_code*`, `batch_number`, `location`, `quantity*`, `uom`, `manufacture_date`, `expiry_date`, `stock_status`, `notes`).

## 8. STATUS / PROGRESS
- [x] Root-caused + fixed the original PHP memory bug (500 on big Excel): memory_limit 128M→512M via `import_raise_memory_limit()` + docker php.ini. VERIFIED web import of 1.15MB works (returns proper JSON error on duplicate, no crash).
- [x] Cleaned test data from PHP DB (all STR/SHP/ZTEST test rows, customers, locations removed). DB back to seed state (users 4, no products/orders/stock).
- [x] **S1 Scaffold** — monorepo at `D:\K-one-v2`; `apps/api` (NestJS 11, `pg` + raw SQL), `infra/docker-compose.dev.yml` (postgres+redis). `apps/worker` + `packages/shared` are stubs; `apps/web` deferred to S14/S15 (frontend = existing React app).
- [x] **S2 Schema** — `apps/api/src/database/migrations/001-schema.sql` (457 lines, all 20 tables ported) + `migrate.ts` seed.
- [x] **S3 Core infra** — config/env, DB pool, JWT auth + guards (write/admin), activity-logger, api-exception + global filter, date-util (Asia/Jakarta), number-gen (race-safe, 20-try), pallet helper, dispatcher gateway + action registry with per-action permissions. Decision: **`pg` + raw SQL** (not TypeORM).
- [x] **S4 Master data** — products / customers / locations (incl. zone_summary, available, suggest) / users (admin) in `master/master.actions.ts`; shared `master-data.service.ts`.
- [x] **S5 Stock + Ledger** — stock list/summary/expiring/by_location/detail/locations/transfer/adjust + ledger list/repair_all in `stock/stock.actions.ts`. Permissions: stock.adjust=admin, ledger.repair_all=admin.
- [x] **S6 Inbound** — `inbound/inbound.service.ts` (~1045 lines), `inbound.module.ts` wired into `app.module.ts`. Typecheck clean (verified this session via `tsc --noEmit`, 0 errors). The previously-tracked 3 errors are gone.
- [x] **S7 Outbound** — `outbound/{outbound.service.ts,outbound.actions.ts,outbound.module.ts}` wired into `app.module.ts`. `pickItems`/`ship` implemented (working tree contains a larger uncommitted rewrite of the whole outbound module relative to the last commit `59b9d18`). Typecheck + `nest build` clean.
- [x] **S8 Picklist** — `picklist/{picklist.service.ts,picklist.actions.ts,picklist.module.ts}` wired into `app.module.ts`. See todo.md S8 note for exact port details.
- [x] **S9 StockTake** — `stocktake/{stocktake.service.ts,stocktake.actions.ts,stocktake.module.ts}` wired into `app.module.ts`. Schema: `stock_take` gained `scope_locations TEXT`, `scope_type VARCHAR(20) DEFAULT 'full'`, `counting_round VARCHAR(10)`; status CHECK `('Draft','Counting','Review','Adjusted','Completed','Cancelled')`. See todo.md S9 for full port detail.
- [x] **S10 BinTransfer** — `bintransfer/{bintransfer.service.ts,bintransfer.actions.ts,bintransfer.module.ts}` wired into `app.module.ts`. Numbers `BTR-YYYYMM-`; errors → 409; ledger TRANSFER_OUT then TRANSFER_IN with same pre-transfer currentBalance. See todo.md S10 for full port detail.
- [x] **S11 Dashboard + reports + activitylog + system(reset)** — `report/{report.service.ts,report.actions.ts,report.module.ts}` wired into `app.module.ts`. See todo.md S11 for full port detail.
- [x] **S12 Import** — `import/{import.helpers.ts, import.service.ts, sheet.reader.ts, import-templates.ts, import.actions.ts, import.module.ts}` wired into `app.module.ts`. SheetJS (`xlsx`) reads uploaded .xlsx/.xls/.csv; ExcelJS builds templates (streamed via gateway `_binary` marker + StreamableFile); global multer memoryStorage middleware in `main.ts`. stock_preview/stock_commit/inbound/outbound/auto ported with exact Indonesian messages, FEFO inline on tx client, PHP `inTransaction()` parity for nested outbound-in-auto. Frontend `ImportPage`/`AutoImportPage` contracts verified. See todo.md S12 for full port detail.
- [x] **S13 BullMQ worker** — `apps/worker/src/{main.ts, import.engine.ts, sheet.reader.ts}` (real BullMQ Worker on `kone:import`, standalone auto-import engine in one DB tx, Redis task status); `packages/shared/src/{import-helpers.ts, date-util.ts, redis-lock.ts, queue.ts}`; API `import::auto_async` + `import::task_status` (ImportQueueProvider in ImportModule); fail-open `RedisLockService` (CommonModule) wrapping outbound pick_items/ship (`fefo:<id>`) and stocktake finish_counting/apply_adjustment (`stocktake:<id>`). Fixed worker TS18003 — root `npm run typecheck` clean. See todo.md S13 for details.
- [x] **S14 infra** — `infra/docker-compose.prod.yml` (postgres+redis+api+worker+web), `infra/{api,worker,web}.Dockerfile`, `infra/nginx.conf` (SPA + `/k-one/api` proxy), `infra/.env.example`, `infra/README.md`. Added `apps/{api,worker}/tsconfig.build.json` (rootDir src, shared→built dist) to fix nested build output; root `build` runs shared first. `docker compose config` validates. See todo.md S14 for details.
- [x] **S16 Verify** — live parity smoke tests against running Docker infra. Docker daemon up → `k2-postgres`(5544)/`k2-redis`(6389) started, API :3000 + worker on `kone-import`. Verified: `auth::login`; all 13 core JSON modules; `export::` XLSX (customers/products/stock/ledger/report/stocktake + inbound/outbound with data); `print::` HTML (inbound_receipt/putaway/outbound_do/surat_jalan/picklist by id & outbound_id idempotent/report types); import (stock_preview/commit, inbound, outbound, auto sync + async); full stocktake workflow (auto_load 1099 items, start/save_counters/advance/finish/review/stats). User's real workbook `Warehouse Management System_11 Agustus 2026_.xlsx` imports cleanly (366 products, 1161 stock, 70 inbound/1100 items, 13 outbound/64 items, 16 sheets skipped). See todo.md S16 for the full bugfix list.
- Working dir note: the v2 source of truth is `D:\K-one-v2\todo.md` + `session.md` (per `prompt.md`). The `todo.md`/`session.md` sitting in the PHP reference repo (`D:\K-one\k-one\todo.md`/`session.md`) are STALE leftovers from the earlier abandoned Express+mysql2 port and should be ignored (do not let them confuse the v2 tracker).

## 9. Frontend adapt notes
- `frontend/src/lib/api.ts` (helper `api(module, action, {params, body})`) must point to new base URL and keep Bearer token semantics; response shape identical so most pages should work unchanged.
- Pages already built against contract in frontend/API.md. Any drift → JSON field names must be preserved exactly.
- vite dev proxy currently → host api URL; update to new backend origin.
- Tailwind v3, React 18, react-router 6, lucide-react. Build: `npm run build` (tsc -b && vite build) → dist served by nginx/web container.

## 10. Decision log — S8 Picklist (2026-08-13)
- **PHP source found**: unlike the earlier session (which reported the PHP tree was absent), `D:\K-one\k-one\` IS present in this environment. S8 was ported directly from `classes/Picklist.php` + `api/handlers/picklist.php` + `frontend/src/pages/Picklist*.tsx` + `frontend/API.md` — no guessing needed. Docker daemon is NOT running, so live-DB cross-checks were not possible; schema facts below are from `database.sql` + the PHP code usage.
- **Schema gap fixed**: `picklist_items.outbound_item_id` is absent from both the MySQL `database.sql` and the S2 Postgres port, yet the PHP `createFromOutbound` INSERTs it and `getItems` JOINs `oi.id = pki.outbound_item_id` — the live DB must have it (an ad-hoc column not in `database.sql`). Added `outbound_item_id BIGINT REFERENCES outbound_items(id)` to `001-schema.sql`. For already-migrated DBs run `ALTER TABLE picklist_items ADD COLUMN IF NOT EXISTS outbound_item_id BIGINT REFERENCES outbound_items(id);` (or delete the `schema_migrations` row for `001-schema.sql` and re-run).
- **updateItem omits picker_id**: API.md lists `{item_id, picked_quantity?, status?, picker_id?}` but `Picklist::updateItem` only sets picked_quantity/status/location/batch_number/batch_no/notes/picked_at. Ported exactly (no picker_id).
- **createFromOutbound is pure mirror**: early-returns existing picklist id (PHP), header created as `Draft` (not Confirmed), one `picklist_items` row per allocated stock_locations row when allocations exist, else one row per decomposed pallet (level-aware `calcPalletByLocation`). Writes NO stock/ledger and does NOT touch outbound status (matches PHP — the spec-2 §2.3 note about `status='Picking'` was not in the actual PHP class).
- **Next**: S9 StockTake (`docs/spec-3-stocktake-report-import.md` §1 + `classes/StockTake.php` + `api/handlers/stocktake.php`).

## 11. Decision log — S9–S12 (2026-08-13)
- **S9 StockTake**: status CHECK replaced (`('Draft','Counting','Review','Adjusted','Completed','Cancelled')`); `scope_type`/`scope_locations`/`counting_round` columns added; counters shape `saveCounters {itemId:{c1,c2,c3}}`, `saveC1/saveC2 {itemId:value}`; `finishCounting` re-snapshots via `IS NOT DISTINCT FROM` batch; avg_accuracy ported from `classes/StockTake.php`.
- **S10 BinTransfer**: errors → 409 conflict; ledger writes TRANSFER_OUT then TRANSFER_IN, both with the SAME pre-transfer `currentBalance` (PHP quirk). Widened `stock_ledger.transaction_type` CHECK to include `TRANSFER_IN`,`TRANSFER_OUT` (MySQL enum in database.sql is stale — the PHP writes these live).
- **S11 reports**: `report` daily wrapped `{report:{...}}`; activitylog module filter + action_label/module_icon maps; `system` reset_operational_data truncates 16 tables in one tx (admin only).
- **S12 Import**: SheetJS `xlsx` (not ExcelJS) reads uploaded workbooks since ExcelJS cannot parse `.xls`; ExcelJS still writes the 3 templates (matching `import_templates.php` header styling). Multipart via global `multer({memoryStorage}).any()` in `main.ts` (512MB cap). Gateway streams binary template responses through a `_binary` marker → `StreamableFile`; PUBLIC_ACTIONS covers `import::tpl_inbound/outbound/stock`. `outboundProcessSheet` honors an already-open transaction (PHP `$db->inTransaction()` parity) so `runAuto` keeps a single tx; `stockCommitTx`, `fefoAllocation`, `generateInboundNumber` (`IN-YYYYMM-`), `generateOutboundNumber` (`OB-YYYYMMDD-`) all operate on the tx-bound `QFn`. Auto-create product defaults `liters_per_unit=209.00, max_sku_qty=44, max_trans_qty=80, reorder_level=0, is_active=1` (parity). Frontend contracts (`ImportPage`, `AutoImportPage`) verified field-by-field; typecheck + `nest build` clean.
- **Next**: S13 BullMQ worker (add `apps/worker/src` to fix TS18003 root typecheck) → S14 infra → S15 frontend API adapt → S16 verify.

## 12. Decision log — S13 Worker (2026-08-13)
- **Async import is additive, not replacing sync**: the existing frontend (`ImportPage`/`AutoImportPage`) uploads synchronously via `import::auto` and reads the result inline. Spec-3 §3.6 wants async with `task_id` + polling. Implemented both: sync parity path unchanged; new `import::auto_async` enqueues to `kone:import` and returns `{task_id}`, `import::task_status` polls Redis status. Frontend can adopt `auto_async` later (S15 optional) without breaking anything.
- **Standalone engine in worker**: the worker's `runAutoImport` is a self-contained port of `ImportService.runAuto` (sheet classify, stock parse/validate/commit, master → products, WMS→inbound by GR date, schedule→outbound with FEFO). It runs on a plain pg `PoolClient` with no Nest DI; pure helpers come from `@k-one/shared` (moved out of the API local copy).
- **Redis locks fail-open**: `RedisLockService.runLocked` proceeds without the lock when Redis is unreachable — the sync parity path must never hard-fail because Redis is down; DB transactions remain the source of truth. When Redis is up, FEFO/stock-take critical sections are serialized across API + worker processes.
- **Shared package**: `packages/shared` gained `import-helpers`, `date-util`, `redis-lock`, `queue` (lock/queue/task key constants). Its `index.ts` re-exports everything; `tsconfig` path mapping (`@k-one/shared` → `packages/shared/src`) works for both api and worker.
- **Next**: S14 infra (docker-compose postgres/redis/api/worker/web + nginx proxy).

## 13. Decision log — S14 Infra (2026-08-13)
- **Build layout fix**: importing `@k-one/shared` via the tsconfig `paths` source mapping made `tsc` pick the monorepo root as `rootDir`, emitting api/worker output under `dist/apps/...` and leaving `dist/main.js` stale. Fix: dedicated `tsconfig.build.json` per app sets `rootDir: "src"` and resolves `@k-one/shared` from its *built* `dist` (`../../packages/shared/dist`). `@k-one/shared` must build first (root `build` = `build:shared && api && worker`; `nest build` auto-uses tsconfig.build.json; worker `build` switched to `tsc -p tsconfig.build.json`).
- **Web image context**: the React SPA lives outside the v2 repo (`D:\K-one\k-one\frontend`). The web Dockerfile's build context is the repo root and it copies `./frontend`; users must `xcopy /E /I D:\K-one\k-one\frontend D:\K-one-v2\frontend` first (documented in infra/README.md).
- **nginx**: serves SPA `try_files → index.html`, proxies both `/k-one/api/` (default `VITE_API_BASE`) and `/api/` to `http://api:3000/index.php`, forwards Authorization header, 512m body limit, 600s proxy timeouts (import uploads / long reports).
- **Compose**: `name: k-one-v2`; web published on host `:8081`; postgres/redis internal-only with healthchecks; api/worker depend on healthy deps. `docker compose config` validated.
- **Next**: S15 frontend API adapt (point `@/lib/api` base URL + token at new backend; verify pages; fix field drift).

## 14. Decision log — S15 Frontend + binary print/export (2026-08-13)
- **Scope**: user chose "Implement binary endpoints in v2" for the print/export buttons instead of keeping PHP coupling. Every remaining `webBase()` page link now hits the v2 API.
- **Binary contract**: gateway detects handler return markers — `{_binary:true,buffer,filename,contentType}` → StreamableFile + `Content-Disposition: attachment` (XLSX); `{_html:true,html}` → inline `text/html` so the print document opens in a new tab for `window.print()` (no PDF lib — same as PHP, which uses browser print). Auth for new-tab links works via `apiHref()` appending `?token=` (gateway `resolveUser` falls back to `req.query.token`).
- **New `export` module** (`apps/api/src/export/{excel-export.service,print.service,export.actions,export.module}.ts`): `ExcelExportService` mirrors `ExcelExport.php` + the inline `ledger.php`/`stock.php`/`stocktake.php` exports (ExcelJS); `PrintService` mirrors `print_inbound.php`, `putaway_sheet.php`, `print_outbound.php`, `surat_jalan.php`, `print_picklist.php`, `print_report.php` (self-contained HTML + A4 print CSS). `ExportModule` imports Inbound/Outbound/Picklist/Report/StockTake modules (all export their services) and is wired into `app.module.ts`.
- **Action list**: `export::inbound|outbound|customers|products|ledger|stock|stocktake|report`; `print::inbound_receipt|putaway|outbound_do|surat_jalan|picklist|report`. `print::picklist` accepts `id` or `outbound_id` (idempotent `createFromOutbound` then render — mirrors `picklist_pdf.php` redirect); permission `write` for `print::picklist`, `any` otherwise (frontend gates buttons via canWrite).
- **Ledger export parity**: `GROUP BY sl.id`, dates 2000-01-01..2099-12-31, correlated subqueries for od_number/so_number/shipment_no (Inbound/Outbound/BinTransfer variants), TX labels Penerimaan/Pengiriman/Penyesuaian/Pindah Masuk/Pindah Keluar. **Stock export parity**: per-location detail with `string_agg(DISTINCT ... ORDER BY ...)` for od_numbers/shipment_nos + `calcExpiry` port (EXPIRED / X days left / X mo X d left). **StockTake export**: detail rows + Clear/Plus/Minus + accuracy summary.
- **Frontend repoint**: `apiHref(module,action,params)` (already existed) replaces `webBase()` hrefs in InboundList, OutboundList, CustomersPage, ProductsPage, LedgerPage, StockPage, StockTakeDetail, ReportsPage (daily→print+excel with date/date_to), InboundDetail, OutboundDetail, PicklistDetail. `webBase()` kept but unused.
- **Next**: S16 Verify — with Docker daemon up, run parity smoke tests (login → each export/print endpoint vs PHP response shapes; real workbook import) against the running Postgres + API.
## 15. Decision log - S16 Verify (2026-08-13)
- **BullMQ queue name**: v6 rejects ':' in queue names -> QUEUE.IMPORT changed 'kone:import' -> 'kone-import' (worker + ImportQueueProvider).
- **PG vs MySQL tx semantics**: PHP's try/catch-continue around statements silently poisons a PostgreSQL transaction ('current transaction is aborted' masks the real first error). Removed the swallow-and-fallback patterns (outbound_items INSERT, outbound_item_locations bind, customer upsert, outbound_destinations INSERT) in both import.service.ts and worker import.engine.ts; ON CONFLICT covers the duplicate cases and real errors propagate.
- **Ad-hoc schema columns**: outbound_items.customer_id (PHP Outbound.php inserts it), picklist_items.outbound_item_id, outbound_destinations.ship_to_street (not street_address), stock_take scope_locations/scope_type/counting_round + statuses Counting/Review/Adjusted. The migration file had most already; the live dev DB predated later edits -> recreated dev DB from migration (DROP SCHEMA public CASCADE + npm run migrate) for guaranteed parity.
- **Outbound import order_number**: v2 used shipment_number as order_number (dup-key on re-import); PHP always generates OUT-YYYYMM-NNNN -> always generate, prefix aligned to PHP.
- **Next**: S17 - manual parity walk of remaining detail paths (outbound pick/ship with stock_locations drain, picklist confirm/picking, inbound receipt/putaway status flows, bin transfer execute/cancel) against PHP on real data; then docker-compose.prod up.

## 16. Decision log - S17 Verify detail paths + prod compose (2026-08-13)
- **MySQL alias-in-SET is invalid in PG**: PHP habit \UPDATE stock s SET s.manufacture_date=…\ breaks under PG (\column "s" of relation … does not exist\). Hit in inbound.service.ts \syncBatchToOutbound\ (broke inbound::complete) and \updateItemDates\; also a \SET pki.\ / \SET oi.\ pair. Fixed all three to bare column names (alias allowed in FROM only). Added a grep guard: pattern \SET \w+\.\ should stay empty.
- **inbound::complete idempotency**: import's stock snapshot already materializes stock for (product,batch,location), so complete()'s unconditional INSERT duplicated rows. Now deletes unlinked Available/Dues-In/Pending stock for (product,batch,location) before INSERT in both branches. Verified idempotent across a re-complete. Ledger side already idempotent (changeItemStatus delLedger→insertLedger).
- **Ledger accounting note (data interpretation, not a code bug)**: importing BOTH the stock snapshot AND open inbound orders means completing an imported inbound order adds a second set of IN ledger rows for goods the snapshot already counts (stock rows get replaced, not doubled, but ledger shows IST-… + Inbound IN rows). For a real go-live, either import inbound orders pre-completed (snapshot only) or treat imported inbound orders as already-received. Flagged for the user rather than silently re-engineered.
- **bintransfer.addLedger arity**: 13 columns vs 14 VALUES expressions (\CURRENT_DATE\ + \$1..\, 12 params). Matched PHP's \CURDATE() + 12 ?\. Balance for both TRANSFER_OUT and TRANSFER_IN stays the pre-transfer currentBalance (PHP parity) — do NOT "fix" to currentBalance±qty.
- **nginx proxy for /index.php**: the SPA always calls \\/index.php?module=…\ (frontend/src/lib/api.ts). A \proxy_pass http://api:3000/index.php\ under \location /k-one/api/\ rewrites \/k-one/api/index.php\ → \/index.phpindex.php\ (nginx replaces the location prefix with the proxy_pass URI). Fix: prefix locations with explicit \ewrite …/index\.php$ /index.php break\ + no-URI \proxy_pass http://api:3000\. Validated end-to-end against a live API through a scratch nginx container.
- **TS incremental builds in Docker**: with \incremental: true\, copying the local \	sconfig.build.tsbuildinfo\ into the image (via \COPY apps apps\) made \
est build\ skip JS emit (dist had only .d.ts + assets) → runtime MODULE_NOT_FOUND. Fixed with \**/*.tsbuildinfo\ in .dockerignore. Also added \**/node_modules\, \**/dist\, logs to .dockerignore (build context was shipping node_modules before).
- **API image must self-migrate**: prod compose has no migrate step and the API doesn't migrate on boot; also migrate.ts compiles to dist but the .sql files never reached dist. Added nest-cli \compilerOptions.assets\ (database/migrations/*.sql → dist) and api.Dockerfile CMD \
ode dist/database/migrate.js && node dist/main.js\ (idempotent). Validated: fresh stack boots, schema_migrations gets 001-schema.sql, seed present.
- **Full prod stack validated** in an isolated compose project (\-p k2v\ + override renaming containers/ports) so the running dev infra (k2-postgres/k2-redis host 5544/6389) was untouched; torn down cleanly after.
- **Next**: S18 — reconcile ledger-vs-stock accounting decision (see note above), consider auto-completing imported inbound orders, and any follow-ups the user picks from the S17 findings.


## 17. Decision log - S18 ledger-vs-stock reconciliation (2026-08-13)
- **Decision (user chose Option 1)**: treat the stock snapshot as reality; imported inbound orders are ALREADY-RECEIVED historical documents. The snapshot provides stock + a single `Stock Import` ledger IN set; imported inbound orders are written as `status='Completed'` with items `in_process_status='ATP'`, `stock_status='Accepted'`, WITHOUT materializing new stock/ledger. This removes the double-ledger-count flagged in S17 without re-engineering the import.
- **Change**: status `'Dues In'`->`'Completed'` / items `'Pending','Dues In'`->`'Accepted','ATP'` in all 3 import sites (identical SQL):
  1. worker `import.engine.ts` `autoCreateInbound` (async `import::auto_async` path)
  2. api `import.service.ts` `autoCreateInbound` (sync `import::auto` path)
  3. api `import.service.ts` `runInbound` (sync `import::inbound` path)
  Manual inbound creation (`inbound.service.ts create`) is untouched (still Draft/Dues In).
- **Validated end-to-end** on isolated scratch DBs (dev infra untouched; only dev API :3000 + dev worker remain running):
  - Async worker path (scratch worker on redis db1 + scratch DB k_one_opt1): IN-202608-0001 status Completed, item ATP/Accepted, `stock_ledger` has exactly 1 row (`Stock Import`), stock 30@C-01-01, outbound Open w/ 1 item. Also surfaced a pre-existing quirk: `importGetter` is exact-key so the toy schedule sheet's `ship to name` never matches the engine candidates -> auto import falls back to any existing customer (needs >=1 customer in DB). Not touched (orthogonal; real SAP export uses a recognized column).
  - Sync `import::auto` (scratch API :3002 / k_one_opt2): same result (Completed, ATP/Accepted, single Stock Import ledger row).
  - Sync `import::inbound` (same API): SHP-9001 status Completed, 2 items ATP/Accepted, no Inbound ledger rows.
  - No `Inbound` reference_type ledger rows in any scratch DB (double-count eliminated).
  - Scratch stacks + DBs (k_one_opt1/k_one_opt2) + redis db1 flushed + temp enqueue script removed.
- **Note**: if a future workbook contains genuinely-new (not-yet-received) inbound orders, Option 1 would pre-mark them Completed and stock would be missing for them (snapshot-only). Current data (all past-dated GR docs) is consistent; revisit if forward-dated orders appear.
- **Next**: none outstanding; dev API (:3000) + worker rebuilt and running with Option 1.


## 18. Decision log - import header-alias fix (2026-08-13)
- **Context**: while validating S18 Option 1, the toy workbook surfaced a pre-existing import quirk: header resolution is exact-key, so a schedule sheet header of `ship to name` never matched the engine''s ship-to candidates (`name of ship-to party` / `ship-to party` / `destination`) -> auto import silently fell back to any existing customer (or threw ''Tidak ada customer'' on an empty DB). Similarly `master` sheets using a plain `code` column were skipped (''kolom Material tidak ditemukan'').
- **Fix (additive aliases, no behaviour change for existing headers)**: added shared header-alias constants to both helper copies (`packages/shared/src/import-helpers.ts` + `apps/api/src/import/import.helpers.ts` - API keeps a local copy): `SHIP_TO_NAME_KEYS` (adds `ship to name`, `ship-to name`), `SHIP_TO_LOC_KEYS` (adds `ship to location`, `ship-to location`), `MASTER_PRODUCT_CODE_KEYS` (adds plain `code` at lowest priority). All 12 outbound ship-to resolution sites (worker `getAlt`+`ga` x3 each, api same) now use the constants; master code resolution uses `[...MASTER_PRODUCT_CODE_KEYS]` in both files.
- **Validated** (scratch DB k_one_opt3, NO pre-seeded customer, isolated worker on redis db1): master sheet processed (products_created 1, ''Master data: 1 produk baru''), customer auto-created from `ship to name` -> `PT Test Dua` (code OUT-PTTESTDU, city Gresik), outbound OUT-202608-0001 references customer_id 1, inbound still Completed/ATP (Option 1 intact), `stock_ledger` still a single `Stock Import` row. Scratch stack + DB dropped, redis db1 flushed.
- **Supersedes** the ''Not touched (orthogonal)'' remark in Decision log 17.
- **Next**: none outstanding; dev API (:3000) + worker rebuilt and running.

## 19. WMS Upgrade plan — deepseek-wms-upgrade-prompt.md (2026-08-14)

- Source-of-truth file: `D:\K-one-v2\deepseek-wms-upgrade-prompt.md`. Full per-phase spec with v2 file references: **`docs/spec-4-wms-upgrade.md`**. Tracking: `todo.md` S19–S23.
- Five phases, run order **1 → 5 → 2 → 3 → 4** (audit-trail early, since Phase 1 adds a new ledger-mutation path that must follow the same discipline):
  1. **Stock Hold / Quarantine** — `stock.hold_status` (available|on_hold|quarantine|damaged) + hold_reason/hold_by/hold_at; `stock::hold` / `stock::release` actions; exclude non-available from ALL picking/allocation; stock_ledger on each hold/release; UI buttons on StockPage.
  2. **Barcode Scanning UX** — single-query barcode/SKU lookup action; autofocused scan-input (Enter-submit) in PicklistDetail/InboundDetail/BinTransferPage; mismatch → error + override logged. No camera scanning unless asked.
  3. **Replenishment Suggestions** — new `pick_face_targets` table; below-min_qty report + bulk/reserve candidates (non-hold); suggestion-only; one-click "Create Transfer" → bin_transfer.
  4. **Wave Planning** — new `waves` + `wave_orders` tables; consolidated multi-order picklist (location-sorted); must show `picklist.service.ts createFromOutbound` in full first; additive (single-order flow stays).
  5. **Audit Trail Hardening** — grep every service for stock/stock_locations writes; verify same-tx ledger; report gaps before fixing.
- **Current state**: none of the 5 phases started. All S1–S18b parity work complete and documented in this file + todo.md.
- **Working notes for implementers**: routing is the single-gateway dispatcher pattern (`registry.ts` + `gateway.controller.ts`), NOT REST controllers. Schema changes go directly into `001-schema.sql` or a new numbered migration (applied via `migrate.ts`). No test suite — check before assuming Jest. Give complete files, list files before editing, flag assumptions.

## 20. Decision log — Phase 1 Stock Hold (2026-08-14) — DONE

- **Migration `003-stock-hold.sql`** (+ base `001-schema.sql` updated for fresh installs): `stock` gained `hold_status VARCHAR(20) NOT NULL DEFAULT 'available'` CHECK `available|on_hold|quarantine|damaged`, `hold_reason TEXT`, `hold_by BIGINT REFERENCES users(id)`, `hold_at TIMESTAMP`. Widened `stock_ledger.transaction_type` CHECK to include `HOLD`,`RELEASE` (0-qty rows so balance math unaffected — same `SUM(in)-SUM(out)` balance source as `Outbound::addToLedger`). Applied to dev DB via `npm run migrate` (schema_migrations 003 row).
- **Actions** (`apps/api/src/stock/stock.actions.ts`, both `write` permission): `stock::hold {stock_id,status,reason}` and `stock::release {stock_id,reason?}`. Guards: status must be `on_hold|quarantine|damaged`, reason required on hold, 400 if already in that status / not held / missing stock (Indonesian messages). Each writes a `HOLD`/`RELEASE` ledger row (running balance, notes `Stock {id} di-hold (status): {reason} oleh {user}`) + `activity_log` `STOCK_HOLD`/`STOCK_RELEASE`.
- **Picking exclusion** — held stock (`hold_status != 'available'`) excluded from all 5 FEFO/allocation queries: outbound `getAvailableStock`/`getTotalAvailableQty`/`pickItems`, import `fefoAllocation` (both api `import.service.ts` and worker `import.engine.ts`), picklist `createFromOutbound` (allocations JOIN + fallback stock_locations SELECT). Used `(hold_status='available' OR hold_status IS NULL)` so pre-migration rows stay pickable.
- **Deliberate non-exclusion**: `stocktake autoLoadByLocations` STILL loads held stock (a physical count should include it). `bintransfer` also untouched (moving held stock between locations is legitimate). Documented in spec-4.
- **Frontend**: `StockPage.tsx` — Hold Status column (StatusBadge styles added for available/on_hold/quarantine/damaged), hold/quarantine modal (status select + required reason), release modal (optional reason), action buttons gated by `canWrite`.
- **Live verification** (dev API :3000, admin token): hold→detail→FEFO exclusion (total_available 596→548 = exactly the 48-unit held row)→release→detail. Edge cases (bad status, missing reason, not-held release, missing stock) all correct Indonesian 400s. Ledger HOLD+RELEASE rows + activity_log rows confirmed in DB.
- **Pre-existing note (NOT Phase-1 scope)**: `activitylog::list` returns `[]` — the frontend sends `module` as both the route and the filter param, and `activityList` reads `ctx.query.module`, so the unfiltered call filters `WHERE al.module='activitylog'` → no rows. This matches the PHP original (`reports.php handle_activitylog` reads `query('module')`), so it is genuine parity behavior, not a regression. Flagged here for a future decision.
- **Next**: S20 = Phase 5 Audit Trail Hardening (grep every service for stock/stock_locations writes; verify same-tx ledger; report gaps first).

## 21. Decision log — Phase 0 Department-Based Roles (2026-08-14) — DONE

- **Source**: `new-roles.md` (confirmed permission matrix inside). Foundational access/routing/UI layer; must NOT change business logic. Tracking: `todo.md` "Department-Based Roles & Dashboards (Phase 0)" section.
- **Migration `004-departments.sql`** EXISTS + APPLIED to dev DB (verified: schema_migrations row + `users.department` column, default 'all', CHECK `inbound|outbound|inventory|all`). Do not recreate.
- **Confirmed mapping**: module-level `auth`=none; `inbound`=inbound; `outbound`=outbound; `picklist`=outbound; `stocktake`=inventory; `bintransfer`=inventory; `stock`=inventory; `ledger`=inventory; `dashboard/report/activitylog/system`=`all`; `import/export/print`=`all`; `products/customers/locations/users`=`all`. Read-only lookups opened: `customers::all`→outbound (OutboundList picker), `locations::all`→inventory (BinTransferPage picker) — both verified pure-read. Product search: user chose to open `inbound::search_products` to `['inbound','inventory']` (StockTakeDetail:122 + BinTransferPage:178 call it) instead of adding stocktake/bintransfer search_products actions.
- **Backend DONE**: `guards.ts` (`CurrentUser.department`, `DEPARTMENTS`, `isDepartment`, `resolveUser` SELECTs `u.department`); `auth.actions.ts` login user payload includes `department`; `registry.ts` added `setModuleDepartments`/`setActionDepartments`/`getDepartments` (override>module-default, unset=open); `gateway.controller.ts` enforces `depts && user.department!=='all' && !depts.includes(user.department)` → 403.
- **Backend module rules DONE**: applied in all action constructors — `inbound`→['inbound'] (+`search_products`→['inbound','inventory']), `outbound`→['outbound'], `picklist`→['outbound'], `stocktake`→['inventory'], `bintransfer`→['inventory'], `stock`+`ledger`→['inventory'], `dashboard`/`report`/`activitylog`/`system`→['all'], `import`→['all'], `export`+`print`→['all'], `master` `products`/`customers`/`locations`/`users`→['all'] with overrides `customers::all`→['outbound'] and `locations::all`→['inventory'].
- **Backend user CRUD DONE**: `master.actions.ts` `userList` now SELECTs `department` + returns `departments` options list; `userCreate`/`userUpdate` accept+validate `department` via `isDepartment` (default 'all'), include it in INSERT/UPDATE + activity log text.
- **Frontend DONE**: `api.ts` (`User.department`, `Department` type, `DEPARTMENTS` const, `departmentHome()`); `AuthContext` (`department` normalized default 'all'; `login` returns the `User`); `Login.tsx` redirects to `departmentHome(u.department)`; `App.tsx` (`/` → `HomeRedirect`, `/dashboard` = combined, `/dashboard/{inbound,outbound,inventory}` = new pages); new `pages/DashboardInbound|Outbound|Inventory.tsx` (KPI cards + work-queue tables, reuse Card/EmptyState/StatusBadge); `Layout.tsx` (nav `depts` filtering per department, added **Picklist** link, dept badge in header); `UsersPage.tsx` (department column + form select, payload/toggleActive include department).
- **Verification DONE**: api typecheck ✓, web typecheck ✓, api `nest build` ✓, web `vite build` ✓ (pre-existing chunk-size warning only).
- **Live manual QA DONE (dev API :3000, `npm run dev`)**: created QA users per dept via `users::create`; `users::list` returns `department` + `departments` options; all allowed actions OK and all cross-dept actions 403 (inbound↛outbound/stock/customers/locations/picklist; outbound↛locations/inbound/stocktake; inventory↛outbound/inbound); read-only exceptions verified (`customers::all`→outbound OK, `locations::all`→inventory OK, `inbound::search_products`→inventory OK); dept `all` user still blocked from `users::list` (admin-role check, not dept); `userCreate` rejects invalid dept with Indonesian 400; QA users deleted after. Dev server stopped.
- **Design notes**: `department==='all'` always passes the guard (supervisor/admin). Frontend nav filtering is UX-only; real security is the backend guard (manual test = dept user hitting another dept's action directly via API must get 403). No cross-department READ views in this phase. Scanning bundled with its supporting action, no `can_scan` flag.
- **Next (resume point)**: Phase 0 is complete. Next scheduled work per todo.md is S20 = Phase 5 Audit Trail Hardening.

## 22. Decision log — S20 = Phase 5 Audit Trail Hardening (2026-08-14) — DONE

- **Scope** (spec-4 lines 53-68): grep every `apps/api/src/**/*.service.ts` for UPDATE/INSERT against `stock`/`stock_locations`; confirm a same-tx `stock_ledger` write for each; report gaps FIRST, propose fix matching existing pattern, then implement; include the S19 hold/release path.
- **Verified OK (stock+ledger in same tx)**: outbound `ship` (OUT per item via `addToLedger`) + whole-order `delete` (restores stock AND deletes its OUT rows — internally consistent); bintransfer `execute` (TRANSFER_OUT/IN); stocktake `applyAdjustment` (ADJUSTMENT); inbound `deleteItem` (OUT 'Inbound-Reversal') + `delete`; import `stockCommitTx` fresh-INSERT path (IN) + `outboundProcessSheet` (`reduceStockForOutboundItem`+`addOutboundLedger`); stock `adjustStock`, `hold`, `release` (HOLD/RELEASE, migration 003 widened CHECK); picklist touches no stock/ledger. Documented exceptions confirmed: inbound `complete` (no ledger — real-time receive/status-change + `regenerateLedger` write it), outbound `pickItems` (no ledger — ship writes it), stock `transfer` (activity_log only).
- **Gap 1 — inbound `changeItemStatus` NOT atomic**: stock deletes/inserts and `stock_ledger` writes (`delLedger`/`runningBal`/`insertLedger`) all ran via separate `this.db.query` calls (no tx). **Fix**: wrapped the entire stock+ledger block (status UPDATE → helpers → ATP/Goods Received/Unserviceable/Dues In branches) in `db.transaction(client => …)`; helpers now use the tx `client`.
- **Gap 2 — import `stockCommitTx` replace/add modes had NO ledger**: only the fresh-INSERT path wrote a ledger row; `replace`/`add` UPDATEs on existing stock were silent. **Fix**: new private `addImportLedger(q, row, delta, refNo)` — writes `IN`/`OUT` 'Stock Import' row (delta = new−old for replace, +row.quantity for add; skips |delta|<0.001; balance = Available SUM, mirroring insert path; refNo `IST-YYYYMMDD-NNNN`). Both modes now call it.
- **Gap 3 — outbound `deleteItem` on Shipped order leaked OUT ledger rows**: `wasPicked` includes `'Shipped'`, and the action only blocks `Completed`; restoring stock without removing the ship-time OUT row desynced `stock_ledger` vs `stock`. (Whole-order `delete` deletes its OUT rows; `deleteItem` never did.) **Fix**: in `deleteItem`, when `item.order_status === 'Shipped'`, `DELETE FROM stock_ledger WHERE reference_type='Outbound' AND reference_id=$1 AND product_id=$2` (mirrors the whole-order delete) before removing the item.
- **Borderline (kept activity_log-only — user "proceed")**: outbound `changeItemStatus` Unserviceable↔ATP moves qty Available↔QUA_SHELL stock rows with no ledger write. It's a zero-sum status move (no net IN/OUT) and the action layer writes `activity_log` `UPDATE_OB_ITEM_STATUS`, so it satisfies "ledger OR activity_log consistently".
- **Verification**: root `npm run typecheck` (api+worker+web) clean; api `nest build` clean. No behavioral tests exist (no suite in repo).
- **Next (resume point)**: S21 = Phase 2 Barcode Scanning UX (single-query scan action; scan-input mode in PicklistDetail/InboundDetail/BinTransferPage; mismatch → override with reason → activity_log). Ask which barcode formats before writing code.

## 23. Decision log — S21 = Phase 2 Barcode Scanning UX (2026-08-14) — DONE

- **Scoping Q&A (user answers)**: (1) scan formats = **internal `product_code` only** (no EAN/UPC column, no schema change); (2) scan action = **auto-confirm expected item** — picklist auto-fills qty/status for the matched Pending row, inbound putaway / bin transfer are **validate-only**; (3) mismatch override = **yes, override + reason logged to activity_log** (never silently accept).
- **Backend `stock::scan`** (`stock.actions.ts`, permission `any`, dept override `['inbound','outbound','inventory']` so outbound/picklist + inbound + inventory/bintransfer depts all pass the Phase-0 guard): ONE SQL query — exact match on `products.product_code` AND `is_active=1` → returns product `{id, product_code, product_name, uom_type, uom_per_pallet, default_location}` plus `expected_location` = FEFO-first Available (non-held, qty>0, not QUA_SHELL/STAGING) stock location ordered by `expiry_date ASC NULLS LAST`, falling back to `default_location`; unknown code → `{found:false, code}`. No N+1.
- **Backend `stock::scan_override`** (permission `write`, same dept override): requires `code` + `reason` (400 `Kode wajib diisi.` / `Alasan override wajib diisi.`), logs `SCAN_OVERRIDE` via ActivityLogger with `context` (`picklist:PKL…` / `inbound:IN…` / `bintransfer:new`) in old/new JSON; returns `{ok:true}`.
- **Frontend `components/ScanInput.tsx`** (new): single autofocused text input, submits on Enter, clears + refocuses after each scan (USB scanner = keyboard). Used by all three pages, per spec (NOT a modal).
- **Page integrations**:
  - `PicklistDetail.tsx`: scan auto-confirms the **next `Pending`** item → `picklist::update_item` full qty + status `Picked` (+ toast with qty/uom). "Berikutnya: …" hint under the input. Mismatch → red banner (AlertTriangle) showing scanned vs expected + required reason field + **Override & Lanjut** (calls `stock::scan_override` then auto-confirms) / Batal.
  - `InboundDetail.tsx`: scan validates against the **putaway target** (first Dues In/Goods Received item); matching row highlighted `emerald` ring; mismatch → override with reason.
  - `BinTransferPage.tsx`: scan **auto-selects** the product when none is chosen (reuses `selectProduct` → loads `locations_with_stock`), else validates against the selected product; green "Produk cocok" indicator; mismatch → override with reason.
- **`report.service.ts`** actionLabel map gained `SCAN_OVERRIDE: 'Override Scan (Mismatch)'` (nicer activity-log display).
- **Verification (live, dev API :3000)**: `stock::scan` unknown code `TEST1` → `{found:false}`; real code `550058593` → `{found:true, product:{…}, expected_location:'CD33E02'}`; `stock::scan_override` POST `{code, reason, context}` → `{ok:true}` and the `SCAN_OVERRIDE` row confirmed in `activity_log` (id 35). Root `npm run typecheck` (api+worker+web) clean; api `nest build` clean.
- **Known pre-existing quirk (NOT caused here)**: `activitylog::list` returns empty rows — the action reads `ctx.query.module` which doubles as the route module, so it always filters `module='activitylog'` (matches PHP parity). Verified directly in DB that activity rows (incl. SCAN_OVERRIDE) exist.
- **Next (resume point)**: S22 = Phase 3 Replenishment Suggestions (`pick_face_targets` table; pick-face below min_qty + non-hold bulk sources; suggestion-only page; one-click pre-filled bin transfer).

## 24. Decision log — S22 = Phase 3 Replenishment Suggestions (2026-08-14) — DONE

- **Scope** (spec-4 Phase 3): new `pick_face_targets` table; query/report of pick-face locations below `min_qty` + candidate bulk/reserve sources with available (non-hold) stock; suggestion-only; one-click "Create Transfer" per row pre-fills a bin_transfer; new Replenishment page. Depends on S19 `hold_status` (confirmed in place).
- **Migration `005-replenishment.sql`** (applied to dev DB; `001-schema.sql` updated for fresh installs): `pick_face_targets(id BIGSERIAL PK, location_id BIGINT→location_master, product_id BIGINT→products, min_qty/max_qty NUMERIC(10,2) CHECK >=0, created_at, updated_at, UNIQUE(location_id, product_id))` + `idx_pft_location`/`idx_pft_product`.
- **Backend `replenishment` module** (`replenishment.service.ts` / `.actions.ts` / `.module.ts`, wired into `app.module.ts`). Module dept `['inventory']` (matches stock/bintransfer/stocktake). Actions:
  - `replenishment::list` — ONE query: per target, `current_qty` = SUM Available non-hold stock at the pick-face location (`stock_status='Available'` AND `(hold_status='available' OR hold_status IS NULL)` AND qty>0); returns only `current_qty < min_qty` rows with `shortage = min_qty − current_qty`. Sources: second grouped query per product (non-pick-face Available non-hold locations, FEFO `MIN(expiry_date) ASC NULLS LAST`, each with available_qty / earliest_expiry / batch_count), then per-suggestion filtered to exclude the pick-face location itself. Numbers int/float-coerced in TS (fixed a `Number(undefined)→NaN→null` bug).
  - `replenishment::targets` — management list (location/product names + zone/aisle/row + current pick-face qty).
  - `replenishment::save_target` (write) — upsert on (location_id, product_id); 400s for missing location/product, negative qty, or `max_qty < min_qty`; logs `SAVE_PICK_FACE_TARGET`.
  - `replenishment::delete_target` (write) — logs `DELETE_PICK_FACE_TARGET`.
- **Frontend `ReplenishmentPage.tsx`** (route `/replenishment`, Layout nav under inventory+all w/ `PackageSearch`, pageTitle map entry): suggestions table (pick-face / product / current / min / shortage / source chips with qty+expiry+batch, "Tidak ada sumber stok tersedia" when empty) + per-row **Create Transfer** button → modal pre-filled (from = first FEFO source, qty = `min(shortage, source.available)`, to = pick-face, reason "Replenishment pick-face (otomatis)") that calls the existing `bintransfer::create`. Separate **Target Pick-Face** management table (add/edit/delete modal: location select from `locations::all`, product picker from `inbound::search_products`, min/max qty). Suggestion-only — no auto-execute.
- **Verification (live, dev API :3000)**: created target (loc 65 CA01A01, product 5, min 50/max 100) → `replenishment::list` returned the suggestion (current 0, shortage 50, 13 FEFO-ordered sources); created + executed `bintransfer` 20 units CD33E02→CA01A01 → list showed current 20 / shortage 30 (end-to-end loop verified); `targets` list correct; `delete_target` → suggestions empty; validation 400s correct (unknown location, max<min). Root typecheck + api `nest build` + web `vite build` clean. Test target deleted after QA (transfer left as real dev data).
- **Next (resume point)**: S23 = Phase 4 Wave Planning (new `waves` + `wave_orders` tables; wave-creation → single consolidated picklist across orders grouped/sorted by location; show `picklist.service.ts createFromOutbound` in full before changing; single-order flow must stay working).

## 25. Decision log — S23 = Phase 4 Wave Planning (2026-08-14) — DONE

- **Scope** (spec-4 Phase 4): new `waves` + `wave_orders` tables; wave-creation action producing ONE consolidated picklist across multiple outbound orders (grouped/sorted by location for pick-path efficiency); single-order flow untouched; Web UI wave-creation screen with multi-select.
- **Schema (migration `006-waves.sql`**, applied to dev DB; `001-schema.sql` updated for fresh installs with the waves tables defined BEFORE picklists): `waves(id, wave_number UNIQUE NOT NULL, status CHECK Planning/Active/Completed/Cancelled, carrier VARCHAR(100), cutoff_time TIMESTAMP, created_by→users, created_at)`; `wave_orders(wave_id→waves ON DELETE CASCADE, outbound_order_id→outbound_orders ON DELETE CASCADE, PRIMARY KEY(wave_id,outbound_order_id))`; `picklists.wave_id BIGINT→waves` + `idx_picklists_wave`; `ALTER TABLE picklists ALTER COLUMN outbound_order_id DROP NOT NULL` (a wave picklist spans many orders → NULL; single-order picklists keep setting it — additive, verified).
- **`picklist.service.ts` refactor (behavior-preserving for single-order flow)**: extracted the per-item generation loop from `createFromOutbound` into private `insertPicklistItems(client, picklistId, outboundIds)` using `WHERE oi.outbound_order_id = ANY($1::bigint[])` (FEFO `exp_date ASC, oi.id ASC` order preserved; still creates one picklist_items row per allocated stock_locations row, else per pallet-distribution row). New public `createFromOrders(outboundIds, createdBy, waveId, client?)` — inserts header with `outbound_order_id=NULL` + `wave_id`, calls `insertPicklistItems`; accepts an optional transaction client so the waves module runs wave + orders + picklist in ONE transaction (otherwise opens its own). `getById`/`getAll` INNER→LEFT JOIN outbound_orders and now return `wave_number`/`wave_carrier`/`wave_cutoff` (wave picklists render with a link to `/waves`).
- **Backend `waves` module** (`waves.service.ts`/`waves.actions.ts`/`waves.module.ts`, imports `PicklistModule`, wired into `app.module.ts`). Module dept `['outbound']` (matches picklist); `create`/`cancel` = write; actions `list`/`detail`/`create`/`cancel`/`candidate_orders`.
  - `create` — body `{order_ids[], carrier?, cutoff_time?}`; validates `order_ids` non-empty (400), all orders exist (400 listing missing ids), then filters to `status='Open'` AND no existing picklist; ineligible reported back as `skipped` (create still succeeds; 400 if none valid). Single transaction: wave header (`Planning`) + wave_orders rows + `picklist.createFromOrders(validIds, createdBy, waveId, client)`. Logs `CREATE_WAVE`.
  - `cancel` — 400 if not found or status `Completed`; deletes the wave's picklist only while `Draft` (a progressed picklist is preserved), then wave → `Cancelled`. Logs `CANCEL_WAVE`.
  - `candidate_orders` — Open orders with no picklist, with customer + item/qty totals (feeds the multi-select).
- **Frontend `WavesPage.tsx`** (route `/waves`, Layout nav under outbound+all with `Layers` icon, pageTitle entry): wave list table (wave_number, StatusBadge, carrier, cutoff, order_count, item_count, picklist link → `/picklist/:id`, created_by, detail eye + Cancel when not Completed/Cancelled); New Wave modal (searchable multi-select of candidate orders showing order/SO/DO/customer/destination/qty/armada/container, carrier, cutoff `datetime-local`); detail modal (orders table with outbound links + picklist link/status). `PicklistList.tsx`/`PicklistDetail.tsx` render wave picklists (wave_id) as a link to `/waves`; `StatusBadge` gained `Planning`/`Active`.
- **Verification (live, dev API :3000)**: temporarily flipped orders 18/19 → Open. `candidate_orders` returned both; `create` [18,19] → wave_id 1, picklist_id 1, skipped [] → `waves::detail` showed both orders + picklist; `picklist::detail` on id 1 → 5 items from BOTH orders, location-sorted (CC06A02, CC07A01, CE02A01, CE06D01, CE06D02), header `outbound_order_id=null`/`wave_number=WAV-202608-0001`; single-order `picklist::create_from_outbound` (order 18) still works → id 2; second `create` [18,19] → skipped [18] (already had picklist); validation 400s correct (`order_ids wajib diisi.`, `Terdapat outbound order tidak ditemukan: 99999`); `cancel` both waves → draft picklists deleted (picklists table empty) + waves `Cancelled`; `activity_log` has CREATE_WAVE (with skip note) + CANCEL_WAVE rows; dept guard correct (inventory user 403, outbound operator 200). Cleanup: orders restored to `Shipped`, temp test scripts deleted. Root typecheck + api `nest build` + web `vite build` all clean (pre-existing chunk-size warning only).
- **Next (resume point)**: S19–S23 (Phases 1, 5, 2, 3, 4) all DONE. Remaining spec-4 items after Phase 4 if any (e.g., print/export parity for wave picklists, mobile picker for wave picklists) — verify against `docs/spec-4-wms-upgrade.md` "Remaining/Risks" sections.

## 26. Decision log — S24 = Phase 6 ASN (Advance Shipping Notice) (2026-08-15) — DONE

- **Scope** (`deepseek-wms-remaining-gaps-prompt.md` Phase 6): suppliers notify inbound of expected shipments in advance so receiving staff can prep. Confirm-vs-expect model — the ASN stays `Pending` until a linked inbound completes, then flips to `Received`. Explicitly OUT OF SCOPE (decision): labor/task management (picker assignment, per-user productivity) and multi-warehouse/multi-site (no `warehouse_id`/`site_id` anywhere).
- **Schema (migration `009-asn.sql`**, applied to dev DB via `npm run migrate`; `001-schema.sql` deliberately untouched — fresh installs get the tables via the ordered migration runner): `asn(id, asn_number UNIQUE, supplier_name, supplier_reference, expected_arrival_date DATE, status CHECK Pending/Received/Cancelled DEFAULT 'Pending', notes, created_by→users, created_at, updated_at)` + indexes; `asn_items(id, asn_id→asn ON DELETE CASCADE, product_id→products, expected_qty NUMERIC(10,2), uom DEFAULT 'Drum', batch_number, exp_date, created_at)` + indexes; `ALTER TABLE inbound_orders ADD COLUMN IF NOT EXISTS asn_id BIGINT REFERENCES asn(id) ON DELETE SET NULL` + index.
- **Backend `asn` module** (`asn.service.ts`/`asn.actions.ts`/`asn.module.ts`, imports nothing special, wired into `app.module.ts`; module dept `['inbound']`, `create`/`update`/`cancel` = write): `generateNumber` (`ASN-YYYYMM-NNNN`, pad 4 via number-gen); `list` (`{rows,total,page,per_page(50),statuses:[Pending,Received,Cancelled]}` with created_by_name/total_items/expected_qty LEFT JOINs); `detail` (`{asn, inbound_orders}` — asn includes `items` joined to products); `create` (single tx: header default `Pending` + addItem loop; rejects 0 items 400 `Minimal 1 item wajib diisi.`); `update`/`cancel` reject unless `Pending` (409 Indonesian messages); activity_log `CREATE_ASN`/`UPDATE_ASN`/`CANCEL_ASN`. Added `asn` to `activity-logger.getModules()`.
- **Inbound ASN integration (`inbound.service.ts`)**:
  - `create()` — when body has `asn_id`: validates ASN exists (404) + `status='Pending'` (else 409 `Hanya ASN berstatus Pending yang dapat dijadikan inbound.`); INSERT now includes `asn_id` (16th column); when `data.items` is empty AND `asn_id` given, server-side pre-fills items from `asn_items` (expected_qty→quantity, uom, batch_number, exp_date, `in_process_status='Dues In'`). No auto-complete — receiving staff still explicitly submits the create form.
  - `complete()` — inside the same tx, after marking the inbound Completed, `UPDATE asn SET status='Received' WHERE id=$1 AND status='Pending'` for a linked `asn_id`.
- **Export**: new `export::asn` action → `excel.asnReport(status)` — per-line XLSX (asn_no/supplier/ref/arrival/product/qty/uom/batch/expiry/status); `ExcelExportService` gained an `AsnService` dependency (ExportModule imports AsnModule).
- **Frontend**: `AsnList.tsx` (route `/asn`, Layout nav "ASN" under inbound+all, `CalendarClock` icon, pageTitle entry) — status filter, table (asn_no/supplier/ref/expected_arrival/items/qty/status/created_by/View), **New ASN** modal (supplier info + expected items with product search, qty, uom, batch, expiry); `AsnDetail.tsx` (route `/asn/:id`) — header info, expected items table, linked inbound orders, Edit (Pending only), Cancel (not Received/Cancelled via ConfirmButton), **Create Inbound** → `/inbound?asn_id=<id>`; `StatusBadge` gained `Received` style.
- **`InboundList.tsx` `?asn_id` pre-fill**: reads `useSearchParams`, on mount fetches `asn::detail`, rejects non-Pending ASNs with a toast, pre-fills the create form (carrier_name←supplier_name, expected_date←expected_arrival_date, notes, items with quantity←expected_qty and `Dues In`), shows a linked-ASN info banner with a link back to the ASN, includes `asn_id` in the create body, navigates to `/inbound/:id` after create.
- **Tests** (`apps/api/test/asn.e2e-spec.ts`, 7 passing): create+list+detail ASN; no-items → 400; inbound from ASN pre-fills expected item (qty 12); non-Pending ASN link → 409; full inbound lifecycle (advance_status → update_item_status ATP → complete) flips ASN → `Received`; cancel only while Pending; update of Received → 409. `TRANSACTIONAL_TABLES` in helpers gained `asn`,`asn_items`. Full suite 9 suites / 35 tests pass. Typecheck (api + web) + api `nest build` clean.
- **Environment note**: Docker Desktop + dev infra (k2-postgres :5544, k2-redis :6389) were down at session start; started Docker Desktop and `docker compose -f infra/docker-compose.dev.yml up -d`, then ran `npm run migrate` (009 applied to `k_one`). The pre-existing "Force exiting Jest / ioredis Connection is closed" teardown noise is unrelated to ASN.
- **Next (resume point)**: run order for remaining gaps is **Phase 8 → 10 → 7**. Phase 8 = S26 (next session), per `deepseek-wms-remaining-gaps-prompt.md` — one phase per session, migration `010-*`. Do NOT run more than one phase in a single session.

## 27. Decision log — S26 = Phase 8 ABC Analysis / Velocity-Based Ranking (2026-08-15) — DONE

- **Scope** (`deepseek-wms-remaining-gaps-prompt.md` Phase 8): rank products by OUT volume and bucket into A/B/C by cumulative volume (standard ABC). OUT OF SCOPE (decision): auto-relocation based on class (classification is advisory only) — plus the standing exclusions labor/task management and multi-warehouse/multi-site.
- **Schema (migration `010-abc-analysis.sql`**, applied to dev DB via `npm run migrate`; `001-schema.sql` untouched): `ALTER TABLE products ADD COLUMN velocity_class VARCHAR(1) CHECK (velocity_class IN ('A','B','C'))` + `velocity_class_at TIMESTAMP`, both nullable. Recomputed periodically by admin, not real-time — so nulls are meaningful (idle/unclassified), never forced to C.
- **Backend `abc` module** (`abc.service.ts`/`abc.actions.ts`/`abc.module.ts`, wired into `app.module.ts`; dept `['all']`, `analyze` = read, `recompute` = admin): `analyze` read-only preview; `recompute` same ranking but writes `velocity_class` + `velocity_class_at` in one tx + `RECOMPUTE_ABC` activity log (label added to `report.service.ts` as `'Recompute Analisis ABC'`, moduleIcon `abc: 'fas fa-chart-pie'`; `activity-logger.ts` gained `'abc'`).
  - `parseSplit` (a+b ≤ 100 else 400, c = 100−a−b), `parseRange` (default last 90 days), `volumePerProduct` (single grouped `stock_ledger` query, OUT only), `productLookup` (batch name/code fetch).
  - `bucket` — cumulative-share bucketing; key decision: the **top mover always lands in A** when any A band exists (a single product driving 100% of volume is a fast mover, not the long tail). Zero-activity products stay `NULL`.
- **Surface**: `stock::list` SELECT now includes `p.velocity_class` (products list/detail already exposed it via `p.*`).
- **Frontend**: `StatusBadge` gained `A`/`B`/`C` styles (red/amber/gray). `ProductsPage.tsx` — Velocity column + admin-only **ABC Analysis** button → modal (date range + split A/B inputs, `analyze` preview, A/B/C/unclassified/total-picked KPI cards, ranked table with cumulative %, admin **Recompute & Simpan** writes + refreshes). `StockPage.tsx` — Velocity column.
- **Tests** (`apps/api/test/abc.e2e-spec.ts`, 6 passing): non-admin recompute → 403 — note `global-setup.ts` only seeds `testadmin` (role `admin`), so the spec creates its own `operator` user (`bcryptjs.hashSync` INSERT, role `operator`) and logs in as it for the negative case; analyze ranks 70/20/10 → A/B/C; custom split 50/50; invalid split 60/60 → 400; recompute writes classes (idle product stays NULL); products/stock lists expose `velocity_class`. Full suite 10 suites / 41 tests pass. Typecheck (api + web) + api `nest build` + web `vite build` clean.
- **Next (resume point)**: remaining run order is **Phase 10 → 7**. Phase 10 = S28 Cycle Count Scheduling (next session), migration `011-*`. One phase per session — do NOT run more than one phase.

## 28. Decision log — S28 = Phase 10 Cycle Count Scheduling (2026-08-15) — DONE

- **Scope** (`deepseek-wms-remaining-gaps-prompt.md` Phase 10): turn ad-hoc stock takes into a recurring, schedulable cycle-count program, optionally velocity-weighted using S26's `velocity_class`. OUT OF SCOPE (decision): a real cron/background scheduler in this phase — a manual admin **Run Due Schedules** action that checks `next_run_date <= today` is the trigger. Future hook-in: a BullMQ repeatable job in `apps/worker` calling the same `CycleCountService.runDue()` entry point (documented in the service). Labor/task management + multi-warehouse remain standing exclusions.
- **Schema (migration `011-cycle-count-schedules.sql`**, applied to dev DB via `npm run migrate`; `001-schema.sql` untouched): `cycle_count_schedules(id, schedule_name, frequency CHECK weekly/monthly/quarterly, scope_type CHECK full/location/velocity, scope_locations TEXT (JSON array), velocity_class CHECK A/B/C, next_run_date DATE NOT NULL, is_active BOOL DEFAULT TRUE, created_by→users, created_at, updated_at)` + index `idx_cycle_count_schedules_due (is_active, next_run_date)`. `scope_type` mirrors stock_take semantics plus the new `velocity` scope (S26 weighting). `ALTER TABLE stock_take ADD COLUMN schedule_id BIGINT REFERENCES cycle_count_schedules(id) ON DELETE SET NULL` — generated takes link back to their schedule.
- **Backend `cyclecount` module** (`cyclecount.service.ts`/`cyclecount.actions.ts`/`cyclecount.module.ts`, wired into `app.module.ts`; dept `['inventory']`, `create`/`update`/`delete` = write, `run_due`/`run_now` = admin):
  - `list` — `{rows}` with `created_by_name`, computed `is_due` (active AND `next_run_date <= CURRENT_DATE`), `total_generated` (LEFT JOIN stock_take ON schedule_id).
  - `create`/`update` — validate schedule_name, frequency, scope_type; location scope requires non-empty scope_locations; velocity scope requires velocity_class A/B/C; `next_run_date` must be `YYYY-MM-DD`.
  - `delete`; `runDue` — active schedules with `next_run_date <= CURRENT_DATE` ordered by date; `runNow` — a single schedule regardless of due date, advancing from today.
  - **Generation reuses the EXISTING `StockTakeService.create()`** (per the prompt's explicit requirement — never reimplemented): take_date=today, status `Draft`, notes `Dibuat otomatis dari jadwal: <name> (<frequency>)`, `scope_locations` for location scope, `schedule_id`; then `autoLoadByLocations(id, locs | null | velocityClass)`; then advances `next_run_date` (weekly +7d, monthly +1m, quarterly +3m).
  - `StockTakeService.create()` gained optional `schedule_id` + `scope_type` override params (scope_type override lets a velocity-scope take record its scope even with no locations); `autoLoadByLocations()` gained an optional `velocityClass` filter (`AND s.product_id IN (SELECT id FROM products WHERE velocity_class=$n)`) so velocity-scoped schedules only auto-load that class.
- **Wiring**: added `LOCK_KEYS.cycleCount()` to `@k-one/shared` — **note: shared is consumed from its dist, so `npm run build -w @k-one/shared` must run before API tests** — and `run_due`/`run_now` are serialized through `RedisLockService.runLocked(...)` (fail-open, matches stocktake pattern). Report labels `CREATE_CYCLECOUNT`/`UPDATE_CYCLECOUNT`/`DELETE_CYCLECOUNT`/`RUN_CYCLECOUNT` + moduleIcon `cyclecount: 'fas fa-calendar-check'`; `activity-logger.ts` gained `'cyclecount'`.
- **Frontend**: `CycleCountPage.tsx` (route `/cycle-count`, Layout nav "Cycle Count" under inventory+all, `CalendarCheck2` icon, pageTitle + ActivityLog module icon added) — table (name / frequency / scope + velocity StatusBadge / next_run_date / Due pill / Active-Inactive / total generated / created_by / actions), admin **Run Due Schedules** button (toast reports generated take numbers), per-row **Run Now** (admin) + Edit + Delete (ConfirmButton); New/Edit modal with name, frequency, scope type, location multi-select (`stocktake::get_scope_locations`), velocity class A/B/C, next_run_date, active toggle.
- **Tests** (`apps/api/test/cyclecount.e2e-spec.ts`, 10 passing): non-admin run_due/run_now → 403 (dedicated `cc_operator` role `operator` dept `inventory`); create+list (is_due true, created_by_name `Test Admin`); create validation (empty name / bad frequency / location-without-locs / velocity-without-class → 400); run_due generates a stock_take via StockTakeService.create (linked schedule_id, note contains name, single pre-loaded item qty_system 10), advances next_run_date +7d, and re-run produces 0 once advanced past today (dates computed relative to today in the test); not-yet-due → 0; run_now runs a future-dated schedule and advances; run_now missing → 404; velocity-scope schedule auto-loads only the A-class product (C-class stock excluded); update + delete. Full suite 11 suites / 51 tests pass. Typecheck (api + web) + api `nest build` + web `vite build` clean.
- **Next (resume point)**: only **Phase 7 (S25) — Cross-Docking** remains, migration `012-*`. One phase per session — do NOT run more than one phase. Cross-docking is the most invasive (touches the existing inbound status flow), so re-read `deepseek-wms-remaining-gaps-prompt.md` Phase 7 + `inbound.service.ts` status flow before starting.

## 29. Decision log — S25 = Phase 7 Cross-Docking (2026-08-15) — DONE

- **Scope** (`deepseek-wms-remaining-gaps-prompt.md` Phase 7, lines 106–130): route specific incoming stock straight to an outbound shipment without putaway, for pre-allocated/pre-sold inbound stock. OUT OF SCOPE (standing exclusions): labor/task management (picker assignment, per-user productivity) and multi-warehouse/multi-site.
- **Hook confirmed with user BEFORE coding** (prompt requirement): cross-dock triggers at the **ATP transition** inside `inbound.service.ts changeItemStatus` (Dues In → Goods Received → **ATP**) — the point where a normal receive would allocate putaway stock. The flag lives as a **nullable column directly on `inbound_items`** (no separate join table).
- **Schema (migration `012-cross-dock.sql`**, applied to dev DB via `npm run migrate`; `001-schema.sql` untouched): `ALTER TABLE inbound_items ADD COLUMN IF NOT EXISTS cross_dock_outbound_order_id BIGINT REFERENCES outbound_orders(id) ON DELETE SET NULL` + `idx_inbound_items_cross_dock`. **Reuses the existing `STAGING` special location** (already seeded in 001/007, excluded from general stock counts) — no new location table.
- **Backend `inbound.service.ts`**:
  - `addItem`/`updateItem` persist `cross_dock_outbound_order_id`.
  - `changeItemStatus(itemId, 'ATP', createdBy?)` — when the item is flagged cross-dock it **bypasses normal putaway/FEFO**: sets item ATP/Accepted/`location='STAGING'`, writes an IN ledger `[Inbound] Cross-Dock | STAGING → Outbound | <no>` (location STAGING), inserts a `stock_locations` row at STAGING (stock_id NULL, `inbound_item_id` set so `complete()` preserves it, qty=totalQty, pallet_seq 1, status Available), and calls new `PicklistService.addCrossDockItem(...)`. Non-cross-dock ATP keeps the normal ledger path.
  - `getItems` LEFT JOINs `outbound_orders` to expose `cross_dock_order_number`/`cross_dock_order_status`.
  - `inbound.actions.ts` `detail` now also returns `cross_dock_orders` (Open/Picking/Picked outbound list joined to customers — feeds the UI selector) and `update_item_status` passes `ctx.user.id` as `createdBy`. `InboundModule` imports `PicklistModule` (no cycle).
- **Backend `picklist.service.ts`**: new `addCrossDockItem(client, outboundOrderId, productId, quantity, uom, batchNumber, createdBy, stockLocationId)` — finds-or-creates the outbound's Draft picklist and inserts ONE `picklist_items` row (Pending, `location='STAGING'`, linked to the matching `outbound_items`), returns the picklist item id.
- **Backend `outbound.service.ts`**: `getItems` gains a LATERAL join exposing `cross_dock_inbound_item_id`/`cross_dock_batch`/`cross_dock_inbound_number` per line (drives the UI flag + auto-ATP logic); the auto-toggle stock probe **includes STAGING** for cross-docked lines (else it'd flip them back to Goods Received); `pickItems` — cross-docked lines pick **from STAGING only** (`AND location = 'STAGING'`), normal lines keep excluding STAGING.
- **Frontend `InboundDetail.tsx`**: "Cross-dock ke Outbound Order" selector in the Add Item modal (optional; options from `cross_dock_orders`, hint explaining the FEFO/putaway bypass); items table Location cell shows a violet **CROSS-DOCK** badge + `→ <order_number> @ STAGING` when flagged. `OutboundDetail.tsx`: Location cell shows a violet **CROSS-DOCK** badge + `via <inbound_number> @ STAGING` on arriving cross-dock lines.
- **Tests** (`apps/api/test/crossdock.e2e-spec.ts`, 4 passing): (1) pre-sold outbound (created without general stock) + inbound item flagged cross-dock → ATP stages at STAGING with NO general `stock` row, STAGING stock_location exists, Draft picklist + Pending STAGING picklist_item attached to the outbound, inbound detail surfaces `cross_dock_order_number` + `cross_dock_orders`, outbound detail flags `cross_dock_inbound_item_id`/number; (2) `complete()` keeps it staged at STAGING (all stock rows at STAGING) and the outbound line auto-resolves to ATP; (3) set/clear via `update_item`; (4) Completed outbound excluded from the selector options. Note: node-postgres returns BIGINT as string — tests coerce with `Number()`. Full suite **12 suites / 55 tests** pass (the Redis "Connection is closed" force-exit noise is pre-existing). Typecheck (api + web) + api `nest build` + web `vite build` clean.
- **Status**: all phases from `deepseek-wms-remaining-gaps-prompt.md` are now complete (6, 8, 10, 7). Remaining session work: final `todo.md` + `session.md` write-ups (done), optional final full-suite re-run. No outstanding phases.

## 30. Decision log — Dashboard Upgrade, Session 1: Inbound (2026-08-16) — DONE

- **Source**: `deepseek-v4-prompt-dashboard-upgrade.md` (4 sessions, one role per session — this is Session 1 only). Rationale: the three Phase-0 department dashboards predate ASN (S24), waves (S23), hold/quarantine (S19), replenishment (S22), cycle count (S28), ABC (S26), cross-dock (S25) — none of that data surfaced on them. This phase catches each dashboard up additively (no redesign, no removal of existing KPI cards/tables, Bahasa Indonesia UI text).
- **`DashboardInbound.tsx` current state (155 lines, read before coding)**: mounts `inbound::stats` + `inbound::list` in ONE `Promise.all`; 4 KPI cards (Inbound Bulan Ini / Dues In / Receiving / Selesai Diterima) + one pending-orders table (Order/Status/Tanggal/Shipment/Carrier/Items/Qty). Note: there is NO putaway-queue section in this file (prompt assumed one) — only stats + the pending table, so "only add to it" was trivially satisfied.
- **`asn::list` contract confirmed**: registered action; supports `status` + `per_page`/`page`; each row = `asn_number, supplier_name, supplier_reference, expected_arrival_date, status, total_items, expected_qty, created_by_name`; sorted `created_at DESC` (no sort param) → dashboard sorts client-side by `expected_arrival_date ASC` (nulls last) for the top-10.
- **User decision (asked per prompt)**: `inbound::list` rows carry NO cross-dock flag today, so the CROSS-DOCK badge source had to be added. User approved the single-query option: `inbound.service.ts getAll()` gained `COUNT(*) FILTER (WHERE ii.cross_dock_outbound_order_id IS NOT NULL)::int AS cross_dock_count` — a 1-line aggregate change, NOT a per-row `inbound::detail` loop (which the prompt explicitly forbade).
- **Implemented**:
  - Backend: `inbound.service.ts` `getAll()` cross_dock_count aggregate (only change; no action-level changes).
  - Frontend `DashboardInbound.tsx` (full replacement): 5th violet KPI card "ASN Pending" (`CalendarClock`); "ASN — Pending Kedangatan" Card below with top-10 table (link → `/asn/:id`); pending-orders table gained a "Cross-Dock" column showing a violet badge when `cross_dock_count > 0`; KPI grid `md:grid-cols-4`→`md:grid-cols-5`.
  - **Per-section resilience** (per the prompt's final-pass rule, applied proactively in Session 1): the ASN fetch is its own then-chain with independent `asnLoading`/`asnError` state — a slow `asn::list` (read-heavy across a newer table) cannot block the main dashboard's render; the ASN card shows `…` while loading and its Card shows its own small spinner/error instead of failing everything.
- **Manual smoke note**: login as inbound (or `all`) user → `/dashboard/inbound` → 5 KPI cards render, ASN table lists Pending ASNs by expected arrival, CROSS-DOCK badge shows on inbound rows with a cross-docked item. (No e2e required — dashboards are read-only views.)
- **Verification**: api typecheck ✓, web typecheck ✓, api `nest build` ✓, web `vite build` ✓ (pre-existing chunk-size warning only).
- **Next (resume point)**: Session 2 — Outbound Dashboard (Active Waves KPI + top-10, incoming cross-dock flag on open-orders table; confirm `waves.actions.ts` action names + `outbound::list` fields first). Run one session at a time.

## 31. Decision log — Dashboard Upgrade, Session 2: Outbound (2026-08-16) — DONE

- **`DashboardOutbound.tsx` current state (205 lines, read before coding)**: mounts `outbound::stats` + `outbound::list` + `picklist::list` + `picklist::stats` in ONE `Promise.all`; 4 KPI cards (Outbound Bulan Ini / Order Open-Picking / Picklist Aktif / Picklist Selesai) + two side-by-side tables (Open/Picking orders, Active picklists). Existing sections preserved verbatim; only added to them.
- **`waves::list` contract confirmed** (`waves.actions.ts`): registered actions `list|detail|create|cancel|candidate_orders`, module dept `['outbound']` (so the outbound dept passes the Phase-0 guard); `list` accepts a single `status` filter + `per_page`/`page`; each row = `w.*` (wave_number/status/carrier/cutoff_time/created_by/created_at) + `created_by_name` + `order_count`/`item_count` + linked `picklist_id`/`picklist_number`/`picklist_status`. Sorted `created_at DESC` (no sort param) → dashboard sorts client-side by `cutoff_time ASC` (nulls last) for the top-10.
- **User decision (asked per prompt)**: `outbound::list` rows carry NO cross-dock flag (the S25 LATERAL join lives only in the detail/`getItems` path). User approved adding it to the list. Implementation note — unlike Session 1 (where a FILTER aggregate worked because `inbound_items` was already joined), `getAll` here has no `inbound_items` join, and a naive LEFT JOIN would MULTIPLY rows and corrupt `SUM(oi.actual_qty)`/`COUNT(DISTINCT oi.id)`. Used a scalar correlated subquery `(SELECT COUNT(*)::int FROM inbound_items cdi WHERE cdi.cross_dock_outbound_order_id = o.id) AS cross_dock_count` — row-count-safe, single query, no per-row loop.
- **Implemented**:
  - Backend: `outbound.service.ts` `getAll()` scalar-subquery `cross_dock_count` (only change).
  - Frontend `DashboardOutbound.tsx` (full replacement): 5th indigo "Wave Aktif" KPI card (`Layers`); "Waves — Aktif / Belum Selesai" Card below the two side-by-side cards with top-10 table (wave_number → `/waves`, StatusBadge, carrier, cutoff_time via `fmtDate`, order_count); open-orders table gained a "Cross-Dock" column showing a violet badge when `cross_dock_count > 0`; KPI grid `md:grid-cols-4`→`md:grid-cols-5`.
  - **Per-section resilience** (same rule as Session 1): the `waves::list` fetch is its own then-chain with independent `waveLoading`/`waveError` state — a slow waves query can't block the main outbound/picklist dashboard.
- **Manual smoke note**: login as outbound (or `all`) user → `/dashboard/outbound` → 5 KPI cards render, Waves table lists non-completed waves sorted by cutoff, CROSS-DOCK badge shows on open orders with an incoming cross-docked item.
- **Verification**: api typecheck ✓, web typecheck ✓, api `nest build` ✓, web `vite build` ✓ (pre-existing chunk-size warning only).
- **Next (resume point)**: Session 3 — Inventory Dashboard (Held/Quarantine KPI via `stock::list`, Replenishment KPI + top-10, Cycle Counts Due KPI; confirm the registered action names + fields for `stock.actions.ts`, `replenishment.actions.ts`, `cyclecount.actions.ts` first, and whether StockPage/CycleCountPage support query-param filters). Run one session at a time.

## 32. Decision log — Dashboard Upgrade, Session 3: Inventory (2026-08-16) — DONE

- **`DashboardInventory.tsx` current state (192 lines, read before coding)**: mounts `stocktake::list` + `bintransfer::list` in ONE `Promise.all`; 4 KPI cards (Total Stock Take / Bulan Ini / Avg Akurasi / Bin Transfer Pending) + two side-by-side tables (Stock Take Berjalan, Bin Transfer Pending). Existing sections preserved verbatim; only added to them.
- **Action/field confirmation (read all three before coding)**:
  - `stock.actions.ts` registered actions `list|summary|expiring|by_location|detail|locations|transfer|adjust|hold|release|scan|scan_override`; `list` supports `status/expiring/q/location/year` ONLY — **no hold_status param**. Rows carry `s.*` incl. `hold_status` (default `available`, nullable) → held count = client-side filter `hold_status && hold_status !== 'available'`.
  - `replenishment.actions.ts` registered `list|targets|save_target|delete_target|detect|suggest|generate|for_demand`; `replenishment::list` → `{suggestions:[...]}` where each row = `target_id/location_id/pick_face_location/product_id/product_code/product_name/uom_type/uom_per_pallet/min_qty/max_qty/current_qty/shortage/sources[]` (already only below-min rows, non-hold sources).
  - `cyclecount.actions.ts` registered `list|create|update|delete|run_due|run_now`; `cyclecount::list` → `{rows:[...]}` where each row = `ccs.*` (schedule_name/frequency/scope_type/velocity_class/next_run_date/is_active/...) + `created_by_name` + server-computed `is_due` (CASE `is_active AND next_run_date <= CURRENT_DATE`) + `total_generated` → "due" = rows where `is_due` truthy (no re-derivation client-side).
- **No backend changes in this session** — all three data sources already exist; the session is purely additive frontend (lower risk than S29/S30).
- **Implemented (`DashboardInventory.tsx` full replacement)**:
  - 3 new KPI cards rendered as `<Link>`s (unlike the existing plain divs — new cards only, so the existing 4 are untouched): red "Stok Di-Hold / Karantina" → `/stock`, teal "Replenishment Dibutuhkan" → `/replenishment`, fuchsia "Cycle Count Jatuh Tempo" → `/cycle-count`.
  - 2 new tables: "Replenishment — Dibutuhkan" (top-10 by `shortage DESC`; columns produkt code+name / lokasi / stok / min / kekurangan) and "Cycle Count — Jatuh Tempo" (schedule_name / frequency / next_run_date), each in the existing `xl:grid-cols-2` layout below the stock-take/bin-transfer pair.
  - KPI grid `md:grid-cols-4` → `md:grid-cols-4 xl:grid-cols-7` (7 cards).
  - **Per-section resilience**: each new fetch (stock / replenishment / cyclecount) is its own then-chain with independent loading/error state — notably the full-table `stock::list` (no pagination) can't block the main dashboard.
- **Flag (gap noted, not fixed — out of scope)**: `StockPage` supports only `?q=` and `?year=` query params; there is NO hold-status filter param, so the Held card links to plain `/stock` rather than a pre-filtered view. Worth a future small enhancement (a `?hold=1` param on StockPage).
- **Manual smoke note**: login as inventory (or `all`) user → `/dashboard/inventory` → 7 KPI cards render and populate; Replenishment table sorted by shortage; Cycle Count table lists due schedules only.
- **Verification**: api typecheck ✓, web typecheck ✓, api `nest build` ✓, web `vite build` ✓ (pre-existing chunk-size warning only).
- **Next (resume point)**: Session 4 — Admin/Combined Dashboard (cross-check Dashboard.tsx already surfaces ASN/waves/hold/replenishment/cycle-count; add "Recent Scan Overrides" via `activitylog` filtered to `action='SCAN_OVERRIDE`; ABC freshness indicator via `products.velocity_class`/`velocity_class_at`; show full Dashboard.tsx first). Run one session at a time.

## 33. Decision log — Dashboard Upgrade, Session 4: Admin/Combined (2026-08-16) — DONE

- **Dashboard.tsx current state (553 lines, read before coding)**: mounts `dashboard::stats` in ONE `Promise.all`; hero + 8 KPI cards (inventory health + today's ops), DashboardAlerts, FefoPriorityQueue, SmartInsights, Inbound/Outbound work-queue tables, Stock Summary, Monthly Activity chart, Aisle/Stock by Location + detail modal. Cross-check vs S29-S31: combined dashboard does NOT show ASN/waves/cross-dock/hold/replenishment/cycle-count — but those are the DEPARTMENT dashboards' coverage, so NOT duplicated (per "don't duplicate existing cross-department report widgets").
- **Action/field confirmation (read before coding)**:
  - `stock.actions.ts scanOverride` (~line 484): logs `action='SCAN_OVERRIDE'`, `module='stock'`, description `Scan mismatch di-override [ctx]: 'code' — reason`, `old_value={scanned, context}`, `new_value={reason}`. `activity_log` schema (001-schema.sql): `full_name`, `created_at`, `new_value` TEXT, etc.
  - `activitylog::list` supports ONLY `module` + `limit` (report.service.ts activityLogList) — NO action filter existed; module='stock' is scan-heavy so a small limit could push overrides out.
  - ABC freshness IS tracked: `products.velocity_class` + `products.velocity_class_at` (migration 010; set to NOW() by `abc::recompute`, NULL when never computed / after reset). But NO action exposed it: `products::all` (master-data productOptions) strips the columns; `products::list` is paginated + GROUP BY; `abc::analyze` computes on-the-fly from stock_ledger, doesn't read velocity_class_at.
- **Backend changes (both additive, same class as the S29/S30 user-approved changes)**:
  1. `report.service.ts activityLogList(module, action, limit)` — optional `AND al.action = $n`; `report.actions.ts activityList` reads `ctx.query.action`. Single existing caller updated to pass `null`.
  2. `abc.service.ts status()` + `abc.actions.ts` `status` action (admin-gated via `setPermission`) — `{total, classified, unclassified, last_computed_at}`.
- **Implemented (Dashboard.tsx, additive only; all existing widgets + Promise.all untouched)**:
  - Admin-only (`canAdmin`) grid `xl:grid-cols-2` placed before Stock Summary: "Recent Scan Overrides" (Waktu/Operator/Kode/Alasan from `activitylog::list` filtered to SCAN_OVERRIDE, top-10, link → /activity-log) + "ABC Analysis — Status ABC" (classified/total progress bar; freshness badge Terkini/Usang — stale if last_computed_at > 30 days old or never — plus "Belum dihitung" when NULL; CTA → /products where recompute lives).
  - Per-section resilience: both new fetches are separate then-chains with own loading/error state; non-admin short-circuits to loading=false so the section never renders.
- **Manual smoke note**: admin → `/dashboard` → both widgets populate; non-admin all-dept user sees no admin section, dashboard still loads.
- **Verification**: api typecheck ✓, web typecheck ✓, api `nest build` ✓, web `vite build` ✓ (pre-existing chunk-size warning only).
- **All 4 sessions complete.** No further resume point. No test files touched this session (full suite remains 12 suites / 55 tests).

## 34. Decision log — Connect `putaway::recommend` to the Inbound putaway UI (2026-08-16) — DONE

- **Source**: `k-one-v2-feature-guide(1).docx` §1b marks the putaway location SUGGESTION engine as **BUILT BUT NOT CONNECTED** — `recommendLocations()` exists and is tested, but the Inbound putaway screen never calls it; staff type locations blind and only get corrected on save by the connected `validatePlacement()` safety net (§1a).
- **Diagnosis (read the code before coding)**:
  - `putaway.actions.ts` registers `recommend` (query: `product_id`, `quantity`, `uom`, `uom_per_pallet`, `prefer_pick`, `force_level`) → `putaway.service.ts:74 recommendLocations()` returns `{success, message, product, uom, limits, rule, total_pallets, pallets:[{pallet_seq, quantity, is_full, location_code, zone_code, level, reason}]}`. Module dept `['inbound','inventory']`, readable.
  - Frontend callers of `api('putaway', ...)`: ONLY ZoningPage config screens (uom_limits, product_rules, zones, zone_aisles, aisle_map, bins). **`putaway::recommend` had ZERO frontend callers** — confirmed the docx claim. `InboundDetail.tsx` putaway UIs (Manage Pallet Locations modal at ~line 1032, Add Item modal Pallet Locations at ~line 1150) are plain text fields.
- **Fix (frontend-only in `InboundDetail.tsx`; NO backend change)**:
  - Manage Pallet Locations modal: `openPalletModal` now auto-calls `putaway::recommend` when the item has no saved pallet locations AND is not cross-docked, pre-filling suggested `location_code/quantity/is_full` per pallet (accept or override); added violet "Saran Lokasi (Putaway)" button (`Sparkles`) + `suggestPallet()` to re-run, with a small message line (`palletSuggestMsg`) showing the engine result.
  - Add Item modal: `suggestAddLocations()` button fires `putaway::recommend` from the selected product + quantity and fills `addLocations` rows.
  - Cross-docked items excluded from auto-suggest (they stage at STAGING, bypassing normal putaway/FEFO).
- **Verification**: web typecheck ✓, web `vite build` ✓ (pre-existing chunk-size warning only); no api change this task.
- **Manual smoke note**: inbound in Dues In → Manage Pallet Locations on an item with no saved locations → suggested bins pre-fill per zoning/UOM; edit or accept then Simpan; cross-docked items skip the suggestion.

## 35. Decision log — Fix Reset Operational Data FK error (2026-08-16) — DONE

- **Symptom** (user-reported): Reset Data → `system::reset_operational_data` throws `cannot truncate a table referenced in a foreign key constraint`.
- **Root cause**: `report.service.ts resetOperationalData()` truncates the Phase-0 operational tables only. The Phase-4 **waves** tables were never added — `wave_orders` (006-waves.sql) has `outbound_order_id REFERENCES outbound_orders(id)`, so `TRUNCATE outbound_orders` failed because `wave_orders` (not in the truncate list) referenced it.
- **Fix**: added `wave_orders` + `waves` to the truncate list (before `picklists`). Single `TRUNCATE ... RESTART IDENTITY` handles FK order between listed tables automatically.
- **Audit (verified all 001-012 `REFERENCES` into truncated tables)**: only `wave_orders → outbound_orders` was unlisted. `asn`/`cycle_count_schedules`/`pick_face_targets`/`product_putaway_rules` reference only master tables; `inbound_orders.asn_id` and `stock_take.schedule_id` point from truncated→non-truncated (allowed). `012` cross-dock FK is inbound_items↔outbound_orders, both listed.
- **Verification**: api typecheck ✓, api `nest build` ✓. Smoke: admin → /reset-data → reset succeeds; operational tables empty, master data intact.

## 36. Decision log — Putaway palletization must match the current system (per-SKU UPP) (2026-08-16) — DONE

- **User request**: current system's Excel files (`Warehouse Management System_11/12 Agustus 2026_.xlsx`) were analyzed; user asked: *"if there's 500 of 550025358 how did they get split in the putaway ... there's 48 items per pallet there's 36 80 — I want my putaway to recognize this pattern and make them just like the current system."*
- **Pattern extracted from live data** (Master SKU, Schedule of the day, picking of the day, WMS sheets):
  - Per-SKU UPP by pack size: `12*1L`→44, `12*0.8L`→48, `4*4L`/`3*5L`→36, `24*0.12L`→80, `1*209L`→4, `1*20L`→24, `1*770kg`→1, plus 1*15KG/18kg→24, 1*180kg→4, 24*1L→16, 10*0.25L→100, 1*1000L→1.
  - Split = `floor(qty/UPP)` full pallets + `qty % UPP` remainder; verified against Schedule (188→3×48+44, 304→8×36+16, 262→7×36+10).
  - Full pallets → own reserve bin (D/E cartons, B/C drums); remainder → Level A pick face. Example: 500 of 550025358 (UPP 44) = 11 reserve bins ×44 + 16 at A.
- **Decision** (user-confirmed): derive UPP from pack size (resolver + used by putaway & import), and use **80** (live WMS) for `24*0.12L` even though Master SKU says 68/85.
- **Implementation**:
  - `apps/api/src/common/pallet.ts` → `deriveUppFromPackSize(text)`: parses `qty*size unit` → UPP or null. 16-case node test → ALL PASS.
  - `putaway.service.ts recommendLocations()`: `upp = req.uom_per_pallet ?? deriveUppFromPackSize(product_name) ?? product.uom_per_pallet ?? 4`; returns effective `uom_per_pallet`. Engine split logic (full→reserve B–E, remainder→pick A) already matched the pattern — unchanged.
  - `import.helpers.ts importUomPerPallet(uom, productUpp, name?)` derives from name; all product-create + stock/inbound import sites pass the name so auto-created products store correct UPP.
  - `InboundDetail.tsx`: `putawayMsg()` shows "N pallet penuh @ UPP + sisa X ke pick-face" in the Saran Lokasi messages.
- **Verification**: api typecheck + `nest build` ✓; web typecheck + `vite build` ✓ (chunk warning pre-existing); resolver node harness 16/16 PASS.

## 37. Decision log - 3D rack map empty after stock/auto import (2026-08-16) - DONE

- **Symptom** (user-reported): the 3D warehouse map (Zoning -> putaway::bins) stays empty even though stock / auto import reports success.
- **Root cause**: the 3D map renderer `putaway.service.ts listAllBins()` reads `location_master LEFT JOIN stock_locations (status Available/Reserved) LEFT JOIN stock LEFT JOIN products`. But `import.service.ts stockCommitTx()` (shared by both `stock_commit` AND `auto`) wrote **stock rows only** and never created **stock_locations** rows, so every bin renders quantity 0 / unoccupied. Only inbound-created stock_locations (via saveItemLocations/savePalletLocations) ever showed up.
- **Fix** (inside `stockCommitTx`, all four paths):
  - NEW `syncLocations()` helper: ensures a matching stock_locations row at `stock.location` (uppercased) with qty, uom, batch, status (Available unless stock_status=Reserved), `is_full_pallet` (qty >= effective UPP) and `pallet_function` (PICK_FACE for Level A, else RESERVE). If stock_locations rows already exist for the stock (e.g. inbound-created), it scales their quantities to the new total instead of duplicating, preserving pallet structure.
  - Wired into insert / replace / add / skip paths; skip backfills rows for stock imported before this fix.
- **Verification**: api typecheck + `nest build` clean. No migration needed. Re-running the same auto-import now backfills stock_locations, so the 3D map populates.

## 38. Decision log - Inbound Items table: one row per pallet/location (like the spreadsheet) (2026-08-16) - DONE

- **User request**: with two example inbounds (IN-202608-0056 = 41 items, IN-202608-0065), user showed that auto-imported orders list ONE row per bin (same product repeated, each with its own location/qty). For a NEW manual inbound of 500 qty, the putaway split is hidden under a single product row - user wants the Items table to show each pallet's actual location, "split into several same product like in spreadsheet".
- **Root cause**: auto-import (autoCreateInbound) creates one inbound_items row per WMS row (per location) - that is why auto-imported orders render per-bin. Manual save_pallet_locations keeps 1 item row and only writes stock_locations rows (visible only via Manage Pallet Locations modal); nothing expanded them into visible rows.
- **Decision**: frontend-only expansion in InboundDetail.tsx - each item with saved pallet_locations renders one row per pallet (same product repeated, showing the pallet's Qty/Actual, its Location, and "Pallet N"). No backend change, no migration. Kept single-row fallback when an item has no saved locations; header count uses the expanded row count.
- **Verification**: web typecheck + vite build clean (chunk warning pre-existing). Smoke: manual inbound 500 qty -> Manage Pallet Locations -> save -> Items table shows one row per bin.

## 39. Decision log - Auto-putaway on inbound add; remainder always PICK_FAST (Level A) (2026-08-16) - DONE

- **User request**: inbound items were landing in UNALLOCATED instead of bulk. User confirmed product/putaway rules were fine and requested: "make the whatever max SKU the bulk and the rest thats not reaching max SKU automatically becomes PICK_FAST (level A)".
- **Root cause**: inbound addItem only created stock_locations when pallet_locations or item.location were passed. An item added with just product+qty left location NULL and no bin rows; on completion the stock build fell back to `loc = item.location ?? 'UNALLOCATED'`. The putaway recommend engine already split full pallets -> reserve B-E, remainder -> Level A, but nothing auto-ran it at add time.
- **Fix**:
  - putaway.service.ts recommendLocations(): remainder target is now ALWAYS PICK_LEVEL (A) (was `pickAllowed ? A : reserveLevels[0]`) so any qty not reaching max SKU/UPP automatically becomes PICK_FAST; full pallets still go to bulk/reserve.
  - inbound.service.ts addItem(): when no pallet_locations AND no item.location, auto-calls putaway.recommendLocations and saves suggested bins via saveItemLocations - manual inbounds now auto-land in bulk / PICK_FAST.
- **Verification**: api typecheck + nest build clean. No migration.

## 40. Decision log - Moved Rack Map 2D/3D from Zoning tab to Locations tab (2026-08-16) - DONE

- **User request**: opinion asked whether rack map 2D + 3D belong on the Locations tab rather than Zoning. Agreed - the maps are a visual of the locations themselves, not zoning rules.
- **Fix (frontend only)**:
  - NEW components/RackViews.tsx: extracted 2D (putaway::aisle_map, aisle x level grid + bin detail) and 3D (putaway::bins, Rack3D + detail panel) views with all state/loading logic.
  - LocationsPage.tsx: 3 tabs - Locations List / Rack Map (2D) / Rack View (3D); "New Location" button only on list tab; renders <RackViews tab />.
  - ZoningPage.tsx: removed the two map tabs + now-unused state (aisleRows/aisleBins/aisleDetail/loadingMap/bins/hoveredBin/selectedBin), loadMap/loadBins/openAisleDetail, Row helper, Map/Boxes/Rack3D/Bin3D imports. Zoning is now rules-only: UOM / Produk & Putaway Rules / Zones / Zone Aisles.
- **Verification**: web typecheck + vite build clean. No backend change, no migration.
